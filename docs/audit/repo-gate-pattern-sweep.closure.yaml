# Closure ledger — Unit 2: repo-wide sweep for the 3 gate-tripping content
# patterns diagnose b3f9e7 hit. Gate 40 (validate_audit_closure.dart). One
# terminal_state per finding; no `deferred:` key. Under the §4.2 "≥4
# findings" mandatory-closure-YAML threshold (5 findings, but only 4 real
# fixes + 1 verified-clean) -- used anyway for clarity, spanning 3
# independently-gated patterns, matching how b3f9e7's own 3 bugs were
# tracked. Branch: repo-gate-pattern-sweep. Diagnose e7c3b9.

batch: repo-gate-pattern-sweep
diagnose: docs/diagnoses/2026-08-03-repo-gate-pattern-sweep-e7c3b9.md
total_findings: 5
closed_count: 5

_commit: &wip "repo-gate-pattern-sweep (uncommitted at authoring)"

findings:
  - id: P1-1
    title: "wardroom/CLAUDE.md:94 bare §3 U7 resolves against root's unrelated §3 SCREENS by coincidence"
    terminal_state: closed_in_commit
    commit: *wip
    verification: "dart run scripts/check_claude_md_citations.dart -- PASS"
    notes: "reworded to name docs/superpowers/specs/2026-04-28-apk-test-4-hotfix-batch-design.md explicitly."
  - id: P1-2
    title: "20 sites in the PRESCRIPTIVE doc/skill zones cite a CLAUDE.md section that does not exist, or (2 of them) one that exists but is the WRONG section. The count was wrong THREE times before landing: the original sweep said 2, round-1 review found a 3rd, round-2 re-derived by grep and found 5 more, and the B-pass found the grep's own input set was 3 directories rather than the mandated-doc set -- adding naming_conventions.md x9 (CLAUDE.md §4.7 mandates reading it), LENS_REGISTRY.md:44 (§4.8), and path-mappings.md:21."
    terminal_state: closed_in_commit
    commit: *wip
    verification: "SCOPE (stated, because this claim was over-broad once already): swept = the PRESCRIPTIVE doc/skill zones a reader is directed into -- .claude/**, docs/naming_conventions.md (mandated by CLAUDE.md §4.7), docs/audit/AUDIT_PLAYBOOK.md + LENS_REGISTRY.md (mandated by §4.8), docs/playbook/**. NOT swept: in-code comments under lib/, test/, scripts/, supabase/, integration_test/ -- see OI-91. Root CLAUDE.md's real ## headings are exactly 0,1,2,2a,3,4,5,6,7 (grep -oE '^## [0-9a-z.]+' CLAUDE.md), so §19/§22/§15/§16/§11/§10/§9 are all dead. Completeness re-derived by grep over the swept set, NOT asserted in prose: grep -rnoE 'CLAUDE\\.md.{0,3}§[0-9]+[a-z]?(\\.[0-9]+)?' <swept paths> | grep -vE '§(0|1|2|2a|3|4|5|6|7)\\b' returns exactly 2 hits post-fix, both PROVENANCE not pointers (common-pitfalls.md:2 'source:' frontmatter recording what the file was migrated FROM; debugging/SKILL.md:502 which now explicitly says '§19, which no longer exists'). Zero prescriptive dead citations remain IN THE SWEPT SET. That filter is also structurally blind to the WRONG-BUT-LIVE class (a citation resolving to a real but incorrect section) -- two were found by reading rather than grepping: naming_conventions.md:293 cited '§6 -- Coding rules' when §6 is MULTI-TIER COVERAGE (coding rules are §4.4), and update-docs/references/path-mappings.md:21 pointed 'Discipline / process' at §3 (SCREENS) instead of §4. Both corrected."
    notes: "No gate covers ANY of these paths -- Gate 26 walks only root CLAUDE.md, AGENTS.md, lib/**/CLAUDE.md and supabase/**/CLAUDE.md, so .claude/ AND docs/ are both unwatched (round-1's note blamed '.claude/skills/ not walked', which was too narrow -- build-apk.md is under .claude/commands/ and AUDIT_PLAYBOOK.md is under docs/audit/). Targets chosen per site rather than uniformly: the SKILL.md:56,80 pair -> docs/playbook/common-pitfalls.md (bug-class 2.17's own prescription); build-apk.md:238 -> .claude/skills/debugging/SKILL.md §2.12, because common-pitfalls.md does NOT cover the exlog-key-duplicate content and pointing there would have swapped one dead pointer for a wrong-but-live one; SKILL.md:503 §22 -> CLAUDE.md §4.4 rule 22 (rule 22 is a numbered rule INSIDE §4.4, never its own section); writer-reader-drift-detector.md:7 §15 -> docs/sot_registry.yaml."
  - id: P1-3
    title: "Gate 26 heading/citation regexes have no letter-suffix support -- §2a citations validate only by accidental collision with §2, zero real protection"
    terminal_state: closed_in_commit
    commit: *wip
    verification: "dart run scripts/check_claude_md_citations.dart -- PASS, negative-control-confirmed discriminating (temporarily renamed the 2a heading, gate correctly FAILed with 4 broken citations, then restored -- git diff CLAUDE.md empty after)"
    notes: "widened both regexes in scripts/check_claude_md_citations.dart from \\d+(?:\\.\\d+)? to \\d+[a-z]?(?:\\.\\d+)?."
  - id: P2
    title: "restoring_screen.dart at the Gate 43 ceiling, zero margin, 28.75% comments"
    terminal_state: closed_in_commit
    commit: *wip
    verification: "wc -l confirms 791 (post-refresh; 824 -> 791, a 33-line reduction). NOT evidenced by the gate: main allow-lists this file, so check_god_screen_max_lines.dart short-circuits before the size check and would PASS at any length -- removing that entry is the OI-88 follow-up."
    notes: "extracted/trimmed 4 narrative comment blocks to short pointers at CLAUDE.md/diagnose-docs; 2 of the 4 also corrected content that had drifted stale (15s-vs-30s CTA timing) relative to the code."
  - id: P3
    title: "SoT registry enclosing-method-name-vs-interior-range (the 3rd b3f9e7 bug) -- sweep for siblings"
    terminal_state: verified_clean
    evidence: "562 entries checked across the 8438-line registry; zero recurrences. The 7 other _ensureLocalUser-adjacent entries are already correctly phrased as prose, confirming the original b3f9e7 fix generalized rather than being a one-off patch."
