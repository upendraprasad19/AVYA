# Test-pyramid audit — 2026-05-28

> Track 6 of the six-industry-gap closure batch.
> Generated by `scripts/audit_test_pyramid.dart`.
> Static classification only (no runtime data joined; rerun with `--runtime <flutter-test-json>`).

## Layer counts

| Layer | Count | % of total |
|---|---:|---:|
| unit | 146 | 33.6% |
| widget | 15 | 3.4% |
| contract | 218 | 50.1% |
| integration | 24 | 5.5% |
| e2e | 32 | 7.4% |
| **total** | **435** | 100.0% |

## Verification samples (5 random per layer)

Spot-check by reading these files; if classification looks wrong, the heuristics in `scripts/audit_test_pyramid.dart` need tuning.

### unit
  - `test/workout_write_service/edit_log_pr_rescan_test.dart` — signals: _(no salient signals)_
  - `test/ai_coach/ceremony_text_test.dart` — signals: _(no salient signals)_
  - `test/sync/custom_exercise_sync_test.dart` — signals: _(no salient signals)_
  - `test/sync/nutrition_redesign_sync_test.dart` — signals: _(no salient signals)_
  - `test/plan_engine_v4_test.dart` — signals: _(no salient signals)_

### widget
  - `test/goldens/wardroom/ward_dashed_border_golden_test.dart` — signals: pumpWidget
  - `test/wardroom/ward_rank_insignia_test.dart` — signals: pumpWidget
  - `test/promotion_celebration/overlay_renders_test.dart` — signals: pumpWidget
  - `test/goldens/wardroom/ward_set_chips_golden_test.dart` — signals: pumpWidget, setUpAll
  - `test/pro_pill_button_test.dart` — signals: pumpWidget

### contract
  - `test/contracts/graduation_stats_provider_field_test.dart` — signals: setUpAll
  - `test/contracts/restore_template_schedule_test.dart` — signals: setUpAll
  - `test/contracts/singleton_provider_invariant_test.dart` — signals: setUpAll
  - `test/contracts/load_all_exercise_prs_per_set_semantic_test.dart` — signals: setUpAll
  - `test/contracts/no_legacy_log_set_with_pr_rescan_declaration_test.dart` — signals: _(no salient signals)_

### integration
  - `test/edge_functions/pgvector_test.dart` — signals: imports supabase, setUpAll
  - `test/safety/hive_key_migrator_test.dart` — signals: imports hive, setUpAll
  - `test/supabase/auth_restore_test.dart` — signals: imports hive, setUpAll
  - `test/subscription/payment_grace_window_test.dart` — signals: imports hive, setUpAll
  - `test/subscription/state_changed_hook_test.dart` — signals: imports hive, setUpAll

### e2e
  - `integration_test/flows/razorpay_purchase_flow_test.dart` — signals: _(no salient signals)_
  - `integration_test/flows/plan_generator_to_first_workout_test.dart` — signals: _(no salient signals)_
  - `integration_test/flows/cross_account_isolation_e2e_test.dart` — signals: HiveUserSession
  - `integration_test/flows/regression/auth_regression_test.dart` — signals: setUpAll
  - `integration_test/critical_flows_test.dart` — signals: _(no salient signals)_

## Heuristics used

Implementation: `scripts/audit_test_pyramid.dart` (`classify` function).

| Layer | Signal |
|---|---|
| e2e | path starts with `integration_test/` |
| contract | path starts with `test/contracts/` |
| integration | imports `package:hive*` OR `package:supabase*`; uses `HiveUserSession`; calls `fakeAsync`; `setUpAll` opens boxes |
| widget | imports `package:flutter_test` AND calls `pumpWidget` (and not classified as integration above) |
| unit | everything else |

## Notes + recommendations

Phase 1 of this batch is data-first. No reorganization. Use the data above to inform:
1. Whether to tier `scripts/pre-commit.sh` (unit+widget at pre-commit, contract+integration at pre-push, e2e in CI) — separate brainstorm.
2. Whether to reorganize `test/` into layered subdirs.
3. Whether to adopt per-layer runtime budgets (gate `check_test_runtime_budget.dart`).
4. Top-20 slowest tests are the highest leverage for individual-test refactoring.
