---
reviewed_at: 2026-06-01T19:05:00+05:30
staged_against: 82547f6f23d5
blast_radius: platform
reviewer: claude-sonnet-fresh-subagent (B-pass)
lens_set: [shell_correctness, blast_radius_mismatch, secrets_in_tree, dart_test_vacuity, writer_reader_drift]
findings_count: 0
verdict: accepted
---

# Code Review (B-pass) — lean dev-workflow: blast-radius-tiered hooks (82547f6f23d5)

Staged diff: `scripts/pre-push.sh` + `scripts/pre-commit.sh` (the safety-net hooks),
`.claude/commands/build-apk.md` (`--from-green`), `.claude/skills/code-review/SKILL.md`
(wording fix), `CLAUDE.md` (§0/§4.3), two new `test/contracts/*` pinning tests,
`docs/adr/0013-blast-radius-tiered-gating.md` + `docs/handbook/process/tiered-gating-by-blast-radius.md`.

Fresh context-blind Sonnet subagent, focused on **shell correctness** of the two hooks (the
safety net). **Verdict: accept — NO findings (0 P0/P1/P2).** The reviewer ran live shell tests
to confirm each property.

## Validated clean (with live verification)
- **pre-push fail-safe is total.** The skip is positively gated (`[ "$TIER" = "feature" ]` —
  only the exact string `feature` skips). Every uncertain path — empty range, absent
  `origin/main`, blank/garbled tier, a `dart run` failure — routes to `run_full_suite`. No
  `pipefail` + a trailing `|| true` means a dart failure yields an empty `TIER` → `"unknown"` →
  the suite runs. `PRE_PUSH_FULL` is checked before any tier computation, so a dart failure can
  never bypass the override.
- **pre-commit failures cannot be lost.** `if ! dart run …` suppresses `set -e` so the
  failure-marker write always executes; a failing background job does not abort the parent;
  `wait || true` drains each bounded batch without propagating a job's exit; the empty
  `GATE_FAILDIR` glob is handled by the `[ -e "$MARK" ] || continue` guard; any marker →
  `GATE_FAIL=1` → `exit 1` blocks the commit.
- **The reminder is non-blocking.** No `exit`, no `*)` arm — an empty/unknown tier falls
  through silently. `run_full_suite` under `set -e` still aborts the hook (non-zero) on a red
  `flutter test` before reaching its `exit 0` — push correctly blocked.
- **Gate-33 markers preserved** — `scripts/check_*.dart` glob + the `case…esac` allowlist
  intact (confirmed live: Gate 33 PASS, 62 gates wired).
- **Dart tests are not vacuous** — assertions match post-comment-strip CODE tokens; removing a
  guarded construct fails the test.
- **secrets:** none. **reversibility:** `PRE_PUSH_FULL=1` + `git push --no-verify` + CI remains
  the full-suite backstop.

## Findings
None. Five candidate concerns were investigated and all verified **FALSE_ALARM** via live shell
testing (`set -e` + background subshell, the TIER pipeline under no-pipefail, the empty-FAILDIR
glob, the reminder block, the comment-stripper accuracy).

## Founder triage
No findings to triage. Platform-tier change, fully reversible (escape hatch + one-file reverts);
CI remains the full-suite source-of-truth. **Verdict accepted.**
