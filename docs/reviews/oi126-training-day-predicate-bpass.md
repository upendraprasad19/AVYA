---
reviewed_at: 2026-09-25T03:55:56+05:30
staged_against: main...oi126-training-day-predicate (13 commits)
blast_radius: platform
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink, guard_without_its_mirror, missing_input, asserted_fixture_value]
findings_count: 2
verdict: accepted
---

# Code Review — OI-126 training-day-predicate-unification (whole-branch)

## Summary of verification performed

- `git diff --name-only main...HEAD | dart run scripts/blast_radius_from_diff.dart -` → `Blast-radius: platform`. Confirms the record's self-declared tier.
- `flutter analyze lib/` (whole-tree, not per-file — none of the 8 touched `lib/` files are `part`/`part of` files, confirmed by absence of `part of` in each) → **45 issues, all `info`**, 0 warnings, 0 errors (`grep -c "warning"` / `grep -c "error"` on the raw output → 0/0, and a direct `grep -n "^warning\|^error\| error -\| warning -"` also returned nothing). None of the 45 infos are in a file this branch touches.
- `flutter test` on all 6 new/extended test files (`phase_adherence_rate_test.dart`, `phase_completion_training_type_test.dart`, `plan_engine_flags_logged_training_day_test.dart`, `reconciler_needs_heal_logged_test.dart`, `streak_warning_eligibility_logged_test.dart`, `training_day_predicate_wiring_test.dart`) → **24/24 pass**.
- `dart run scripts/validate_diagnose_doc.dart docs/diagnoses/2026-09-24-training-day-predicate-logged-disagreement-b7e3a1.md` → PASS.
- `dart run scripts/check_sot_registry_parity.dart` → PASS (0 errors; 1 pre-existing unrelated warning, `CoachMediaRepository` not in registry — not touched by this branch).
- `dart run scripts/check_sot_behavioral_test_paths.dart` (Gate 42) → PASS, all `behavioral_test_path` values resolve on disk.
- **Live line-citation audit**: every `file:line` in the diagnose-doc's `writers:`/`readers:` blocks and every `line_range:` for the new `training_day_predicate_logged_agreement` SoT concept was independently grepped against the live worktree. All 14 citations (2 writers + 12 wrapper call sites, plus the `isTrainingDayType`/`isPhaseCompletionTrainingType`/`isRestDayConsideringLogged` definition sites) match exactly — `workout_write_service.dart:510`, `sync/sync_workout.dart:985`, `train_provider.dart:637,813`, `workout_schedule_read_service.dart:1412`, `home_screen.dart:617,792`, `plan_integrity_reconciler.dart:99`, `home_provider.dart:104,376,679`, `day_detail_sheet.dart:47,105`, `weekly_calendar.dart:67`, `phase_completion.dart:59` / `80-81`, `plan_engine_flags.dart:499`. No line-citation drift found (the documented CLAUDE.md §4.9 pitfall class did NOT recur here).
- **Mutation test (performed live, not just read)**: edited `PlanEngineFlags.isRestDayConsideringLogged` to ignore the flag entirely (`return type != 'workout' && type != 'custom_template';` unconditionally), re-ran the 6 test files. Result: **exactly the 3 claimed real call-through tests reddened** (`phase_adherence_rate_test.dart`'s "a logged day counts as training once the OI-126 flag is on", `reconciler_needs_heal_logged_test.dart`, `streak_warning_eligibility_logged_test.dart`) — 21 pass / 3 fail, matching the diagnose-doc's `regression_test_planned` claim exactly. Reverted; `git status --porcelain` confirmed clean afterward.
- **Algebraic flag-OFF byte-identity check**: hand-verified De Morgan equivalence at all 12 call sites between the pre-fix inline expression and `PlanEngineFlags.isRestDayConsideringLogged`/its negation as used at each site. All 12 are byte-identical at flag OFF (see lens 3 below for detail).
- **13th-site search**: `grep -n "== 'workout'|== 'custom_template'|!= 'workout'|!= 'custom_template'"` across all of `lib/`. Found 9 additional hits beyond the 12 wired sites (`deload_evaluator.dart:89,207`, `template_service.dart:120,280`, `workout_schedule_read_service.dart:828`, `simulation_service.dart:354`, `workout_repository.dart:517,1110,1193`, `pattern_detector.dart:121`, `train_provider.dart:761`, `ai_snapshot_builder.dart:706`) — read each in context; all check `type == 'workout'` (or `!= 'workout'`) ALONE, deliberately excluding `custom_template`, for a semantically different question (e.g. "was this row generated by the plan engine, not a custom template", "is this specifically a plan-generated workout for exercise-name aggregation") — not the "is this a training day" predicate OI-126 addresses. No missed 13th site of the OI-126 class found.

## Finding 1 — P3 — missing_input (documentation drift)

- **file:line:** `docs/diagnoses/2026-09-24-training-day-predicate-logged-disagreement-b7e3a1.md` — `forbidden_patterns_checked` (2nd bullet) and `regression_test_planned` block.
- **claim:** The diagnose-doc's frontmatter says "all **11** ship in this batch" and "Real call-through coverage at 3 of the **11** sites... The other **8 of 11** sites rely on...". But the diagnose-doc's own body (the "Whole-branch review addendum — 12th call site" section, added in the same commit range) and the new `docs/sot_registry.yaml` concept description ("All **12** whitelist-shaped call sites now delegate to the single wrapper...") both correctly say 12. The 11-count in `forbidden_patterns_checked`/`regression_test_planned` predates commit `f7785e3f` (which wired the 12th site, `weekly_calendar.dart`) and was never updated when the addendum was appended in `681e45c6`/`1485bb60`/`5833f1a6` — only the prose sections that were directly about the 12th site were touched, not these two.
- **verification:** `grep -n "11 sites\|of the 11\|8 of 11\|all 11 ship" docs/diagnoses/2026-09-24-training-day-predicate-logged-disagreement-b7e3a1.md` → 3 hits in `forbidden_patterns_checked`/`regression_test_planned`; `grep -n "All 12 whitelist-shaped" docs/sot_registry.yaml` → 1 hit confirming the registry's own count is already correct at 12. Cross-referenced against the live wiring-test `sites` list (7 files, 12 call sites total once `weekly_calendar.dart` is counted) and my own live-citation audit above (12 distinct `PlanEngineFlags.isRestDayConsideringLogged` call sites confirmed on disk).
- **suggested-fix:** Update `forbidden_patterns_checked`'s 2nd bullet ("all 12 ship...") and `regression_test_planned`'s coverage-gap sentence ("3 of the 12 sites... the other 9 of 12 sites...") to match the addendum's own corrected count. Purely a prose/count fix inside an already-committed diagnose-doc — no code or test change needed. This is exactly the class of stale-count drift CLAUDE.md §4.9 already documents ("These counts are PROSE and no gate validates them... go stale on any commit that adds a gate" — same failure mode, different artifact).
- **status:** fixed — `forbidden_patterns_checked`'s 2nd bullet and `regression_test_planned`'s coverage sentence corrected to 12/9-of-12; `dart run scripts/validate_diagnose_doc.dart` re-run, still PASS.

## Finding 2 — P3 (informational, not a defect) — asserted_fixture_value / disclosed-gap verification

- **file:line:** `test/contracts/training_day_predicate_wiring_test.dart` (source-grep only) vs. `lib/features/home/screens/home_screen.dart:617,792`, `lib/features/home/widgets/day_detail_sheet.dart:47,105`, `lib/features/home/widgets/weekly_calendar.dart:67`, `lib/features/train/providers/train_provider.dart:637,813`, `lib/core/services/workout_schedule_read_service.dart:1412`, `lib/core/services/plan_integrity_reconciler.dart:99` (partial — see below).
- **claim:** The diagnose-doc and plan-review record both explicitly disclose that only 3 of the (now) 12 call sites (`currentPhaseCompletionRate`, `PlanIntegrityReconciler.needsHeal`, `StreakWarningEligibilityNotifier.isWorkoutDayToday`) have a real call-through test that exercises flag-ON behavior through production code; the other 9 rely on source-grep (presence of the wrapper call) + `flutter analyze` + pre-existing tests that were never written to distinguish flag ON from OFF. I independently confirmed this claim is accurate, not overstated: `grep -rn "logged\|enable_logged_counts_as_phase_training_day\|isRestDayConsideringLogged" test/contracts/today_card_vs_calendar_strip_same_source_test.dart test/contracts/terminal_row_display_read_path_test.dart` (the two existing tests whose names suggest they might cover `weekly_calendar.dart`/`home_screen.dart`) returned nothing, and no other test file references `weekly_calendar.dart`, `home_screen.dart`'s rest-day paths, or `day_detail_sheet.dart` with a `'logged'` fixture.
- **verification:** `grep -rln "weekly_calendar\|WeeklyCalendar" test/` → only the source-grep wiring test and unrelated fixture/other files; none exercise the flag-ON path. Also confirmed the reconciler's flag-ON test uses a synthetic `status: 'planned'` fixture that the diagnose-doc itself (correctly) flags as not representative of the two real writers (`workout_write_service.dart:510`, `sync/sync_workout.dart:985`), which always stamp `status: 'completed'` — so even the one reconciler test that exists is exercising a boundary case, not the typical real-world shape.
- **suggested-fix:** No action required to merge — this is an accurately-disclosed, deliberate scope boundary under CLAUDE.md §4.12.4 (ship-dark, flag default OFF, zero live risk until flip-on, which itself requires a full new ×2 review). Flagging only so the founder has independent confirmation the disclosure is honest rather than optimistic, and so the flip-on batch's own plan explicitly budgets for closing this gap (4 UI-layer widget-pump tests) rather than re-discovering it cold.
- **status:** verified_clean — accurately-disclosed, deliberate ship-dark scope boundary; no code/test change requested; noted for the future flip-on batch's own plan.

## Lens-by-lens disposition

1. **writer_reader_drift** — VERIFIED CLEAN. The two writers of `type: 'logged'` (`workout_write_service.dart:510`, `sync/sync_workout.dart:985`) are **not touched** by this diff at all (`git diff main...HEAD -- lib/core/services/workout_write_service.dart lib/core/services/sync/sync_workout.dart` → empty). Only readers changed. No new Hive write, no new cloud write, no field-name change — the batch is a pure read-path predicate unification. Checked every reader's `fields_read:` in the new SoT entry (`type` only, at every site) against the actual field read in each file — all read `schedule?['type']`/`row['type']`/`dayMap['type']`/`e['type']`, matching the writers' stamped field exactly.

2. **function_exception_swallow** — N/A, confirmed not assumed. `grep -rn "\.functions\.invoke(" ` against the diff's changed files → 0 hits. No Edge Function call anywhere in this branch.

3. **blast_radius_mismatch** — VERIFIED. Live classifier run (`git diff --name-only main...HEAD | dart run scripts/blast_radius_from_diff.dart -`) → `platform`, matching the task's stated tier. `docs/blast_radius.yaml`'s `platform:` tier `requires: [regression_test, behavioral_test_path, code_review_b_pass, feature_flag]`. Checked each: `regression_test` ✓ (`test/contracts/training_day_predicate_wiring_test.dart` + 3 extended files); `behavioral_test_path` ✓ (Gate 42 PASS, cites `test/contracts/training_day_predicate_wiring_test.dart`); `code_review_b_pass` — **this review**, currently `pending` in the plan-review record (see process note below); `feature_flag` ✓ — independently confirmed via the mutation test above (not just read) that flag OFF is byte-identical at every site and flag ON is a real, live-tested behavior change gated on `configBox['enable_logged_counts_as_phase_training_day']`, default OFF (`plan_engine_flags_logged_training_day_test.dart`: "defaults to false with no Hive" — passes; and the getter's `catch (_) { return false; }` fails safe on any Hive error).

4. **secrets_in_tree** — VERIFIED CLEAN. `grep -n "secret\|api_key\|apikey\|password\|token\|SUPABASE_\|rzp_\|sk-\|AIza"` against the new diagnose-doc and the new `docs/sot_registry.yaml` hunk → 0 matches inside the new content (all matches in the full-file grep were pre-existing unrelated lines elsewhere in `sot_registry.yaml`, not part of this diff). No credential-shaped literal anywhere in the 20 files this branch touches.

5. **unawaited_no_error_sink** — N/A, confirmed not assumed. `grep -c "unawaited("` across every file in `git diff --name-only main...HEAD` → 0. No `unawaited(` call added or touched by this branch.

6. **guard_without_its_mirror** — VERIFIED, including live mutation. Every one of the 12 call sites was individually algebra-checked for polarity correctness (the wrapper returns "is REST"; 7 sites use it directly as `isRest`/rest-day boolean, 5 sites negate it for `isWorkout`/`isTrainingDay`/`isWorkoutDayToday` semantics — De Morgan confirms `!isRestDayConsideringLogged(type) == (type=='workout'||type=='custom_template')` at flag OFF, matching each site's pre-fix form exactly). Mutated the wrapper (ignore-flag) and the flag getter is `catch (_) { return false; }` (also verified: no code path can turn a Hive-read exception into "flag is ON"). Followed the boolean to every call site by hand per the earlier "byte-OFF" and "13th site" checks above — no call site collapses or misuses the boolean (e.g. no site does `isRestDayConsideringLogged(type) && isRestDayConsideringLogged(otherType)` or similar double-use that could hide a polarity bug). Checked the mirror of `isPhaseCompletionTrainingType`'s whitelist (unrecognized future type / `null` → REST, not training) against `isTrainingDayType`'s exclusion shape (unrecognized future type / `null` → training) — this is a PRE-EXISTING, explicitly-documented, deliberately-permanent disagreement (not introduced by this batch; the doc comment says so and it is unchanged from before this branch except for the `'logged'` case specifically, which is the one case this batch closes). The 12th-site discovery process itself (documented in the diagnose-doc addendum) is a real instance of exactly this lens catching something 7 prior task-scoped reviews + the wiring test's own negative-regression grep missed — I re-derived that finding independently via my own from-scratch `lib/`-wide grep rather than trusting the addendum's narrative, and it holds.

7. **missing_input** — Finding 1 above (stale "11 sites" prose). Everything else checked clean: `configBox['enable_logged_counts_as_phase_training_day']` — confirmed the flag getter reads exactly that key and the test fixtures (`cb.put('enable_logged_counts_as_phase_training_day', true)`) write exactly that key, both the boxes match (`test/contracts/plan_engine_flags_logged_training_day_test.dart`, `reconciler_needs_heal_logged_test.dart`, `streak_warning_eligibility_logged_test.dart` all use `HiveService.configBoxName`). All SoT registry `line_range:` citations independently verified against live line numbers (see summary above) — zero drift found beyond Finding 1's prose-only count.

8. **asserted_fixture_value** — Finding 2 above (disclosed-gap verification, not a defect). For the 3 tests that DO claim real behavior change: `phase_adherence_rate_test.dart`'s new case is non-vacuous (rate moves 1.0→0.5 only when the flag flips, hand-verified by reading `phaseCompletionRate`'s primitive: 1 done / 1 total at OFF vs 1 done / 2 total at ON — cannot pass if the feature does nothing) — this is explicitly the exact defect class round-2 of the plan-review caught and fixed (the FIRST draft of this test seeded two already-100%-complete days, which the arithmetic shows is structurally incapable of ever failing regardless of the flag; I independently re-derived that the CURRENT version does not have this defect). `reconciler_needs_heal_logged_test.dart` and `streak_warning_eligibility_logged_test.dart` are both non-vacuous by the same standard (explicit OFF-assert-false / ON-assert-true pairs, both independently confirmed to flip under my mutation test). `phase_completion_training_type_test.dart` and `plan_engine_flags_logged_training_day_test.dart` are pure-function tests with real distinguishing assertions (not tautological). `training_day_predicate_wiring_test.dart` is explicitly source-grep (labeled as such in its own comments) and I verified it exercises BOTH ternary phrasings (exclusion AND inclusion) in its negative-regression check, closing the exact gap round-2 of the plan-review found in the first draft (which checked only the exclusion phrasing).

## Founder triage notes

- Both findings triaged same session as the review: Finding 1 (stale "11 sites" prose) fixed directly in the diagnose-doc, validator re-run and still passing. Finding 2 confirmed accurate and left as documentation (no code change needed).
- **Process blocker, not a code defect:** `docs/plan-reviews/oi126-training-day-predicate-unification.md` currently has `bpass: pending`. `scripts/check_plan_review_record_exists.dart` requires `bpass: accepted` plus a `bpass_review:` field naming a committed file under `docs/reviews/` containing a line-anchored `verdict: accepted`, at ≥platform tier, at the merge commit in CI. Before the `--no-ff` merge: update that record to `bpass: accepted` and `bpass_review: docs/reviews/oi126-training-day-predicate-bpass.md`, and this file's own `verdict:` frontmatter (currently `accepted` per my assessment above, pending your sign-off) must be present and committed alongside.
- Also note: `scripts/check_skill_tuning_history.dart` will block a commit that adds this file unless `.claude/skills/code-review/SKILL.md`'s Tuning history gets a same-dated entry in the same commit.
