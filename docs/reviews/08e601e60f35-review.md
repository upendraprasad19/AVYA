---
reviewed_at: 2026-09-10T12:58:19+05:30
staged_against: 08e601e60f35
blast_radius: platform
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, stale_or_wrong_citation, guard_without_its_mirror, missing_input, asserted_fixture_value, modelled_on_is_a_checkable_claim, self_attesting_artifact]
findings_count: 7
verdict: accepted
---

# Code Review — 08e601e60f35

OI-172: a terminal push-result record for `scripts/safe_push.sh`, plus the §4.9
verification-width row. 12 staged files, blast-radius **platform**.

**0 P0, 1 P1, 4 P2, 2 P3. 0 false alarms. No functional defect found in the
shipped logic** — the reviewer ran the suite, re-derived the 13-key writer/reader
contract by hand, live-ran the script against a throwaway bare remote and read
the actual emitted record, and planted a file to confirm the record really is
invisible to `git status --ignored`. Every finding is in the surrounding
artifacts, which is where this batch was weakest.

⚠ **A note on the reviewer's own frontmatter note.** It flagged that
`git cat-file -t 08e601e60f35` fails and inferred the hash might be wrong. The
hash is correct: `git hash-object --stdin` WITHOUT `-w` computes an id without
writing the object, so it is unresolvable by design, and
`check_code_review_pass_exists.dart` never resolves it either. This skill's own
tuning history records the identical subagent misdiagnosis (2026-08-30), where
that reviewer silently substituted a commit sha into its report. This one flagged
the discrepancy instead of substituting, which is the behaviour to keep.

## Finding 1 — P2 — stale_or_wrong_citation
- **file:line:** `docs/diagnoses/2026-09-10-safe-push-outcome-not-recorded-a7f3c1.md` `writers:`
- **claim:** cited `:78` for the unresolvable-ref exit; the real line is `:89`, because THIS commit inserted a 12-line comment block above it.
- **verification:** `grep -n "could not resolve local ref" scripts/safe_push.sh` → `89`.
- **status:** **accepted, fixed.** Re-derived to `:89` after the last edit, with the reason recorded inline. The plan's §5 table had the same defect for the four terminal exits (`:167/:176/:189/:201` → `:319/:329/:343/:356`); fixed by labelling the pre-fix numbers as such AND giving the post-diff ones.
- **note:** the plan's §2 table legitimately cites the pre-fix numbers and self-labels "(201 lines)". The reviewer correctly distinguished the two uses rather than flagging both.

## Finding 2 — P2 — self_attesting_artifact / stale_or_wrong_citation
- **file:line:** `CLAUDE.md`, the plan §7, `docs/audit/open_issues.md`, the diagnose-doc — four places
- **claim:** all said "10 new" cases in `safe_push_test.dart`; the real figure was 11 at review time.
- **verification:** `grep -c "^  test(" test/scripts/safe_push_test.dart` → 17 total, 6 pre-existing → 11 new. Suite total 21+12+17 = **50**, not the claimed 31 (which omitted the contract test file entirely).
- **status:** **accepted, fixed** — and the number moved again: adding the kill-switch test for F4 makes it **12 new / 18 in file / 51 across three files**. All four citations re-derived from `grep -c` AFTER the final edit.
- **why it matters more than the number:** this shipped in the same commit that adds a §4.9 row about verification-width, and rule 21 requires the diagnose-doc to state how many tests reddened. A count is a claim.

## Finding 3 — P1 — self_attesting_artifact
- **file:line:** `docs/audit/open_issues.md` OI-172 resolution block
- **claim:** cited `docs/plan-reviews/oi172-push-result-file.md`, which existed nowhere.
- **verification:** `git cat-file -e ":docs/plan-reviews/oi172-push-result-file.md"` → does not exist.
- **consequence:** `check_plan_review_record_exists.dart` reads exactly that branch-keyed path at the merge commit in CI. Merging as-was would have failed the keystone gate — the repair for which is a `git reset --hard` unwind.
- **status:** **accepted, fixed.** The record now exists at that path, authored from the three rounds documented in the plan doc §10–§12. ⚠ The citation was written before the file — precisely the self-attesting-artifact error this lens exists for, committed while the author was consciously waiting to write it.

## Finding 4 — P3 — blast_radius_mismatch
- **file:line:** `docs/blast_radius.yaml:25` — `platform: requires: [..., feature_flag]`
- **claim:** `feature_flag` was unmet and undiscussed. Nothing enforces the list: `check_blast_radius_coverage.dart` never reads `requires:`.
- **status:** **accepted, FIXED rather than exempted.** Added a kill switch: `$(git rev-parse --absolute-git-dir)/.safe_push_result.disabled`. Arguing the exemption (§4.6 scopes feature flags to payment/sync/auth/AI/plan-generator, and every write here is `|| true`-guarded) would have cost more words than the five lines of code.
- **⚠ the precedent was followed in SHAPE and deliberately not in LOCATION:** the two existing kill switches live in `.claude/`, and **neither is listed in `retire_worktree_lib.dart`'s `regenerableIgnoredPaths`** — so a worktree where someone flipped one is unretirable until they remember to delete it. Copying the location would have imported the exact hazard this record's own placement exists to dodge. Verified live both ways (switch present → no record, push exit 0; removed → record returns) and covered by a test with both legs.

## Finding 5 — P3 — self_attesting_artifact
- **file:line:** the diagnose-doc's `forbidden_patterns_checked`
- **claim:** enumerated five fields; the real record carries thirteen. `worktree` is an absolute path and discloses a username.
- **verification:** the reviewer ran the real script against a throwaway bare remote and read the emitted file — the method the lens asks for, rather than reasoning from the writer.
- **status:** **accepted, fixed.** All thirteen enumerated, and the `worktree` disclosure stated explicitly along with why the field stays.

## Finding 6 — P3 — secrets_in_tree (residual, not reachable here)
- **file:line:** `scripts/safe_push.sh` `_sanitize_reason`
- **claim:** the sanitizer collapses and truncates but does not REDACT, so a credential in an `https://user:pass@host/` remote's stderr could be persisted.
- **verification:** `git remote -v` → `git@github.com:...` (SSH, which cannot embed a password). Every current call site passes a script-authored single-line string, never raw stderr — which is also why making it a passthrough reddens zero.
- **status:** **accepted, documented at the function.** Not reachable in this repo or from any current caller; recorded for whoever reuses the pattern against an HTTPS remote.

## Finding 7 — P2 — guard_without_its_mirror / mutation-count accuracy
- **file:line:** the diagnose-doc's `impact_analysis` mutation paragraph
- **claim:** of six legs re-run by the reviewer against the full suite, three reddened exactly one MORE test than claimed. The guards are all real; the counts were undercounted.
- **verification:** the reviewer re-ran both predicted-GREEN legs (both confirmed 0/50) and four RED legs, reporting measured counts against claimed.
- **root cause, accepted:** the first pass measured each leg against ONE test file and wrote those numbers down. **The same input-set-width error as the §4.9 row this batch adds** — and the error direction is the instructive part: a narrower run can only UNDERCOUNT, so it reads as a slightly weaker proof rather than as alarming, which is the shape nobody re-checks.
- **status:** **accepted, fixed.** All 15 legs re-run against the full 51-assertion suite; the diagnose-doc now carries those numbers and says plainly that they are the second measurement and why the first was wrong.
- **one deliberate divergence from the reviewer's numbers:** its "collapse UNVERIFIED into FAILED → RED 2" mutated the script's EXIT CODE, which also reddens the pre-existing exit-2 test. The leg recorded here mutates only the RECORD's result value (RED 1), which isolates the record from the script's own verdict. Both are valid; they test different things and the numbers are not interchangeable.

## Founder triage notes

All 7 accepted, 0 false alarms, all fixed in-batch. Two required real changes
rather than prose: the kill switch (F4) and the plan-review record (F3, which
would have failed CI). The remaining five were false or incomplete claims in this
batch's own documentation — which is exactly what the 2026-08-25 entry in this
skill's tuning history predicted would be left once the ×3 plan review had taken
the design defects.
