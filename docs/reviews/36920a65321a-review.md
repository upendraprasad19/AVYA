---
reviewed_at: 2026-07-30T17:50:00+05:30
staged_against: 36920a65321a
blast_radius: catastrophic
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 3
verdict: accepted
---

# Code Review — 36920a65321a

## Hash lineage note (4th shift — why this file exists alongside the three prior review files)

Hash history: `8d5a2f558995` (Final B-pass, original) → `19eeb84131e2` (Finding
1's own fix + regression test applied) → `0d23d5d5811c` (Hermes report
verdict-flip + closing section) → `d7662eb41486` (real `backups/
applied_migrations.json` ledger entry added after the live migration apply) →
**`36920a65321a` (this file)**.

This fourth shift is also mechanical, not substantive: the first real commit
attempt at hash `d7662eb41486` FAILED at `scripts/check_no_deferral_
euphemism.dart` (Gate-DEU, wired into `pre-commit.sh`'s `for GATE in
scripts/check_*.dart` loop). The flagged line, in
`docs/audit/2026-07-30-hermes-cross-device-progress-lock.md`, was a sentence
EXPLICITLY DISCLAIMING deferral (all 13 Hermes findings were fixed inline, in
this same batch, as that very diagnose-doc's own "Closing status update"
section documents) — but it happened to contain one of the exact banned
two-word entries listed in `docs/deferral_euphemisms.yaml` (a "spawn a
[batch-deferral phrase]" turn of phrase), which Gate-DEU matches on
staged-added lines regardless of surrounding negation
(`scripts/check_no_deferral_euphemism.dart` is a plain case-insensitive
substring match against `git diff --cached --unified=0` additions — read
directly, confirmed no negation-awareness and no inline-suppression
mechanism exists, by design, per the script's own header comment: the phrase
list must "stay cheap to extend" and the gate "fails closed" rather than
offering an escape hatch that could quietly widen). Note for anyone reading
this file itself: this paragraph deliberately does NOT quote the exact
banned wording verbatim, having tripped this same gate on ITSELF one attempt
earlier by doing exactly that inside this same lineage note — the working
copy of the file at that point is what `git log` / the prior failed attempt
would show, not reproduced here again. Rephrased the flagged sentence in the
Hermes report to preserve the exact same meaning (still an explicit
disclaimer of deferral) while avoiding every entry in
`docs/deferral_euphemisms.yaml`. Re-ran `dart run scripts/check_no_deferral_
euphemism.dart` directly post-fix: `PASS`. No other code, test, or migration
file changed since `d7662eb41486`; `git diff --cached --name-only` shows the
identical 26-file set. Everything below is identical in substance to
`d7662eb41486-review.md` / `0d23d5d5811c-review.md` / `19eeb84131e2-review.md`.

Live post-apply verification performed before the ledger entry was written
(carried forward from the prior generation, still current): `has_function_
privilege` queried directly against `dedsavbjuwgarrhphgnl` for `anon` /
`authenticated` / `service_role` on both RPCs — `anon_can_exec=false`,
`authenticated_can_exec=true` for both `update_user_progress_snapshot` and
`update_streak_progress` (the latter's grants preserved across the
`CREATE OR REPLACE`, as the migration's own comment claimed). `pg_proc`
catalog query confirmed both functions exist with the exact expected
argument lists.

## Finding 1 — P3 — monotonic_field_guard (task-brief item 4, extends writer_reader_drift)
- **file:line:** `supabase/migrations/115_user_progress_snapshot_optimistic_lock.sql:194` (UPDATE branch) and `:134` (INSERT branch, N/A — fresh row)
- **claim:** `longest_gap_days` is updated with a plain `COALESCE(p_longest_gap_days, longest_gap_days)`, unlike its two sibling "record" columns in the SAME UPDATE statement — `total_workouts_done` (line 177) and `deployments_complete` (line 191) — which both explicitly use `GREATEST(COALESCE(p_x, x), x)` with an inline comment citing `docs/sot_registry.yaml`, `feedback_monotonic_field_recompute_demotion.md`, and diagnose `3a7b9f`. `longest_gap_days` is semantically the same shape of field: `supabase/functions/_shared/tools/progress/getPromotionStatus.ts:182-204` computes it as a running max (`if (gap > longestGapDays) longestGapDays = gap`) — a lifetime high-water-mark, the exact category `lib/core/services/CLAUDE.md`'s pitfall table names explicitly ("Lifetime monotonic fields (rank, lifetime workout count, **longest streak**, peak weight, deployments_complete, badge unlock state) MUST have an only-increment writer guard"). Both `rank_service.dart:448` (client rank gate) and `supabase/functions/evaluate-rank-promotions/index.ts:151` (server rank gate) read the STORED column directly for `maxGapDays` disqualification logic (`rank_service.dart:507`), so a silent regression to a lower value would cause a false-positive rank qualification, not just cosmetic drift.
- **verification:** `grep -n "longest_gap_days\|GREATEST" supabase/migrations/115_user_progress_snapshot_optimistic_lock.sql` — confirms `total_workouts_done`/`deployments_complete` get `GREATEST(...)`, `longest_gap_days` gets bare `COALESCE(...)`. Reachability check: `grep -rn "'longest_gap_days'\]" lib/` finds only 2 READ sites building RPC params (`sync_profile.dart:115,320`) and zero Hive WRITE sites. So `p_longest_gap_days` is always NULL on every real call today, making `COALESCE(NULL, existing)` a no-op — this is currently **dormant, not live-exploitable**, which is why I scored it P3 rather than matching the P1/Hermes-C3 severity its siblings got.
- **suggested-fix:** For consistency with the migration's own stated rationale, change line 194 to `GREATEST(COALESCE(p_longest_gap_days, longest_gap_days), longest_gap_days)`, matching the pattern immediately above it.
- **status:** accepted — fixed and live: `supabase/migrations/115_user_progress_snapshot_optimistic_lock.sql`'s UPDATE branch uses the `GREATEST` wrap (mirrored in `test/sql/cross_device_progress_optimistic_lock_verify.sql`'s embedded copy, Case 21, 21/21 `ok` pre-apply); the DEPLOYED function's identical text independently confirmed via the `pg_proc` catalog query cited above, post-apply.

## Finding 2 — N/A (false_alarm) — writer_reader_drift
- **file:line:** `lib/core/services/sync/sync_restore_completeness.dart:159,224,238,376,389` vs `supabase/functions/restore-user-snapshot/index.ts:274`
- **claim (investigated, not confirmed):** The Hive key used for the freeze-used-dates list is `streak_freeze_used_dates` (singular) throughout the new retry/merge code, while the cloud column is `streak_freezes_used_dates` (plural) — looked like writer/reader field-name drift.
- **verification:** `grep -rn "'streak_freeze_used_dates'\|'streak_freezes_used_dates'" lib/` — singular is the ESTABLISHED Hive-local key (pre-existing writer/reader, untouched by this diff); plural is used ONLY for cloud references. The new code follows the pre-existing convention consistently. No drift introduced.
- **status:** false_alarm

## Finding 3 — N/A (false_alarm) — blast_radius_mismatch
- **file:line:** `supabase/functions/restore-user-snapshot/index.ts:274` (freezes projection, 4-col → 5-col) vs `lib/core/services/sync/sync_restore_completeness.dart:342`
- **claim (investigated, not confirmed):** EF not yet redeployed with this change — is the client robust to the OLD 4-col response in the meantime?
- **verification:** `sync_restore_completeness.dart`'s handling does `(res['streak_progress_version'] as num?)?.toInt()` — absent key returns `null`, not an exception, degrading to `cloudVersion == null` → version stamp left untouched → next sync hits a legitimate version-mismatch against the NOW-LIVE cloud row and self-heals via the existing bounded-retry path (re-fetches version directly from `.from('user_progress')`, not the EF). Pinned by `test/contracts/restore_user_snapshot_freezes_projection_parity_test.dart`.
- **status:** false_alarm — tracked as an explicit, tested, self-healing residual requiring its own redeploy (separate authorization, separate from this merge and from the migration apply this commit records).

## Lens: writer_reader_drift — otherwise clean
Every Hive write introduced/touched traced to its cloud reader and vice versa: `_stampProgressVersion`, `_buildUserProgressRpcParams`'s 11-field map against migration 115's now-LIVE RPC signature (confirmed via the `pg_proc` catalog query, not just source), `pushOnboardingProgressSnapshot`'s manual params, `syncFreezes`'/`_retrySyncFreezesOnceAfterConflict`'s freeze fields against `update_streak_progress`'s (also live) signature. All matched. `mergeRpcParamsPreferringNonNull`'s fallback-keys-only iteration pinned by `test/sync/merge_rpc_params_preferring_non_null_test.dart` (6 cases) — not a finding.

## Lens: function_exception_swallow — clean
Every `.rpc(` call added (5) plus the 2 new retry-refetch calls verified inside the pre-existing outer try/catch (`ErrorTelemetry.recordNonFatal` + `_reportSyncFailure`), or — for `pushOnboardingProgressSnapshot` — an intentional documented throw preserving the pre-existing caller contract. No swallowed exception. RPCs are now LIVE, so this is no longer a purely static claim.

## Lens: blast_radius_mismatch — clean
Inline rollback present (DROP FUNCTION + verbatim `CREATE OR REPLACE` reverting `update_streak_progress` to migration 096's body). 21/21 live-Postgres rollback-transaction cases pre-apply; migration now APPLIED live against `dedsavbjuwgarrhphgnl` with ACL independently re-verified via `has_function_privilege` post-apply. `backups/applied_migrations.json` correctly records the real apply — both gate scripts enforcing CLAUDE.md §4.5's same-commit pairing (`applied_migrations_parity_test.dart`, `check_migration_ledger_paired.dart`) read directly to confirm the exact requirement before satisfying it. `docs/diagnoses/2026-07-30-cross-device-progress-optimistic-lock-e6b9c4.md` carries a fully populated `touched_layers_checked` across all 12 tiers.

## Lens: secrets_in_tree — clean
`git diff --cached | grep -iE "sk-[a-z0-9]|rzp_live_|AKIA[0-9A-Z]{10,}|-----BEGIN"` → no matches, including the ledger entry (bare version string, ISO timestamp, sha256 hex digest, static applier string) and the euphemism-fix rewording. No credential-shaped values anywhere.

## Lens: unawaited_no_error_sink — clean
All 4 `unawaited(` call sites added are `unawaited(ErrorTelemetry.logEvent(...))`, whose body wraps itself in its own `try/catch` (declared internal error sink). `dart run scripts/check_unawaited_has_error_sink.dart` does not flag either new site.

## Founder triage notes
<leave blank, a human fills this in>
