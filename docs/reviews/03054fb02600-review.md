---
reviewed_at: 2026-07-19
staged_against: 03054fb02600
blast_radius: platform
reviewer: claude-sonnet-via-skill
lens_set: [flag_off_inertness, safety, avoidnames_soft, composition_4quadrant, parser_extraction, threading, tracer_mirror, behavioral_test_rigor, secrets]
findings_count: 1
verdict: accepted
---

# Code Review — 03054fb02600 (Batch 11-B W3.4 cross-phase variety)

Fresh context-blind Sonnet B-pass over the staged diff (849 lines, 10 files). One P1
(test-coverage gap) — **fixed in-batch**. All other lenses clean, with the reviewer
independently running 55 regression tests + the frozen-baseline gate.

## Finding 1 — P1 — test-coverage gap (the SERVICE reader was untested) — **FIXED**
- **file:line:** `lib/core/services/workout_schedule_read_service.dart` `previousPhaseNamesByDay`
  (+ the two advance-caller wirings) vs `test/contracts/cross_phase_variety_behavioral_test.dart`.
- **claim:** The behavioral test drove `generateV4(previousPhaseByDay: <hand-built map>)` directly,
  proving the ENGINE re-rank but never calling `previousPhaseNamesByDay()` — so the flag gate, the
  `getWeek(1)=A`/`getWeek(2)=B` mapping, the `.toLowerCase()` pass, and the day-index union had ZERO
  runtime coverage; a flag inversion / getWeek swap / missing lowercase would pass every test.
- **resolution (fixed):** Extracted the reader's pure core to `@visibleForTesting static
  previousPhaseNamesFrom({week1, week2})` (mirroring the `repeatPinsFrom` pure/instance split);
  `previousPhaseNamesByDay()` = flag-gate + `getWeek(1)/getWeek(2)` → the pure helper. Added a
  `previousPhaseNamesFrom — service reader core` test group: per-day A(week1)/B(week2) LOWERCASED +
  day-index union + non-workout-row skip + the ship-dark `flag OFF → {}` gate. 8/8 green.
- **status:** fixed

## Lenses clean (verified against code + live test runs)
- **Flag-OFF inertness** — `previousPhaseNamesByDay()` returns `const {}` before any getWeek when the
  flag is off; both advance callers gate on `pins == null`; `avoidNames = const {}` default →
  `_preferNovel(pool, {})` → `pool.first` → matches the 3 pre-11-B early-returns. Scorecard gate
  (606-persona, flag-off) unmoved (mean 86.7).
- **Safety** — `_preferNovel` returns a member of `pool ⊆ candidates` (already injury-filtered) →
  never a contraindicated exercise; `pool` provably non-empty at the call site (no throw/null).
- **avoidNames SOFT** — never merged into `pickedNames`/`excludeNames`/`queryV4`; its own param
  straight to `_selectCandidate`; attempt-5 correctly variety-ineligible.
- **Composition (4 quadrants)** — OFF/OFF → candidates.first; injurysub-ON/variety-OFF → 11-C sorted
  subs.first (unchanged, sort intact); injurysub-OFF/variety-ON → `_preferNovel` walks candidates;
  ON/ON → within the sorted curated subs (variety never escapes the safe pool).
- **Parser extraction** — `_exerciseNamesOfRow`/`_namesByDayIndex` are a mechanical extraction;
  `repeatPinsFrom` unchanged (repeat_phase_pinned_selection 8/8 green); `previousPhaseNamesByDay`
  adds its own lowercase on top (repeatPinsFrom's consumer keeps original case).
- **Threading** — generate→generateV4 forwards; only the pickV4 branch gets it (buildPinnedDays
  unchanged); indexed loop → avoidA/avoidB → _fillSlots → _cascadeFill; both advance callers wired.
- **Tracer mirror** — `_selectCandidateName`/`_preferNovelName` 1:1 with production; trace gains
  avoidNames; 4 pick sites pass it; QueryV4Mirror unchanged.
- **Behavioral test rigor** — variety changes the SET (deep pool); null=deterministic (inertness);
  bounded non-empty; injury-safe. Reviewer ran 55 regression tests across the touched files → green.
- **secrets/unawaited/function-exception** — n/a (synchronous pure-Dart plan-engine).

## Founder triage
Self-triaged under auto-mode: the P1 (reader-core test gap) fixed in-batch. Verdict accepted.
