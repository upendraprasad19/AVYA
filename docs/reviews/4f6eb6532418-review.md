---
reviewed_at: 2026-09-10T20:55:00+05:30
staged_against: 4f6eb6532418
blast_radius: platform
reviewer: claude-sonnet-via-skill
lens_set: [rebase_semantic_drift, guard_without_its_mirror, unawaited_no_error_sink, asserted_fixture_value, stale_or_wrong_citation]
findings_count: 6
verdict: accepted
---

# Code Review — 4f6eb6532418

Cherry-pick of the realtime PRO gate (e4a7c9, authored 2026-08-16) onto a `main`
25 days newer. **0 P0, 2 P1, 3 P2, 1 P3. 0 false alarms.**

**The fix's LOGIC survived the rebase intact** — the reviewer ran the suite (9/9)
and performed its OWN mutation (`proStateSnapshot()` -> `isPro()` -> 8 pass / 1
red) rather than trusting the diff's claim. What the rebase broke is the
documentation layer, and one break was hard-blocking.

## Finding 1 — P1 — stale_or_wrong_citation
- **file:line:** `docs/sot_registry.yaml`, `checkAndSync` citation
- **claim:** cited `sync_service.dart:808`, correct in August, now a bare `}` 167 lines from the real call site (974-977).
- **verification:** `sed -n '808p' lib/core/services/sync_service.dart`
- **status:** **accepted, fixed** -> `871-1004`, the DECLARATION span. ⚠ My first fix used `974-977`, the precise call site, and the gate REJECTED it: it requires the method NAME inside the range, and the call site does not contain it. Precision and gate-satisfiability are different targets.

## Finding 2 — P1 — stale_or_wrong_citation (blocking)
- **file:line:** `docs/sot_registry.yaml` — `compileDailySnapshot` (963-987) and `restoreFailureReason` (2203-2205), two concepts this diff never touches
- **claim:** `check_sot_registry_parity.dart` FAILS on the staged tree, so the cherry-pick could not be committed.
- **verification:** `dart run scripts/check_sot_registry_parity.dart` -> EXIT=1, 2 stale-line-range errors
- **status:** **accepted, fixed** -> `1007-1052` and `2235-2237`.
- ⚠ **The reviewer's CAUSE was wrong and I checked rather than accepting it.** It attributed the drift to four intervening commits on main. **Clean main PASSES (EXIT=0)** — verified by running the gate in the primary worktree. The shift is caused by THIS diff's own insertions into `sync_service.dart`, which move every method below them. That is CLAUDE.md §4.9's row: adding code breaks source-grep contracts for methods in files you never conceptually touched. Right finding, wrong mechanism; the fix is identical either way but the lesson is not.
- ⚠ **I had run this same gate earlier and reported PASS.** That run happened mid-cherry-pick, after 1 of 3 commits. A gate run against a partial tree, reported as validation of the whole.

## Finding 3 — P2 — stale_or_wrong_citation (gate blind spot)
- **file:line:** `scripts/check_sot_registry_parity.dart:141`
- **claim:** the block regex matches `(\d+)-(\d+)` only, so single-number `line_range:` entries are never checked.
- **verification:** counted in `docs/sot_registry.yaml` — **30** single-number vs **640** dash-form.
- **status:** **accepted, filed as OI-180.** Not fixed here: it needs its own gate test and a rule-24 ledger entry, and `scripts/**` is platform-tier. Finding 1 is its proof — that citation rotted unseen for 25 days *because* it was single-number, and became a hard failure the moment its punctuation changed.

## Finding 4 — P2 — stale_or_wrong_citation
- **file:line:** `docs/sot_registry.yaml`, `_downgradeLocally` `notes:` prose
- **claim:** "8 call sites funnel here (439, 461, 579, 599, 890, 900, 908, 1066)" is wrong on all eight.
- **verification:** `grep -n "_downgradeLocally()" lib/core/services/subscription_service.dart` -> 461, 483, 601, 621, 913, 923, 931, 1089
- **status:** **accepted, fixed by DELETING the numbers.** The reviewer established via `git show b6b0c89e:` that the list was wrong **the day it was authored** — not rebase drift. A hand-maintained line list inside a free-text `notes:` field is invisible to the parity gate, which only reads the `line_range:` key, so nothing could ever have caught it. The count (8) is kept as the durable claim with the command to re-derive positions.

## Finding 5 — P2 — unawaited_no_error_sink
- **file:line:** `lib/core/services/sync/sync_realtime.dart:47-48`
- **claim:** `realtime_subscribe_skipped_free_tier` is absent from `highPriorityOpTypes`, so the shared (not per-op_type) client cooldown can drop it with zero network call.
- **verification:** `grep -c "realtime_subscribe_skipped_free_tier" lib/core/services/error_telemetry.dart` -> 0
- **status:** **accepted, fixed** — promoted to `highPriorityOpTypes`. This event is the fix's ONLY evidence that the gate turns free users away in prod, and it fires across the whole free population right after deploy — exactly when a shared cooldown is most likely armed. It would have gone dark precisely when it mattered. Same reasoning that already promoted four other event families (diagnose `c4f8d2`).
- **note:** the August B-pass checked this lens and passed it, having asked "can this throw?" (no — the telemetry body is try/caught). It never asked "does it arrive?". Same lens, narrower question.

## Finding 6 — P3 — stale_or_wrong_citation
- **file:line:** `_onUserChanged` cited `158-195`; `_downgradeLocally` cited `1174`
- **verification:** real span `166-216`; definition at `1175`
- **status:** **accepted, fixed.**
- ⚠ **Why the gate was green on a wrong range**: line 158 is `SingletonLifecycleRegistry.register('SyncService', _onUserChanged);` — a REFERENCE. The gate asks only whether the name appears in the range, so a mention satisfied it. A second false-negative shape, folded into OI-180.

## Clean lenses

`guard_without_its_mirror` — all four mirrors traced (PRO->free downgrade, free->PRO upgrade, signed-out, account swap), plus an independent mutation the diff did not contain. `asserted_fixture_value` — every literal re-derived from the mechanism. `rebase_semantic_drift` (runtime half) — no new callers, `_onUserChanged` intact, `proStateSnapshot()` pure and unchanged, `app.dart` hook symmetry verified.

## Founder triage notes

All 6 accepted, 0 false alarms. Five fixed in-batch; F3 filed as OI-180 because it
is a platform-tier gate change needing its own test. **No functional defect was
found in the shipped code** — the value was entirely in the documentation layer
and in one blocking gate failure that would have stopped the commit.
