---
reviewed_at: 2026-06-01T09:50:00+05:30
staged_against: 5b4cc4433166
blast_radius: platform
reviewer: claude-sonnet-fresh-subagent (B-pass)
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 2
verdict: accepted
---

# Code Review (B-pass) — coach backoff-retry + nutrition RenderFlex (5b4cc4433166)

Staged diff: `supabase/functions/_shared/gemini.ts` (PLATFORM — every user's AI coach backoff-retry),
`gemini_backoff_retry_test.ts`, `lib/features/nutrition/screens/nutrition_screen.dart` (`_macroRow`
RenderFlex), `derive_only_tool_surface_test.dart` (rest-day guard), `nutrition_summary_macro_row_no_overflow_test.dart`.

Fresh context-blind Sonnet subagent. **Verdict: accept — no P0/P1 regressions.**

## Validated clean (with verification)
- **Retry bound:** `for (pass=0; pass<2)`; on `pass=1`, `canRetry` = `1 < 1` = false → break. Max 2 passes × attempts. No infinite loop.
- **Idempotency / no double-execution:** `_callOnceWithTools` never mutates `messages`; `runToolLoop` only appends after a SUCCESSFUL return. A retry fires only on a FAILED Gemini call → nothing was queued → safe to re-submit the same `messages`.
- **Retriable classification:** AbortError(timeout)=`false`; non-429 4xx=`false` (`status===429||status>=500`); all 6 `ok:false` sites assign `retriable` (TS union enforces it).
- **Happy path:** first-call success → 1 fetch, `usedFallback:false`.
- **5 lenses:** writer_reader_drift (GeminiToolsResult shape unchanged; `retriable` is internal) clean; function_exception_swallow (final `throw` re-raises; `runToolLoop` catch logs+apology) clean; blast_radius_mismatch (additive on success path, bounded on failure, one-line-constant rollback) clean; secrets_in_tree (test key `"test-key-not-a-real-secret"` not an `AIza…` pattern) clean; unawaited_no_error_sink (`_sleepMs` awaited; no `unawaited(`) clean.

## Findings

### Finding 1 — P1 — blast_radius / latency — gemini.ts (outer loop)
- **claim:** On a primary-model timeout (AbortError → `retriable:false`), the inner loop still calls Flash-Lite (its own `timeoutMs`), so a timeout costs `2 × timeoutMs` on pass 0.
- **verification:** trace `_callOnceWithTools` AbortError → outer `for (const attemptModel of attempts)`.
- **resolution:** `false_alarm` for THIS diff — the worst-case is **unchanged from pre-diff** (the old single-pass code also iterated Flash→Lite). Not introduced here; no action. (Noted: revisit if `timeoutMs` is ever raised.)
- **status:** false_alarm

### Finding 2 — P2 — test robustness — gemini_backoff_retry_test.ts:`baseOpts`
- **claim:** `baseOpts` relied on the `fallbackToLite` default; the call-count assertions (2/pass, 4/2-passes) silently break if the default ever flips.
- **verification:** read `baseOpts` — `fallbackToLite` was absent.
- **resolution:** `accepted` — fixed in-batch: `fallbackToLite: true` set explicitly in `baseOpts` + comment citing this review.
- **status:** fixed

## Founder triage notes
Both findings resolved (1 false_alarm/pre-existing, 1 fixed). Platform change additive + bounded; rollback = `node .claude/deploy_via_api.js --rollback ai-proxy previous`. Verdict accepted.

## Addendum — 2 files changed AFTER this staging hash (full-suite gate, 2026-06-01)

The end-of-batch full `flutter test` run (`+2495 ~2 -0`) surfaced 2 brittle tests; both were fixed
post-review. Neither touches the reviewed platform-tier `gemini.ts` path, and both are
self-contained feature/test-tier changes — re-running a fresh B-pass on them is disproportionate, so
they are recorded here instead. Verdict **unchanged (accepted)**.

- **`lib/features/train/widgets/week_selector.dart:660` (feature tier) — passes the `writer_reader_drift`
  lens.** Swapped a top-level `log['duration_seconds']` read for the canonical
  `WorkoutReadService.bestPerSetDuration(log)`. This is a drift *remediation* (the top-level field is never
  emitted on modern multi-set rows → was silently 0), the opposite of a drift risk. No cloud/Hive writer
  touched; pure display read. `flutter analyze` clean (dropped `?? 0` to avoid `dead_null_aware_expression`).
  Verification: `grep -n "log\['duration_seconds'\]" lib/features/train/` → 0 hits;
  `no_top_level_duration_seconds_reads_test.dart` green.
- **`test/contracts/sync_natural_key_guard_test.dart` (test-only) — no production-code change.** Comment-strip
  in `setUpAll` + widen the nutrition window to 2000, recovering from the c9f2a7 comment-block growth
  (§2.18 stale-source-grep). The runtime guard in `sync_nutrition.dart` is unchanged and still reads
  `log['date']`/`log['meal_type']`, emits `sync_skipped_null_natural_key`, and `continue`s.
  Verification: `flutter test test/contracts/sync_natural_key_guard_test.dart` → 6/6.
