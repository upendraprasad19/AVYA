---
reviewed_at: 2026-07-30T00:00:00+05:30
staged_against: 4488c520a021
blast_radius: catastrophic
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 1
verdict: accepted
---

# Code Review — 4488c520a021

## Finding 1 — P3 — blast_radius_mismatch
- **file:line:** supabase/migrations/115_user_progress_snapshot_optimistic_lock.sql:1573-1581 (the `-- Rollback (inline)` block at file-end)
- **claim:** The migration's header declares `Rollback strategy: inline`, and the rollback block gives an actual, copy-pasteable `DROP FUNCTION public.update_user_progress_snapshot(...)` statement for the new-RPC half of the change — but for the `update_streak_progress` hardening half (the ON CONFLICT guard + the `::date` cast fix), the rollback is prose only: `-- Re-apply migration 096's update_streak_progress body verbatim`. An incident responder executing a rollback under time pressure has to go find and re-read migration 096 rather than run a statement that's already in front of them. Everything else about this migration's risk posture is unusually well-covered (pre-apply live-rollback-transaction tests, explicit grant re-verification, a diagnose-doc) — this is a small gap in an otherwise thorough package, not a functional defect.
- **verification:** `git show 4488c520a021:supabase/migrations/115_user_progress_snapshot_optimistic_lock.sql | tail -15` — confirm the rollback block has no actual `CREATE OR REPLACE FUNCTION public.update_streak_progress(...)` statement, only a prose instruction to consult migration 096.
- **suggested-fix:** Either inline migration 096's `update_streak_progress` body verbatim in the commented rollback block, or narrow the header to `Rollback strategy: migration 096` for that half so the pointer is a command, not a paragraph to interpret.
- **status:** accepted — fixed. Inlined migration 096's exact `update_streak_progress` body (read directly from that file, not reconstructed from memory) as commented-out SQL in migration 115's rollback block, alongside the existing `DROP FUNCTION` statement for the new RPC. Both halves of the rollback are now copy-pasteable.

## Lens coverage notes

- **writer_reader_drift**: Traced every write and read of `streak_progress_version` across `lib/` (7 sites: `sync_service.dart` `_stampProgressVersion` writer; `sync_profile.dart` `_syncUserProgress`/`pushOnboardingProgressSnapshot`/`_retrySyncUserProgressOnceAfterConflict`/`_restoreUserProgress` readers; `sync_restore_completeness.dart` `syncFreezes`/`_retrySyncFreezesOnceAfterConflict`/`_restoreFreezes` readers) — every reader uses the identical `(x['streak_progress_version'] as num?)?.toInt()` pattern, no field-name drift. Cross-checked every `p_*` RPC-parameter name in both Dart call sites (`_syncUserProgress`'s `rpcParams`, `pushOnboardingProgressSnapshot`'s `rpcParams`, `syncFreezes`'s and both retry helpers' inline param maps) character-by-character against migration 115's two function signatures — all 5 `update_streak_progress` params and all 13 `update_user_progress_snapshot` params match by name on both sides. Verified the `restore-user-snapshot` EF's "freezes" bundle projection (4→5 col) is consumed by exactly the client code path that was also updated (`_restoreFreezes`'s `preFetched: row('freezes')`), and that the separate `user_progress` bundle (bare `select("*")`, line 152-154 of the EF) already covers `_restoreUserProgress`'s `preFetched` path for free, so no second EF projection needed updating — confirmed by reading the EF source directly, not inferring. Confirmed the Hive key `streak_freeze_used_dates` (singular "freeze") used by the new `_retrySyncFreezesOnceAfterConflict` write is the pre-existing, already-established Hive key (grepped 4 other pre-existing sites using the same singular spelling) — not a fresh typo. Verified `updated_at` stamping (diagnose a2d8f4's requirement — no DB trigger exists) survived the upsert→RPC migration: moved from client-side `DateTime.now()` to server-side `now()` inside both the INSERT and UPDATE branches of `update_user_progress_snapshot`; confirmed `update_streak_progress` never stamped `updated_at` either before or after this diff (not a regression). Independently re-verified round 2's own line-range citation fixes in `docs/sot_registry.yaml` by reading the actual current file at each cited range (`_syncUserProfile` 133-244, `_restoreUserProfile` 419-499, `_syncUserProgress` 255-346, `syncFreezes` 22-103, `_restoreFreezes` 266-359, `_restoreSavedDietPlan` 436-459) — every closing brace lands exactly on the cited end line. Ran `dart run scripts/check_sot_registry_parity.dart` (0 errors) and `dart run scripts/check_schema_column_refs.dart` (851 refs validated, 0 drift) as independent, tool-driven confirmation. No drift found.

- **function_exception_swallow**: Read the full, current (not diff-hunk) bodies of `_syncUserProgress`, `pushOnboardingProgressSnapshot`, `_retrySyncUserProgressOnceAfterConflict`, `syncFreezes`, and `_retrySyncFreezesOnceAfterConflict`. `_syncUserProgress` and `syncFreezes` each wrap their entire body (including the new RPC calls and their retry-helper calls) in the SAME pre-existing try/catch that reports via `ErrorTelemetry.recordNonFatal` — unchanged swallow contract, matches the established `sync_service_sync_user_progress`/`sync_service_sync_freezes` pattern. `pushOnboardingProgressSnapshot` has NO try/catch at all, and neither does `_retrySyncUserProgressOnceAfterConflict` — so a genuine RPC/network exception from either propagates uncaught all the way through `UserRepository.syncOnboardingToSupabase` (also no try/catch of its own) to its two real callers, both of which DO catch generically (`onboarding_provider.dart:566` `catch (syncErr)`, `sync_service.dart:1122` `catch (e, st)` in `_replayPendingOnboardingSync`) — this exactly preserves `syncOnboardingToSupabase`'s pre-existing "throws so the caller can detect sync gaps" doc-commented contract; confirmed the contract was equally true pre-diff (the old code was a bare `.upsert()` with no try/catch either), so switching the transport from a table upsert to an RPC call doesn't change caller-visible behavior. Verified the benign-vs-genuine split is real, not just claimed: a version mismatch returns `NULL` from the RPC (a normal return value, not a thrown exception) and is handled via ordinary `if` branching, not `catch` — so it can never be misclassified as a swallowed genuine error. Traced the P2 COALESCE-defaults fix to a REAL, exercised call site: `sync_service.dart`'s `_replayPendingOnboardingSync` (line 1101-1117) deliberately omits `current_week` from `progressData` with an explicit comment explaining why, which flows through `_sanitize` (drops absent keys, doesn't synthesize them) into `pushOnboardingProgressSnapshot`'s `rpcParams['p_current_week'] = null` — confirming the fresh-insert COALESCE branch is not just theoretically reachable but is the exact shape this real caller produces. No swallow gap found.

- **blast_radius_mismatch**: Confirmed catastrophic tier is not self-assigned prose — ran `git diff --cached --name-only | dart run scripts/blast_radius_from_diff.dart` (stdin mode, per the project's own footgun note about positional-args) myself: `[blast-radius content-rule] ...SECURITY DEFINER content forces catastrophic (path-tier was platform)` / `Blast-radius: catastrophic`, matching the diagnose-doc's claim. Read migrations 090, 091, and 103 (independent, unrelated prior incidents in this same repo) to cross-check the round-1 P0 grant-fix reasoning rather than trust it: migration 091's own header explains `REVOKE FROM PUBLIC` (not per-role) is the fix for PUBLIC-inherited EXECUTE; migration 103 / diagnose a9d3f1 is a SEPARATE, previously-shipped incident in this exact codebase where a Supabase platform default-ACL entry granted EXECUTE directly to `anon`/`authenticated` on new `public`-schema functions, making a PUBLIC-only revoke a no-op — this is documented as a known pitfall in `supabase/migrations/CLAUDE.md`'s own table, independent of this batch's narrative. Then went further than reading: ran two live, read-only queries against project `dedsavbjuwgarrhphgnl` myself (`pg_proc.proacl` + `has_function_privilege` for both RPC names, and `pg_default_acl` for `public`/`objtype='f'`). Results: `update_streak_progress`'s LIVE acl is `{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}` — no anon entry, `anon_exec=false` — exactly matching the diagnose-doc's claim, independently confirmed by me, not taken on trust. `update_user_progress_snapshot` does not exist live yet (migration 115 genuinely not yet applied, matching the `blocked_on_user` claim). `pg_default_acl` shows the `postgres` role (the migration-running role) DOES carry a `{postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}` default ACL for new `public` functions — a verbatim, independent match to the exact ACL string the diagnose-doc quotes as its root-cause evidence. This means migration 115's `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` is not just plausible but structurally necessary given this project's actual current default-privilege configuration, and the fix pattern used matches the one already proven in migration 103. Confirmed `CREATE OR REPLACE FUNCTION` with an identical signature preserves ACLs in Postgres (why `update_streak_progress` doesn't need its own new REVOKE/GRANT block in this migration) both from Postgres semantics and by direct observation (its live ACL still has no anon entry after 3 prior same-signature replaces: 090, 096, and now 115). One completeness gap found and reported above (Finding 1) — otherwise the catastrophic-tier posture (pre-apply rollback-transaction tests, diagnose-doc, live grant re-verification) is real, not just declared.

- **secrets_in_tree**: Grepped the entire staged diff (all 12 files, 2286 inserted lines) for `sk-`, `rzp_live_`, `AKIA`, `-----BEGIN`, JWT-shaped strings (`eyJhbGciOi...`), and generic `token`/`apikey`/`api_key`/`secret`/`credential`/`password` keywords. Zero credential-shaped literals anywhere, including in the two new SQL test files (`test/sql/cross_device_progress_optimistic_lock_verify.sql`, `security_definer_anon_revoke.sql` extension) and the new Dart contract test — the only synthetic-looking values are placeholder UUIDs (`00000000-0000-0000-0000-0000003b0001` etc.) and `test+3b-a@avya.local`-style synthetic emails used inside a `BEGIN...ROLLBACK` transaction, not real credentials. The one `secret`-adjacent hit was the diagnose-doc's own `touched_layers_checked` YAML field text "No secret or API key touched" (a true claim, not a literal). Clean.

- **unawaited_no_error_sink**: Enumerated every `unawaited(` textual match in the diff (5 hits). Two are prose inside the diagnose-doc markdown describing pre-existing, untouched call sites (not code). One (`sync_restore_completeness.dart`'s `unawaited(SyncService.instance.syncFreezes())` inside `_restoreFreezes`) is a diff CONTEXT line, not an added/touched line — confirmed via the unified diff (no leading `+`) and confirmed it's the same pre-existing line pre- and post-diff. The two genuinely NEW call sites are `sync_profile.dart:379` and `sync_restore_completeness.dart:159`, both `unawaited(ErrorTelemetry.logEvent(...))` on the "dropped after one retry" path — read `ErrorTelemetry.logEvent`'s implementation directly (`lib/core/services/error_telemetry.dart:251-283`): it wraps its entire body in its own `try { ... } catch (_) { /* Swallow — events must never break the host flow. */ }`, i.e., a declared internal error sink, matching the lens's own acceptance criteria. Cross-checked against the project's own automated gate, `dart run scripts/check_unawaited_has_error_sink.dart` (574 call sites scanned) — neither `sync_profile.dart:379` nor `sync_restore_completeness.dart:159` appear in its 95-entry "without nearby error sink" advisory list (the pre-existing `_restoreFreezes:348` line does appear there, but that's the untouched context line, not a new one). Separately noted (not scored as a finding): the new shared helper `SyncService._stampProgressVersion` does a bare `box.put('progress', p);` — not awaited AND not wrapped in `unawaited()` — which technically falls outside this lens's literal scope. Checked whether this is a fresh risk: grepped `lib/` for the same bare-Hive-put pattern and found 8+ pre-existing, unrelated call sites doing the identical thing (`badge_service.dart`, `day_rollover_service.dart`, `ai_coach_provider.dart`, `sign_in_screen.dart`, `profile_provider.dart`, `create_custom_exercise_sheet.dart`, `pwa_install_banner.dart`), two of which carry explicit comments justifying it via Hive's synchronous-in-memory write semantics (the disk flush is backgrounded, but the in-memory read-back is immediate, so no read-your-own-write bug). Treating this as an established, accepted codebase convention rather than a defect introduced by this diff — flagging only this one new instance while 8 pre-existing ones go unflagged would be inconsistent scope creep for a narrow B-pass, not a finding about this diff specifically.

## Founder triage notes
