---
reviewed_at: 2026-08-02T00:00:00+05:30
staged_against: 762507c768be
blast_radius: account
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 2
verdict: accepted
---

# Code Review — 762507c768be

## Finding 1 — P2 — blast_radius_mismatch
- **file:line:** `docs/diagnoses/2026-08-02-google-oauth-web-redirect-mobile-scheme-f2b8a1.md` frontmatter
- **claim:** The diagnose-doc self-declared `blast_radius: feature`, but the actual touched paths (`lib/features/auth/providers/auth_provider.dart`, `android/**`) are registered `account` tier in `docs/blast_radius.yaml`. Confirmed via `git diff --cached --name-only | dart run scripts/blast_radius_from_diff.dart -` → `Blast-radius: account`. Does not bypass any live gate (the gate computes independently from the diff, not from the doc's self-declaration) but is a documentation-accuracy defect that could mislead a future recurrence-lookup audit.
- **verification:** `dart run scripts/blast_radius_from_diff.dart` against the staged diff.
- **suggested-fix:** Change `blast_radius: feature` → `blast_radius: account`; soften `impact_analysis`'s "Low risk" framing to acknowledge the tier while noting it's narrowly scoped within it.
- **status:** accepted — fixed in same batch (doc now reads `blast_radius: account`, impact_analysis reworded).

## Finding 2 — P3 — correctness (diagnose-doc citation accuracy)
- **file:line:** `docs/diagnoses/2026-08-02-google-oauth-web-redirect-mobile-scheme-f2b8a1.md` frontmatter `readers:` entry
- **claim:** Cited reader line 41 is the pre-existing `LAUNCHER` intent-filter, not the new OAuth intent-filter — the new filter actually starts at line 47 (`<data>` element with the scheme/host match at line 51).
- **verification:** `Read` of the post-diff `AndroidManifest.xml` — lines 41-44 are the unrelated MAIN/LAUNCHER filter; lines 45-52 are the new OAuth filter.
- **suggested-fix:** Correct the citation to line 47.
- **status:** accepted — fixed in same batch (doc now cites line 47).

## Lenses that returned clean (verified, not assumed)

- **writer_reader_drift** — no Hive/cloud read or write in the diff; confirmed via scan for `.box(`, `Hive.`, `.from(`/`.select(`/`.insert(`/`.update(`.
- **function_exception_swallow** — `grep -n "functions.invoke("` over the diff: zero matches. Not applicable.
- **secrets_in_tree** — `git diff --cached | grep -inE "sk-|rzp_live_|AKIA|-----BEGIN|client_secret|apikey|api_key|GOCSPX|client_id"` returns only the diagnose-doc's own prose noting Client ID/Secret live in the Supabase dashboard — no actual credential literal present.
- **unawaited_no_error_sink** — `grep -n "unawaited("` over the diff: zero matches. Not applicable.
- **AndroidManifest scheme/host matching** — `android:host="login-callback"` with no path attribute matches Android's documented data-matching rule (unset path attributes match any path), and the scheme/host strings are character-identical to the redirectTo literal in `auth_provider.dart`.
- **kIsWeb branch reachability/syntax** — `kIsWeb` resolves via the already-imported `package:flutter/foundation.dart`; ternary is syntactically valid as a named-argument value.
- **Regression test validity** — confirmed non-vacuous: pre-fix manifest has none of the scheme/host/BROWSABLE markers (all 3 manifest sub-tests would fail pre-fix), and `'https://app.icanbefitter.com'` appears exactly once, added by this diff. All 6 tests pass post-fix (`flutter test`).
- **Manifest placement** — new intent-filter sits inside the same `.MainActivity` `<activity>` block that also carries the LAUNCHER filter — correct activity, not a stray alias.

## Founder triage notes

Both findings were documentation-accuracy issues in the diagnose-doc, not defects in the shipped code or test. Both fixed in the same batch before commit — no code changes required.
