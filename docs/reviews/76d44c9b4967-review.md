---
reviewed_at: 2026-07-31T00:00:00+05:30
staged_against: 76d44c9b4967
prior_hash: 6d2c1d8cc8da
blast_radius: catastrophic
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 3
verdict: accepted
---

# Code Review — 76d44c9b4967 (Unit 5, re-engagement-prefilter, OI-48)

## Context

This B-pass runs on the staged diff AFTER 2 independent context-blind review rounds (14
findings, all fixed and re-verified live). Per CLAUDE.md §4.3, this is the self-triggered
≥account (here: catastrophic) B-pass before merge, and a precondition for the required
Hermes-pass per §4.12.3.

## Hash regeneration — what changed after this B-pass ran

This file was authored against staged hash `6d2c1d8cc8da` and renamed to
`76d44c9b4967` when the staged diff moved. **The diff moved for reasons this
B-pass did not itself review**, so they are enumerated here rather than left
implicit — all of them were reviewed, by the Hermes pass that ran after:

1. **Hermes L34 fixes** — `errorSummary` now unwraps `.message` (a bare
   `String(err)` renders a supabase-js `PostgrestError` as `"[object Object]"`);
   both throw sites tagged `path_a …` / `path_b …`; `prefs_off` added to the
   zero-candidate response so both success shapes agree.
2. **Hermes L21 revert** — L34's `markProactiveSent` wrapper + `markFailures`
   counter were REMOVED after verifying the helper cannot throw
   (`_shared/proactive_dedup.ts:87-95` swallows internally). The regression
   test was rewritten to pin the helper's non-throwing *premise*.
3. **Hermes L31** — saturation detection (`POSTGREST_MAX_ROWS`) on both
   candidate paths; OI-79 filed for the pagination fix itself.
4. **Hermes L37** — the deliberate cutoff-vs-exclude-array guard asymmetry now
   documented in migration 117; an 11th live-verify case pinning `search_path`.
5. **Artifacts** — `docs/plan-reviews/re-engagement-prefilter.md`,
   `docs/audit/2026-08-01-hermes-reengagement-prefilter.md`, the OI-78/OI-79
   board entries + regenerated `OPEN_INDEX.md`.
6. **`backups/applied_migrations.json`** — migration 117's ledger entry, which
   could only be written AFTER the live apply (round-2 finding N1) and which
   `applied_migrations_parity_test.dart` requires in the same commit.

The three findings below were all fixed before the hash moved; none is stale.
Post-B-pass coverage lives in
`docs/audit/2026-08-01-hermes-reengagement-prefilter.md`.

## Lens results

- **writer_reader_drift** — CLEAN. Zero Hive touched anywhere in this diff (Deno Edge
  Function + Postgres migration only) — confirmed by grep, not assumed from the diff's
  description.
- **function_exception_swallow** — CLEAN. The one new `.rpc()` call
  (`re-engagement/index.ts:164-172`) checks `if (fallbackErr) throw fallbackErr;`, matching
  the file's existing Path A error-propagation pattern.
- **blast_radius_mismatch** — CLEAN, tier substantively earned. Reproduces `catastrophic`
  live via the documented content-rule (comment-only "SECURITY DEFINER" text, not a DDL
  clause — accepted on the merits per the diagnose-doc's own "Blast-radius confirmation"
  section). Rollback is inline and documented; `touched_layers_checked` correctly marks
  tiers 5/6 `pending_explicit_authorization` rather than overclaiming.
- **secrets_in_tree** — CLEAN. Zero credential-shaped literals in the staged diff.
- **unawaited_no_error_sink** — N/A. Zero `unawaited(` calls in the diff.

## Finding 1 — P2 — factual/documentation-accuracy (090/091 timing+count)
- **file:line:** `docs/diagnoses/2026-07-31-reengagement-prefilter-a4e1c9.md` (symptom field +
  Root cause section), `supabase/migrations/117_reengagement_silent_candidates_rpc.sql:74-77`,
  `docs/audit/open_issues.md` (OI-78 "What's wrong")
- **claim:** an earlier draft said `find_orphan_chat_media`'s creating migration (071) "was
  created a week after" migrations 090/091's SECURITY DEFINER audit, and that 090/091 fixed
  "8" functions.
- **verification:** `git log --diff-filter=A --format=%ai -- supabase/migrations/071_*.sql`
  → 2026-05-17; same for `090*.sql`/`091*.sql` → 2026-06-11. 071 PREDATES 090/091 by ~25
  days, not after. Live `pg_proc.prosecdef` for all 9 functions 090/091 REVOKE — all
  `true`, so the count is 9, not 8.
- **status:** REAL, FIXED. Reworded in all 3 locations: the real reason
  `find_orphan_chat_media` wasn't covered is SCOPE (090/091's REVOKE pass targeted SECURITY
  DEFINER functions specifically; `find_orphan_chat_media` is plain SQL/STABLE), not timing.

## Finding 2 — P3 — same locations, count precision
- Folded into Finding 1's fix (8→9, live-verified).

## Finding 3 — P2 — rollback documentation gap
- **file:line:** `supabase/migrations/117_reengagement_silent_candidates_rpc.sql`
  (rollback comment block)
- **claim:** the inline rollback DDL drops the new function but doesn't note that
  `re-engagement/index.ts`'s Path B has an unconditional runtime dependency on it (the old
  per-user-loop fallback was deleted, not preserved) — rolling back the migration alone
  while the EF still calls the dropped RPC would 500 every cron tick.
- **verification:** read `re-engagement/index.ts:164-172` — no fallback path survives; the
  RPC call result feeds directly into `mapFallbackCandidates` with no catch-and-degrade.
- **status:** REAL, FIXED. Added a coupling warning to the rollback comment: reverting this
  migration requires reverting the Edge Function deploy in the same operation.

## Founder triage notes

All 3 findings fixed in-batch (no deferrals). Verdict: accepted — proceeding to Hermes-pass.
