---
reviewed_at: 2026-08-02T07:20:00+05:30
staged_against: 6d0855844893
blast_radius: account
reviewer: claude-sonnet-via-skill (fresh, context-blind)
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 5
verdict: accepted
---

# Code Review (B-pass) — `6d0855844893`

Branch `oi50-exlog-aggregate-read`, Unit 7 / OI-50, diagnose `d4e7c2`.
Run AFTER two context-blind plan-review rounds, so the lenses were pointed at
the hardened diff. All 5 findings fixed before the merge.

## Finding 1 — P1 — writer_reader_drift — FIXED

- **file:line:** `lib/features/train/providers/train_provider.dart:1556`
- **claim:** A **seventh** unconverted hand-rolled aggregate reader, driving the
  workout-finish PR banner. The divisor was
  `((set_number ?? sets_completed) ?? 1)` — first-non-null, blind to the per-set
  array length. On the documented APK Test #12.1 shape (BOTH count keys present
  and `0` while `sets[]` is populated) `??` never fires, the divisor is `0`, the
  `> 0` gate fails, and the reps estimate collapses to `0` — silently
  suppressing a genuine PR with no error and no telemetry. Not an edge case:
  `best_single_set_reps` (the short-circuit above it) is written ONLY by the
  edit sheet, never by `WorkoutWriteService.logExercise`, so the fallback is the
  common path for every un-edited completion.
- **verification:** `grep -n "best_single_set_reps" lib/core/services/workout_write_service.dart` → no matches.
- **fix applied:** divisor is now `WorkoutReadService.aggregateSetCount(log)`.
- **status:** fixed

## Finding 2 — P2 — unawaited_no_error_sink / observability gap — FIXED

- **file:line:** `lib/features/train/widgets/workout_receipt_card.dart:380`
- **claim:** The new `exlog_no_aggregate_signal` event guarded only
  `hasAggregateSetCount`. A `timed`/`cardio` row can have a valid set count
  while every per-set entry omits its duration (the edit sheet writes
  `duration_seconds` into a set map only `if (duration > 0)`) and the modern
  writer never stamps a top-level one — so `?? 0` renders "0m 0s" for a logging
  type whose primary metric IS duration, uninstrumented.
- **verification:** `grep -n "hasAggregateSetCount\|aggregateDurationSeconds" lib/features/train/widgets/workout_receipt_card.dart`
- **fix applied:** the event now also fires when `aggregateDurationSeconds` is
  null for `timed`/`cardio`, and the message carries `set_signal` /
  `duration_signal` so the two causes are distinguishable in telemetry.
- **status:** fixed

## Finding 3 — P3 — docs-drift (closure record undercounts) — FIXED

- **file:line:** `docs/audit/open_issues.md` OI-50 closure block
- **claim:** Said "not 2 but 5" and omitted the round-2-discovered sixth reader
  (`workout_repository.dart`), while CLAUDE.md and the diagnose-doc said SIX.
- **fix applied:** corrected to **7** (Finding 1 added a seventh), naming
  `workout_repository.dart:941` and `train_provider.dart:1556` explicitly.
- **status:** fixed

## Finding 4 — P3 — docs-drift (citation resolves to wrong line) — FIXED

- **file:line:** `docs/sot_registry.yaml` `workout_log_edit_surface` note
- **claim:** Cited the `hasAggregateData` save-guard at
  `edit_workout_log_sheet.dart:236`; line 236 is an unrelated `break;`. The
  guard is at `:255` — a 19-line offset introduced when the round-1 F5 comment
  block was inserted above it without re-deriving the citation.
- **verification:** `sed -n '236p;255p' lib/features/train/widgets/edit_workout_log_sheet.dart`
- **fix applied:** corrected to `:255`.
- **status:** fixed

## Finding 5 — P3 — cross-account static state — FIXED

- **file:line:** `lib/features/train/widgets/workout_receipt_card.dart:291`
- **claim:** `_noAggregateSignalReported` is a process-lifetime static `Set`,
  never cleared on account switch, so a second user on the same process whose
  row collides on `(date, exercise)` gets their signal silently swallowed.
  Observability-only — no data crosses accounts — but this repo treats
  un-scoped static state surviving an account switch as inherently flaggable.
- **verification:** `grep -rn "_noAggregateSignalReported" lib/`
- **fix applied:** registered with the repo's existing primitive —
  `SingletonLifecycleRegistry.register(...)`, which `HiveUserSession` fires via
  `notifyUserChanged()` on every switch/logout. Also bounds growth.
- **status:** fixed

## Checked clean

- **function_exception_swallow** — `git diff --cached | grep -n "functions.invoke("` → 0 matches. No Edge Function calls in this diff.
- **blast_radius_mismatch** — ran the real classifier: `git diff --cached --name-only | dart run scripts/blast_radius_from_diff.dart -` → `account`. `lib/features/train/**` defaults `feature`; `lib/core/services/**` is `account` and `workout_read_service.dart` is touched, which is what sets the tier. Declared tier matches computed tier.
- **secrets_in_tree** — `git diff --cached | grep -nE "sk-[A-Za-z0-9]{10,}|rzp_live_|AKIA[0-9A-Z]{16}|-----BEGIN"` → 0 matches.
- **`_perSetList` "longer array" safety** — traced every writer of `sets_detail`: only the edit sheet writes it, and `editLog:754-774` translates that same payload into `sets` in one merge, so both keys always land at equal length. No writer produces a longer-and-staler `sets_detail`, so the tie-break can never select a stale array.
- **null-vs-0 for `aggregateDurationSeconds`** — the one caller that must distinguish them (`hasAggregateDur`, the save guard) uses `!= null`; the display readers collapse `?? 0`, which is correct since there is no user value to protect there.

## Founder triage notes

All 5 accepted and fixed in-branch; no finding spawned a follow-up. Post-fix:
`flutter analyze` → **No issues found** across all 8 changed lib files;
**63 tests pass** across the 10 affected contract files.
