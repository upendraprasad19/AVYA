---
reviewed_at: 2026-08-07T00:00:00+05:30
staged_against: 62ab8735369f
landed_as: cc6d29c
followup_commit: pending
blast_radius: platform
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink]
findings_count: 7
false_alarms: 0
verdict: accepted
---

# Code Review (B-pass) — 62ab8735369f

Unit 1 of the OI backlog batch (OI-75 / OI-76 / OI-82). Dispatched to a fresh,
context-blind Sonnet subagent per `.claude/skills/code-review/SKILL.md`, prompted
to find bugs rather than validate, with the 5 standard lenses plus 6
diff-specific checks (router `extra` cast, captured `BuildContext`, list-aliasing,
Edge Function rename completeness, test quality, doc-citation spot-check).

**Seven findings, zero false alarms.** All seven were accepted and fixed in the
same batch per §4.2; none were rejected or spawned as follow-ups. The
false-positive rate is 0%, so no lens tuning is indicated (SKILL.md §5).

Note on hashes: the review ran against the staged tree `62ab8735369f`, which
landed as commit `cc6d29c`. The fixes below are in the follow-up commit, so the
current staged hash differs — recorded here rather than renaming the file,
because the file should be named for the state that was actually reviewed.

## Finding 1 — P2 — blast_radius_mismatch → FIXED
- **file:line:** docs/audit/oi_unit1_backlog.closure.yaml:25
- **claim:** Ledger self-declared `blast_radius: feature`; the measured tier is
  `platform` (root `CLAUDE.md` and `supabase/functions/**` both map to platform).
  Gate 40 never reads this field, so a wrong value passes silently.
- **verification:** `dart run scripts/blast_radius_from_diff.dart` → `platform`;
  `grep -n blast_radius scripts/validate_audit_closure.dart` → no match.
- **fix applied:** field corrected to `platform`.
- **status:** fixed
- **note:** the reviewer's structural suggestion — have `validate_audit_closure.dart`
  recompute the tier and fail when the ledger understates it — is a real gap and is
  NOT done here. It is a new gate, which §4.11 says ships as its own unit with a
  warn-only baseline, not bolted onto a batch it would retroactively judge.

## Finding 2 — P3 — generated_index_staleness → NO CHANGE NEEDED
- **file:line:** docs/audit/OPEN_INDEX.md (13 rows with +4 anchor drift)
- **claim:** the staged index did not match its own generator's output.
- **verification:** re-ran `dart run scripts/build_oi_index.dart` after the commit;
  `git diff docs/audit/OPEN_INDEX.md` → empty.
- **resolution:** self-healed. `scripts/pre-commit.sh` regenerates and re-stages
  the index whenever `open_issues.md` is in the commit, and the commit went
  through `safe_commit.sh`, so the hook ran. The reviewer saw a mid-edit tree.
- **status:** no_change_needed

## Finding 3 — P3 — test_quality → FIXED
- **file:line:** test/contracts/promote_community_vote_tally_test.dart:53-59
- **claim:** `_codeOnly()`'s line-comment stripper used `indexOf('//')`, which also
  matches inside string literals. It truncated 3 live lines of the target file
  (two `https://` import specifiers and the OneSignal fetch URL) mid-statement.
  Proven inert against today's 5 assertions, but it makes the stripper lie about
  what the code says.
- **verification:** reviewer traced each assertion against the mangled string and
  confirmed none currently shares a line with a URL.
- **fix applied:** replaced with `_stripLineComment`, a string-literal-aware scanner
  handling `"`/`'`/backtick quoting and backslash escapes. Tests still pass (14/14
  across the three new files).
- **status:** fixed

## Finding 4 — P3 — doc_citation_accuracy → FIXED
- **file:line:** docs/audit/open_issues.md:1327
- **claim:** cited `paywall_sheet.dart:363` for the letterhead string; the batch's
  own two new `_featureSubtitle` cases shifted it to :367.
- **fix applied:** citation updated to :367.
- **status:** fixed

## Finding 5 — P3 — doc_citation_accuracy → FIXED
- **file:line:** docs/diagnoses/2026-08-07-promote-community-dead-rpc-d5b8c2.md:26
- **claim:** `promoteExercises` cited at line 173; the declaration is at 174 (173
  is blank).
- **fix applied:** citation updated to 174.
- **status:** fixed

## Finding 6 — P3 — doc_citation_accuracy → FIXED (narrowly)
- **file:line:** CLAUDE.md:305, docs/audit/open_issues.md:658,
  docs/diagnoses/…-d5b8c2.md:150
- **claim:** all cite `feedback_source_grep_false_confidence.md`, which does not
  exist in the repository. Only 2 of ~66 referenced `feedback_*.md` names resolve;
  the rest live in a harness-local, untracked directory that a fresh clone or a
  cloud session can never read. Pre-existing repo-wide pattern, but this diff added
  fresh citations to it.
- **verification:** `find . -iname "feedback_*.md"` → 2 results; `memory/MEMORY.md:8`
  documents the trap explicitly.
- **fix applied:** the class is now named in words at each of this batch's own
  citation sites so the reference is followable from a clone, with a pointer to
  `memory/MEMORY.md:8`. The pre-existing citation in CLAUDE.md §4.4 r21 is
  annotated rather than deleted — the name is load-bearing vocabulary across the
  board, and silently removing it from the rule would lose more than it fixes.
- **status:** fixed
- **note:** the general problem (64 unfollowable citations repo-wide) is untouched
  and belongs to whoever picks up the docs-rot class alongside OI-91.

## Finding 7 — P3 — api_consistency → FIXED
- **file:line:** lib/features/profile/services/notification_prefs_repository.dart:90-92
- **claim:** `controllableKeys` returned a `const` list (fully unmodifiable) on the
  PRO branch and `.toList(growable: false)` (fixed-length but element-settable) on
  the free branch — one declared return type, two mutability contracts.
- **verification:** only caller (`profile_content.dart:72`) iterates and reads
  `.length`, so latent rather than live.
- **fix applied:** both branches now return `List<String>.unmodifiable(...)`.
- **status:** fixed

## Lens coverage

Every lens reported with specifics rather than a bare "clean" — see the dispatch
record. Summary:

| Lens | Result |
|---|---|
| writer_reader_drift | clean — confirmed `allKeys` + `emissionMap()` byte-identical and unfiltered on every path; the briefed P0 (free user's snapshot omitting the 2 PRO keys → ABSENT ⇒ SEND turning them ON) cannot occur |
| function_exception_swallow | clean — no `.functions.invoke(` touched; the EF's try/catch is byte-identical to pre-diff; the removed guard was unreachable |
| blast_radius_mismatch | 1 finding (F1) |
| secrets_in_tree | clean — no credential-shaped literals; the only credential-adjacent string is the public project ref already in CLAUDE.md §2a |
| unawaited_no_error_sink | clean — no `unawaited(` added or touched |
| (a) router `extra` cast | clean — all 4 push sites pass `void Function(String)`; a bare push yields `null`, and `null as ValueChanged<String>?` succeeds |
| (b) captured BuildContext | clean — same shape as the pre-existing reviewed pattern; `context.push` keeps the pusher mounted |
| (c) list aliasing | 1 finding (F7) |
| (d) EF rename completeness | clean — whole file read; zero live references to the old name |
| (e) test quality | 1 finding (F3); the other two files' assertions confirmed non-tautological against manual reverts |
| (f) doc citation spot-check | 3 findings (F4, F5, F6); 24+ citations checked, the rest exact |

## Founder triage notes

Triaged by the implementing session under §4.2 (fix all in the same batch, no
deferrals). All 7 accepted: 6 fixed, 1 (F2) confirmed already resolved by the
pre-commit regen. Two structural suggestions were deliberately NOT absorbed and
are recorded above rather than dropped: the blast-radius self-report gate (F1) and
the repo-wide unfollowable-citation cleanup (F6). Both are new-gate / sweep work
that §4.11 and §4.12.1 say should ship as their own units.
