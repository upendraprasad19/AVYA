---
reviewed_at: 2026-09-22T03:02:11+05:30
staged_against: d2ce94ab0a72
blast_radius: account
reviewer: claude-sonnet-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink, guard_without_its_mirror, missing_input, asserted_fixture_value]
findings_count: 3
verdict: accepted
---

# Code Review — d2ce94ab0a72

> Renamed twice: first from `a4bcd634b7a7-review.md` to `5ebf78e29706-review.md`
> after all 3 findings below were fixed and re-staged (the standard
> hash-fixed-point rename this skill's own tuning history documents
> repeatedly — `docs/reviews/` is hash-excluded so triaging THIS file is
> free, but the fix commits touched files that are not excluded and moved
> the staging hash the gate keys on); then again to this filename after
> rebasing the branch onto a moved `origin/main` (PR #31 merged mid-batch,
> touching the same OI board entries this diff also touches — resolving
> that conflict changed the staged diff's content again, and with it the
> hash, though no reviewed CODE changed in either rename). Content
> otherwise unchanged from the original dispatch except each finding's
> `status:` line.

## Finding 1 — P1 — guard_without_its_mirror
- **file:line:** `test/ai_coach/initial_scroll_to_bottom_test.dart:101-138` (new test "channel-switch-back re-triggers _jumpToBottom (OI-232)"); root cause is `test/helpers/read_screen_source.dart:31-49` (`readScreenSource`, no comment-stripping — contrast with the same file's own `_stripComments`-backed `readLibrarySource` added later, `:65-107`)
- **claim:** The only regression test guarding the OI-232 fix (`lib/features/ai_coach/screens/ai_coach/screen.dart:464-466`, the new `ref.listen(channelProvider, ...)` that re-scrolls to bottom when the user switches back from Telegram) is a pure source-TEXT-presence check via `readScreenSource()`, which concatenates the raw file contents with comments intact. I mutated the working tree to wrap the entire fix in a comment (`// TEMP DISABLED FOR DEBUG` + `//`-prefixed lines) — leaving the exact same substrings (`ref.listen(channelProvider`, `next == 'in_app'`, `previous`, `_jumpToBottom()`) present in the file, just inert — and re-ran the file: all 5 tests, including the new OI-232 case, still reported "All tests passed!". The test cannot distinguish "the fix is live" from "the fix is fully disabled but still typed into a comment somewhere in the file". This is exactly the lens-6 "if the guard is a SOURCE GREP, assume it is defeatable" shape, and the mutation used is the realistic one the method note calls for ("someone restores/comments the old behavior out" — not deletion, which is what the diagnose-doc's own mutation-proof tested instead: a full `git checkout` revert to pre-fix HEAD, which removes the substring entirely rather than probing whether present-but-dead text is detected). `docs/blast_radius.yaml`'s `account` tier (this diff's own confirmed tier, re-verified via `dart run scripts/blast_radius_from_diff.dart`) requires `behavioral_test_path`, and no other test in the repo touches `channelProvider` or pumps `AiCoachScreen` in a way that exercises this mechanism (`grep -rln "channelProvider" test/` → only this one source-grep file; the one other `AiCoachScreen(`-pumping test, `test/features/ai_coach/coach_chat_restore_invalidation_test.dart`, is about an unrelated restore-invalidation bug and asserts nothing about scroll position).
- **verification:** Comment out `lib/features/ai_coach/screens/ai_coach/screen.dart:464-466` (the `ref.listen(channelProvider, ...)` block), then `flutter test test/ai_coach/initial_scroll_to_bottom_test.dart` → still 5/5 "All tests passed!". (I performed this mutation directly, confirmed the red-vs-green outcome, then restored via `git checkout -- lib/features/ai_coach/screens/ai_coach/screen.dart` and re-confirmed 5/5 green and a clean `git status`.)
- **suggested-fix:** Add a genuine widget-pump test: build `AiCoachScreen` under a `ProviderScope` with `channelProvider` overridden/settable, seed `chatHistoryProvider` with messages, pump, flip the channel `in_app` → `telegram` → `in_app`, and assert the scroll position lands at/near `maxScrollExtent` after the switch-back (e.g. via a `Finder`/`tester.getRect` on the last message, or by exposing the controller's offset for the test). If a full widget-pump is judged too costly for this screen (it depends on SpeechToText/ImagePicker/SyncService/SubscriptionService — the same friction CLAUDE.md's common-pitfalls table documents for this screen's siblings), at minimum switch this file to a comment-stripping source read (mirroring `readLibrarySource`'s `_stripComments`) so a commented-out block can no longer satisfy the assertions — weaker, but closes the cheapest defeat vector.
- **status:** fixed — took the cheaper option, since ALL FOUR pre-existing tests in this file are already source-grep (not widget-pump) by established convention, so a full widget-pump would have been inconsistent with the file's own pattern rather than a targeted fix. Added `readScreenSourceStripped()` to `test/helpers/read_screen_source.dart` (wraps the existing private `_stripComments` — previously only used by `readLibrarySource`) and switched this file's shared `setUpAll` to it, so the fix applies to all 5 tests in the file, not just the new one. Mutation re-verified: the exact "comment out, don't delete" mutation the finding used now reddens exactly the OI-232 test (Expected: true / Actual: false), the other 4 stay green; restored and reconfirmed 5/5 green.

## Finding 2 — P2 — asserted_fixture_value (self-attesting artifact)
- **file:line:** `docs/audit/closed_issues.md` — new OI-230 entry (Status line + trailing `**Closes**:` line) and new OI-232 entry (same two spots); also `test/ai_coach/initial_scroll_to_bottom_test.dart` (new test's `reason:` string)
- **claim:** Both new closure records cite an unresolved `<pending>` placeholder for their diagnose-doc identity — `- **Status**: CLOSED (2026-09-22, ...) — diagnose \`<pending>\`` and `**Closes**: diagnose-doc \`docs/diagnoses/<pending>.md\`.` — even though this SAME staged diff already creates the real files with fully-known names: `docs/diagnoses/2026-09-22-rank-eta-binding-constraint-blind-a8f3e2.md` (frontmatter `bug_id: a8f3e2`, matching OI-230) and `docs/diagnoses/2026-09-22-coach-scroll-channel-switch-c1b9d4.md` (frontmatter `bug_id: c1b9d4`, matching OI-232). This differs from the repo's established (and legitimate) `commit \`<pending>\`` convention — I checked ~20 historical entries in the same file and every one of those placeholders is for the commit SHA specifically, which genuinely cannot be known before `git commit` runs. The diagnose-doc's identity has no such excuse: it was written to disk, with a fixed bug_id, in this same commit. A reader who follows "Closes: docs/diagnoses/<pending>.md" today finds nothing, while the correct file sits one `ls docs/diagnoses/` away. A fifth, lower-stakes instance of the identical oversight is in the new test's own failure-reason prose (`'closes-diagnose: <pending>'`, only ever seen if that assertion fails).
- **verification:** `git diff --cached | grep -n "^\+.*pending>"` → 5 hits, at diff lines 34, 112, 116, 171 (closed_issues.md) and 1273 (the test file). Cross-check: `grep -n "## OI-230\|## OI-232" docs/audit/closed_issues.md` then read each block — both already-created diagnose docs exist on disk right now (`ls docs/diagnoses/2026-09-22-*`).
- **suggested-fix:** Replace `<pending>` with the real values: OI-230 → `diagnose \`a8f3e2\`` / `docs/diagnoses/2026-09-22-rank-eta-binding-constraint-blind-a8f3e2.md`; OI-232 → `diagnose \`c1b9d4\`` / `docs/diagnoses/2026-09-22-coach-scroll-channel-switch-c1b9d4.md`; and the test's `reason:` string → `closes-diagnose: 2026-09-22-coach-scroll-channel-switch-c1b9d4`.
- **status:** fixed — all 5 sites (4 in `closed_issues.md`, 1 in the test's `reason:` string) replaced with the real diagnose-doc identities exactly as suggested. Re-grepped: `grep -n "pending>" docs/audit/closed_issues.md` now only matches the pre-existing, legitimate `commit <pending>` convention (genuinely unknown until `git commit` runs) — zero hits inside the OI-230/OI-232 blocks.

## Finding 3 — P3 — missing_input (gate-blind stale citation)
- **file:line:** `docs/sot_registry.yaml` new `rank_monotonic_current_code` reader entry — `line_range: 1384-1465`, `method: _getCurrentRankFromLadder / _getNextRankFromLadder`
- **claim:** The cited range ends 6 lines short of `_getNextRankFromLadder()`'s real closing brace. In the fixed file, `_getCurrentRankFromLadder()` spans 1385-1402 and `_getNextRankFromLadder()` spans 1404-1471 (confirmed by `Read`) — so the correct combined span is 1384-1471, not 1384-1465. Line 1465 lands mid-method on `'workouts': totalWorkouts,` inside the `current_state` map, excluding lines 1466-1471, which include exactly the `'remaining': remaining` and `'binding_constraint': binding` entries — the two fields this same batch's `scripts/check_hive_map_field_drift.dart` change is specifically about. `check_sot_registry_parity.dart` cannot catch this because its `method:` field here is two identifiers joined by `" / "`, which fails `_bareSymbolRe` (`^[A-Za-z_][A-Za-z0-9_]*(\.[A-Za-z_][A-Za-z0-9_]*)*$`) — so `_extractSymbol()` returns `null` ("prose — skip symbol check") and the gate silently skips BOTH the method-existence and stale-line-range checks for this entry; only the (unaffected) `range-overflow` bound check still runs. This is the same "a citation lives in a field a gate reads, but the gate's parser cannot actually read this value" class this skill's own tuning history already names (2026-09-03 entry). Low practical impact — both methods sit immediately adjacent in the file, so a human reader lands in the right neighborhood regardless — but it is a real, verifiable citation drift that reads as gate-checked and is not.
- **verification:** `dart run scripts/check_sot_registry_parity.dart` → `PASS — registry file:line parity checked (0 errors, 1 orphan service/repo classes warned)` (the 1 orphan warning, `CoachMediaRepository`, is pre-existing and unrelated to this diff). Cross-check the real span: `Read lib/features/ai_coach/services/ai_snapshot_builder.dart` lines 1385-1402 (`_getCurrentRankFromLadder`) and 1404-1471 (`_getNextRankFromLadder`, closing brace at 1471).
- **suggested-fix:** Widen `line_range` to `1384-1471`, or split into two separate block-form entries (one per method, each with its own bare-identifier `method:` field) so each is actually verified by the gate going forward.
- **status:** fixed — took the minimal option (widened `line_range` to `1384-1471`); independently re-confirmed the real span by reading the file directly (`_getCurrentRankFromLadder` 1385-1402, `_getNextRankFromLadder` 1404-1471, closing brace at 1471) rather than trusting the finding's numbers. Splitting into two gate-verified bare-identifier entries is a real structural improvement but a larger, separate change to the registry's entry shape for this one concept — not taken here to keep the fix scoped to the actual citation drift.

## Founder triage notes
<filled in by founder during triage>
