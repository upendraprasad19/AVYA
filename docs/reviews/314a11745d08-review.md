---
reviewed_at: 2026-07-18
staged_against: 314a11745d08
blast_radius: platform
reviewer: claude-sonnet-via-skill
lens_set: [flag_off_inertness, safety, tracer_mirror_fidelity, threading_completeness, map_correctness, preferredFor, behavioral_test_rigor, secrets_in_tree]
findings_count: 0
verdict: accepted
---

# Code Review — 314a11745d08 (Batch 11-C ①.1d curated injury-substitute preference)

Fresh context-blind Sonnet B-pass over the staged diff (723 lines, 9 files). **No P0/P1/P2
findings survived verification.** The reviewer independently ran the test files and
cross-referenced the curated map against the live library.

## Findings
None.

## Lenses clean (verified against code + live test runs, not assumed)
- **Flag-OFF inertness** — `_selectCandidate`'s first line is `if (!applyInjurySubstitutePreference) return candidates.first;`; the flag defaults `false` at every layer (pickV4/buildPinnedDays/_fillSlots/_cascadeFill/CascadeTracer.trace); `injurySubstitutePreferenceEnabled` returns false on unset-key AND on Hive-throw. The 7 `CascadeTracer.trace` call sites all stay defaulted → the D3 frozen-baseline scorecard gate passes unchanged (ran it).
- **Safety** — `subs ⊆ candidates` (a `.where` filter of the already-injury-filtered list) → never surfaces a contraindicated exercise; `prefs.indexOf` is never −1 (every `subs` member is in `prefs`). Dual-injury case verified (Landmine Press curated for wrist but shoulder-tagged → queryV4 excludes it first).
- **Tracer mirror fidelity** — `_selectCandidateName` is line-by-line identical to `_selectCandidate` (map lookup, lowercase, where, indexOf-sort, fallback); all 4 tracer pick sites use it; attempt-5 untouched in both.
- **Threading completeness** — every hop generateV4→pickV4/buildPinnedDays(+fresh())→_fillSlots→_cascadeFill carries the flag; plan_generator is the only production caller; no crossed-wire named args.
- **Map correctness** — all 20 curated names cross-referenced against exercise_library.json (258 rows): each an exact name, same-movement_pattern, not self-tagged; all 16 pattern counts + 9 injury totals recomputed exactly.
- **preferredFor** — ordered/deduped/lowercased `List<String>` (Round-2 Set→List fix in place); empty for uncurated/empty injuries; map keys match InjuryVocab.canonicalTokens vocabulary.
- **Behavioral test rigor** — ran it (4/4 pass); test (1)'s ordered-list inequality is non-tautological (phase-1 gates off L2/L6 + progression, so the only variable is the flag); safety + inertness cases meaningful. 27 related tests pass (v4_diagnostic, repeat_phase, injury_safe_omission).
- **secrets / unawaited / function-exception** — clean (synchronous pure-Dart, no new async/network/credentials).

## Founder triage
Self-triaged under auto-mode: 0 findings. Verdict accepted.
