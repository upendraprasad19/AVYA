---
reviewed_at: 2026-07-27
kind: merge-diff attestation
branch: cron-secret-auth
merge_into: main
blast_radius: catastrophic
staged_diff_hash: fb97257a6ca0
verdict: accepted
---

# Merge-diff attestation — `cron-secret-auth` → `main`

This file exists because `check_code_review_pass_exists.dart` keys on the
**staged diff**, and at a merge commit that is a different diff — and therefore
a different hash — from any commit that was authored on the branch. It is an
attestation that this exact merge diff is covered, not a fresh independent
review. Stating that plainly is the point: a reader should not mistake it for a
fifth review round.

## What this diff is

The `cron-secret-auth` branch, merged whole. Catastrophic tier is forced by
**content**, not path — migrations 107 / 109 / 110 contain `SECURITY DEFINER`
(the gate reports each one).

## What already reviewed this content

| Artifact | Scope |
|---|---|
| `docs/reviews/a3ff9571fbc9-review.md` | B-pass on the code commit's own staged diff, `verdict: accepted` |
| `docs/audit/2026-07-26-hermes-cron-secret-auth.md` | Hermes pass (catastrophic tier requires it) |
| `docs/plan-reviews/cron-secret-auth.md` | 4 review rounds, `verdict: converged` |

Those four rounds were not ceremonial — each caught a defect that would
otherwise have shipped, including a uniform cron repoint that would have broken
the one job still working, a cleanup migration that would have destroyed the
incident's own evidence, and a set of fixes that were never actually staged.

## What is in this merge diff and NOT in that review

Exactly one thing: `docs/diagnoses/INDEX.md`, regenerated by
`scripts/build_bug_index.dart` to resolve the only merge conflict. It is a
generated file — 335 bugs indexed — and was rebuilt from the merged docs rather
than hand-resolved. No hand-written content exists in this merge that was absent
from either parent.

That distinction matters and is the known weak point of merge-time attestation:
conflict resolution *can* introduce content reviewed on neither side. Here it
did not, and this file is the record of having checked rather than assumed.

## Live-state caveat carried forward

Migrations 107-110 are already applied to prod. The Edge Functions gated by this
batch have **not** been redeployed — that is a live prod action requiring
explicit founder authorization, and it is recorded in the diagnose-docs.
Merging this branch does not make the fix live.

## Verdict

Accepted. The reviewed content is unchanged by the merge, and the sole addition
is a regenerated index.
