---
reviewed_at: 2026-09-17T20:30:00+05:30
staged_against: 7446a3c8a418
branch: web-razorpay-checkout
blast_radius: account
reviewer: claude-qa-agent-via-skill
lens_set: [writer_reader_drift, function_exception_swallow, blast_radius_mismatch, secrets_in_tree, unawaited_no_error_sink, guard_without_its_mirror, missing_input, asserted_fixture_value]
findings_count: 8
verdict: accepted
---

# B-pass review — web-razorpay-checkout @ 7446a3c8a418

Fresh context-blind qa-agent over the 15-file branch diff vs `main`. All 8 findings triaged `accepted` and fixed in the same session (commit following this file). Severity counts: P0 0 · P1 0 · P2 4 · P3 4. False alarms: 0.

## Finding 1 — P2 — function_exception_swallow — RESOLVED
- **file:line:** lib/core/services/razorpay_web_checkout_web.dart (bridge catch-all)
- **claim:** the only catch in the payment path with no `ErrorTelemetry` pair (H-42 convention); a field failure (ad-blocker/CSP/CDN outage) would be invisible.
- **verification:** `git grep -n "recordNonFatal" lib/core/services/razorpay_web_checkout_web.dart` → 0 hits pre-fix.
- **fix:** catch captures `e, st` → `unawaited(ErrorTelemetry.recordNonFatal(e, st, reason: 'razorpay_web_checkout_unavailable'))`; user-facing message unchanged.
- **status:** accepted (fixed)

## Finding 2 — P2 — guard_without_its_mirror — RESOLVED
- **file:line:** razorpay_web_checkout_web.dart header + razorpay_service.dart onDismissed comment
- **claim:** "checkout.js has NO failure callback" is FALSE — Razorpay documents `rzp1.on('payment.failed', …)`. The comment recorded a false technical claim as justification for an unwired error path.
- **fix:** `payment.failed` is now WIRED (bridge `instance.on('payment.failed')` → new `onPaymentFailed(message)` callback), not just comment-corrected — which also resolves Finding 3. Spec updated to match.
- **status:** accepted (fixed)

## Finding 3 — P2 — guard_without_its_mirror — RESOLVED
- **file:line:** razorpay_service.dart web branch + paywall_sheet.dart onFailure
- **claim:** web had NO app-level failure feedback: the only `openCheckout` caller's `onFailure` is empty, native's error-snackbar mirror was missing, and `ondismiss` fires even after a failed payment.
- **fix:** `_showPaymentFailedFeedback(msg)` extracted from `_handlePaymentError` and shared by both paths; web `onPaymentFailed` → snackbar + `onFailure`.
- **status:** accepted (fixed)

## Finding 4 — P2 — writer_reader_drift / asserted_fixture_value — RESOLVED
- **file:line:** docs/sot_registry.yaml:2624
- **claim:** `line_range: 382-500` was correct at the extraction commit (`bc3e1819`) but three later SAME-BATCH commits shifted the method without re-pointing — the common-pitfalls line-count-shift class, invisible to `check_sot_registry_parity.dart` (round-2 P2: the gate keys on the class name).
- **fix:** re-derived LAST after all other fixes landed (2026-09-16(d) Tuning 4 method): method now spans **453-568**.
- **status:** accepted (fixed)

## Finding 5 — P3 — guard_without_its_mirror — RESOLVED
- **file:line:** razorpay_service.dart web branch
- **claim:** ondismiss-after-success double-fire is docs-silent; a success-then-failure callback inversion is possible.
- **fix:** one-shot `settled` latch in the web branch — first callback wins; the rest no-op. Cheap insurance independent of SDK behavior.
- **status:** accepted (fixed)

## Finding 6 — P3 — guard_without_its_mirror — RESOLVED
- **file:line:** razorpay_service.dart
- **claim:** the service kill-switch branch was the only openCheckout early-return with no user feedback, and ran AFTER `create-razorpay-order` (wasting a server order when flipped).
- **fix:** check hoisted to the top of `openCheckout` (before key/user/order-create). Paywall keeps the informative snackbar path; the hoisted return is silent defense for future non-paywall callers.
- **status:** accepted (fixed)

## Finding 7 — P3 — missing_input — RESOLVED
- **file:line:** web/index.html
- **claim:** the inserted script+comment pushed `<meta charset="UTF-8">` past the HTML 1024-byte first-window (measured offset 1088) — a latent garbling trap.
- **fix:** charset meta moved above the Razorpay script block.
- **status:** accepted (fixed)

## Finding 8 — P3 — asserted_fixture_value — RESOLVED
- **file:line:** lib/core/services/razorpay_web_checkout.dart:23
- **claim:** `v == null ? null : v.toString()` trips `prefer_null_aware_operators`.
- **root cause worth recording:** the fix had been applied once already, then silently DISCARDED by the author's own `git checkout --` mutation-restore in Task 6 (the file carried uncommitted lint fixes when the restore ran). The fresh reviewer caught what the author believed was done.
- **fix:** `v?.toString()`; verified `flutter analyze` → 0 issues across all 5 touched lib files.
- **status:** accepted (fixed)

## Founder triage notes
Auto-accepted in-session per §4.2 (account-tier verdict advisory; no deferrals). Founder may override any disposition.

## What was checked and found clean (reviewer's live verification)

- **Lens 1:** extraction fidelity line-by-line — write order verbatim (markPaymentInFlight FIRST :490 → writeSubscriptionState :498 → localActivationAt :509); `_pendingPlan` timing identical; H-20 guards :672-680/:756-764; A7 reset covers web (same call shape, no new cross-account seam).
- **Lens 2:** all 3 `callFunction` sites in try/catch with e.status/e.details; web callbacks invoke no Edge Functions.
- **Lens 3:** `lib/core/services/**` = account (blast_radius.yaml:338); regression_test + behavioral_test_path obligations verified on disk; mutation 4 independently re-run by the reviewer → exactly 1/3 red, tree reverted clean.
- **Lens 4:** no credential-shaped literals (only `rzp_test_x` placeholder fixture + prose).
- **Lens 5:** both `unawaited(` sites target `handlePaymentConfirmed`, every await inside a telemetered catch.
- **Lens 6:** kill-switch predicate byte-identical to `isSyncAutoDrainDisabled`; mirror gaps filed as findings 2/3/5/6.
- **Lens 7:** boot order verified (main.dart:60 `await HiveService.instance.init()` before runApp :136); missing-Razorpay failure surfaces via callAsConstructor → catch (now telemetered) → snackbar.
- **Lens 8:** every expected literal in the 5 new tests verified against source; no test passes if the feature does nothing.
- **Edge Functions untouched:** `git diff main --stat` — zero files under `supabase/`.
