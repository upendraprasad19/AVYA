---
reviewed_at: 2026-08-10T17:05:00+05:30
staged_against: 617a340a28c9
blast_radius: catastrophic
reviewer: claude-sonnet-via-skill (fresh, context-blind)
lens_set: [union_completeness, generated_artifact_staleness, recurrence_claim_accuracy, rule24_ledger_validity, conflict_marker_residue, automerge_correctness]
findings_count: 3
verdict: accepted
---

# Code Review — 617a340a28c9 (merge of origin/main into post38-auth-fixes)

## Scope

This is a merge commit. Main's own content arrived already reviewed on main and was
explicitly OUT of scope. What was reviewed is what this merge *decides*: three
conflict resolutions, one diagnose-doc correction, and one rule-24 ledger entry.

## Finding 1 — P0 — Gate-44 number collision, hidden inside a carried-over GATE_INDEX.md

- **file:line:** `scripts/check_nested_claude_md_content.dart:1` (`// Gate: 44`, canonical, on main
  since 2026-05-20) vs `docs/audit/post38-auth-fixes.closure.yaml:277` (minted "Gate 44" for
  `scripts/check_sot_registry_citations.dart`)
- **claim:** The merge silently combined two independently-numbered gate systems. `GATE_INDEX.md`
  exists ONLY on main — this branch never had it (`git show HEAD:docs/audit/GATE_INDEX.md` → exit
  128) — so when the branch minted "Gate 44" for its new gate on 2026-08-08 there was no registry
  to check against and no mechanism that could have caught it. The merge staged main's
  `GATE_INDEX.md` byte-identical rather than regenerating it against the merged tree, so the
  collision was invisible in the diff.
- **verification:** `dart run scripts/build_gate_index.dart` →
  `Gate 44 claimed by 2 scripts: check_nested_claude_md_content.dart …; check_sot_registry_citations.dart (from docs/audit/post38-auth-fixes.closure.yaml)`
- **resolution:** FIXED, per rule 24's own instruction — *"A new gate takes NO number. The filename
  is the identity."* The new gate yields the number rather than renumbering: the mint text in the
  closure YAML no longer claims one (8 → 0 occurrences), the script's stdout labels changed
  `[Gate 44]` → `[sot-citations]` (7 sites), and its header now records WHY it is numberless.
  Main's canonical claim is untouched. Re-verified: `build_gate_index.dart` exit 0, no problems;
  `check_gate_index_fresh.dart` exit 0; `GATE_INDEX.md` regenerated and staged; the gate itself
  still PASSes (380 docs, 116 concepts); its 28 tests all pass after the rename.
- **status:** fixed

## Finding 2 — P0 — the rule-24 ledger entry was never staged; the gate that checks it reads the working tree

- **file:line:** `docs/audit/gate_test_ledger.yaml` — entry present on disk, absent from the index
- **claim:** The `mutation_proven` entry for `check_sot_registry_citations.dart` existed only in the
  working tree. `git commit` would have written a tree WITHOUT it, shipping a rule-24 violation.
  Worse, it was undetectable locally: `check_gate_test_ledger.dart` reads
  `File(path).readAsStringSync()` — the working tree — never `git show :path`, so running the gate
  by hand read the already-patched disk copy and reported PASS for content that was not staged.
- **verification:** `git show :docs/audit/gate_test_ledger.yaml | grep -c check_sot_registry_citations`
  → `0` before the fix, `1` after. `git status --porcelain` showed `AM` (staged add + unstaged edit).
- **resolution:** FIXED — file staged; staged blob now contains the entry.
- **note, because this is the batch's recurring class:** I ran that gate myself and read its PASS as
  proof. It was proof about the wrong input set — the fifth instance in this batch of a check
  reading something adjacent to what it means (the others: a smoke set that omitted the only status
  the function can return; an archive naming a sha whose tree lacks its payload; a lock check
  reading the primary's path instead of this worktree's; `git merge-tree` in a form that emits no
  conflict markers). The gate is not wrong to read the working tree — that is right for pre-commit,
  which runs before the index is final — but "I ran the gate and it passed" is not the same claim as
  "the committed tree satisfies the gate", and I conflated them.
- **status:** fixed

## Finding 3 — P2 — trailing blank line at EOF in two closure YAMLs

- **file:line:** `docs/audit/ci-speedup.closure.yaml:79`, `docs/audit/gate-registry.closure.yaml:94`
- **claim:** `git diff --cached --check` reports "new blank line at EOF" for both.
- **verification:** `git cat-file blob origin/main:<path> | tail -c 3 | od -c` → `" \n \n` for BOTH.
  The blank lines are **already on main**; they surface in `--check` only because this branch never
  had these files, so the merge presents them as additions.
- **resolution:** NOT changed. They are pre-existing main content, not introduced by this merge.
  Editing main's files inside a merge commit that exists to resolve unrelated conflicts would be
  scope creep and a future conflict source.
- **status:** false_alarm (correctly flagged, but not attributable to this change)

## Lenses that came back clean, with evidence

- **Union completeness** (`docs/audit/open_issues.md`): headings from `git show HEAD:…` (52) and
  `git show MERGE_HEAD:…` (47) → merged 53. `comm -23` and `comm -13` between union(branch, main)
  and the merged result are BOTH empty — nothing dropped, nothing invented. No duplicate `OI-NN`
  (`sort | uniq -d` empty). OI-91's stale branch draft is correctly superseded by main's newer
  CLOSED entry, which quotes the old text verbatim.
- **Generated-artifact staleness**: `build_oi_index.dart` → 39 open; `build_bug_index.dart` → 380
  bugs; `git diff --stat` on both afterwards → empty. The committed artifacts are byte-identical to
  a fresh regeneration, which is the only meaningful check for a generated file.
- **Recurrence-claim accuracy** (a7c3f9's rewritten `recurrence:`): verified against the source, not
  the doc's word. `.claude/deploy_via_api.js` really does record three prior instances
  (2026-07-27 notif-prefs, 2026-07-28 OI-47, 2026-08-10 weekly-recap-ready), the source itself
  labels the third "THIRD instance", and the comment quoted in the doc is verbatim.
  `validate_diagnose_doc.dart` → OK.
- **Rule-24 entry validity** (content, as distinct from staging): exactly one state
  (`mutation_proven: true`); `test_path` exists, is staged, names the gate twice, and contains
  `expect(r.exitCode, 1, …)` — an accepted red-path form per `gate_test_ledger_lib.dart`.
- **Conflict-marker residue**: `git grep -n -E "^<{7} |^>{7} "` → no matches repo-wide.
- **Auto-merge correctness** (`.claude/deploy_via_api.js`): both sides' additions survive —
  `'log-client-error': [400, 401]` and `'weekly-recap-ready': [401]` — and
  `node --check .claude/deploy_via_api.js` → exit 0.

## Triage

All three findings resolved or dispositioned. Both P0s were real, both are fixed, and both were
found by the review rather than by any gate — the local pre-commit could not have caught either
(the installed hook predates the gate-index block, and the ledger gate reads the working tree).

**verdict: accepted**
