# gate-integrity — design spec

**Branch:** `gate-integrity` (base `8ffe28fb`, 2026-09-19)
**Batch:** A of the sequence A → B (OI-204 delta sync) → C (OI-154 profile tombstone). Founder-approved 2026-09-19.
**Units:** OI-220 (contract-sweep gate) · OI-155 (six unwired gates + prose allowlist) · OI-195 (Gate 42 never resolves cited paths) · OI-181 (absent plan-review record invisible at merge time).
**Blast radius:** `platform` for every unit (classified live with `dart run scripts/blast_radius_from_diff.dart -`; the `scripts/pre-push.sh`, `scripts/safe_merge.sh` and `scripts/check_*.dart` globs are individually pinned platform in `docs/blast_radius.yaml`). CLAUDE.md riders make the batch L-tier per §4.12.6 — same ×2 review + B-pass platform already demands.
**Closure ledger:** `docs/audit/gate-integrity.closure.yaml` (§4.2, ≥4 units).

## 0. Why these four together, and why now

- Zero file overlap between the four units (verified: OI-220 = new `scripts/contract_sweep{,_lib}.dart` + `scripts/pre-push.sh`; OI-155 = `scripts/check_gate_scripts_wired.dart` + `scripts/gate_scripts_wired_lib.dart` + `scripts/pre-commit.sh:331,337` + `.github/workflows/test.yml` skip block + `.claude/commands/build-apk.md`; OI-195 = `scripts/check_sot_behavioral_test_paths.dart`; OI-181 = `scripts/safe_merge.sh` + `test/scripts/safe_merge_test.dart`). One review cycle instead of four.
- OI-181 is live: `0768a0ce` (Merge branch 'aab-versioncode-bump-44', 2026-09-18) landed without `docs/plan-reviews/aab-versioncode-bump-44.md` (`git cat-file -e 0768a0ce:docs/plan-reviews/aab-versioncode-bump-44.md` → 128) and CI's keystone job went red; `8ffe28fb` repaired it. Third instance (2026-08-30, 2026-09-10, 2026-09-18).
- OI-220 pays for itself on batches B and C, which rewrite the most contract-tested subsystem (sync): the retro that spawned it measured ~2h lost in one batch to a contract regression first seen at pre-push, twice.
- OI-195's `existsSync` makes OI-220's registry arm (which selects tests via `behavioral_test_path:`) trustworthy from day one.

## 1. Verified integration facts (do not re-derive)

Every line number below was read from the worktree at `8ffe28fb` by this session (fork research + direct `Read` spot-checks), not copied from an OI entry.

### Enumeration & naming
- `scripts/pre-commit.sh:324` `for GATE in scripts/check_*.dart` and `.github/workflows/test.yml:234` enumerate every `check_*.dart`. A `check_contract_sweep.dart` would spawn `flutter test` at every commit and in CI's gate loop unless case-skipped in BOTH plus prose-allowlisted — the exact OI-155 mechanism. **The sweep is therefore `scripts/contract_sweep.dart`, not `check_*`.**
- `scripts/gate_test_ledger_lib.dart:135-139` builds `gatesOnDisk` from `check_*.dart` only; `:270-276` flags any ledger key not in that set as *"has a ledger entry but no scripts/<gate> on disk"*. A `contract_sweep.dart:` ledger entry would therefore FAIL Gate `check_gate_test_ledger`. Rule 24 is scoped by its own text to `check_*.dart` gates. **The sweep is mutation-proven per rule 21 instead, recorded in the plan-review record and a CLAUDE.md §7 row** — the same trust model as `safe_push.sh`, `retire_worktree.dart` and `batch_close_hook.dart`, none of which hold ledger entries.
- `check_gate_scripts_wired.dart:156-161` (Gate 33) covers `check_*`, `:199-206` covers `validate_*`/`audit_*`. `contract_sweep.dart` matches neither prefix and is outside Gate 33's contract.

### pre-push
- `scripts/pre-push.sh:91-93` wraps `flutter` with `env -u GIT_DIR -u GIT_WORK_TREE -u GIT_INDEX_FILE`; `:96` `cat > /dev/null` consumes the ref-update stdin (the sweep cannot read the push range from stdin; it computes `origin/main..HEAD` itself, matching `:157`); `:111-112` unconditional `flutter analyze --no-fatal-infos`; `:114` begins the full-suite block; `:139-144` `run_full_suite()` runs `TZ=Asia/Kolkata flutter test test/ --exclude-tags golden`; `:147` `PRE_PUSH_FULL` early return; `:152` origin/main fail-safe; `:157` `RANGE_FILES=$(git diff origin/main..HEAD --name-only …)`; `:167-170` tier extraction via `grep -oE 'Blast-radius: (…)' | tail -1 | awk '{print $2}'`; `:172` feature-tier skip. **Insertion point for the sweep: between `:112` and `:114`** — above every `exit 0` path, so it runs for ALL tiers.
- `test/scripts/pre_push_matches_ci_invocation_test.dart:27` pins the FIRST non-comment, non-`echo` line matching `^(VAR=x )*flutter\s+test` in pre-push.sh to CI's invocation. **The wiring line must be a `"$DART_BIN" run scripts/contract_sweep.dart …` call; the Dart runner owns the `flutter` spawn.** `test/contracts/hook_gate_placement_test.dart` + `test/scripts/pre_push_analyze_always_e2e_test.dart` pin analyze-above-everything; the sweep sits BELOW analyze.
- `DART_BIN` is resolved at `:60-84` via `. scripts/_dart_bin.sh` → `resolve_dart_bin()`, fallback `dart`.

### Registry (`docs/sot_registry.yaml`)
- 140 concepts (`grep -cE '^  - concept:'`), 137 `behavioral_test_path:` lines, 135 distinct values, **0 missing** on disk today (positive control: an appended fake path reports 1). 17 `presence_only: true` lines (+1 `presence_only: false` at `:1820`). Gate 42's own tally prints "7 carry presence_only" because `flushCurrent()` (`check_sot_behavioral_test_paths.dart:76-86`) classifies a concept holding BOTH fields as behavioral — CLAUDE.md rule 21's "6 entries carry it today" is stale on both counts.
- Key-shape quirks: `:826 behavioral_test_path_cqrs:` (a sibling key, second path for one concept); `:4219` 8-space-indented `behavioral_test_path:`; `:68` value is a `.sql` (`test/sql/onconflict_live_arbiter.sql`); trailing `# <id> — …` comments on values (`:40,:66,:68`) — comment-strip before resolving. All 137 are scalars, none lists. Justification prose lives in trailing `# …` comments on the `presence_only: true` line (16 of 17) and one `presence_only_reason: |` block (`:6081-6090`). Repo-shaped paths in that prose today: 4, all exist.
- No pure YAML loader exists; 13 scripts each block-walk the file. The sweep and Gate 42 both reuse the `check_sot_behavioral_test_paths.dart:66-113` block-walk shape (`^  - concept:\s*(\S+)` opens a block; `^\s+behavioral_test_path\s*:\s*(.*)` and `^\s+file:\s*(\S+)` inside it).
- Positive control for the sweep's registry arm: concept `phase_adherence_rate` (`:8359`) lists `lib/core/services/workout_schedule_read_service.dart` and `behavioral_test_path: test/contracts/phase_adherence_rate_test.dart` — exactly the retro's regression.
- Content-reference arm: `git grep -l "workout_schedule_read_service.dart" -- test/` → 41 files incl. `phase_adherence_rate_test.dart` (matched via its `package:` import, so importers are caught, not just `File('lib/…')` readers). Basename-with-extension (`sync_workout.dart` → 23) is tighter than the stem (`sync_workout` → 25) and sufficient.

### Gate 33 (`check_gate_scripts_wired.dart`)
- `_allowList` at `:37-82` is `Map<String, String>` — gate name → free prose. `:180-183` infers wiring as `content.contains(script) || (dynamic && !caseSkips.contains(script))` — a COMMENT mention reads as wired. `--warn-only` at `:137`, but BOTH loops invoke every gate with NO arguments (`pre-commit.sh:366`, `test.yml:264`), so any warn-only mode must be a script DEFAULT, never a flag the loop passes. Ledger: `grandfathered: 2026-08-10` (`gate_test_ledger.yaml:343`); `gate_wiring_args_required_test.dart` tests `extractCaseSkips` + allowlist presence only — no red-path assertion exists.
- **The six, re-derived mechanically** (grep over `pre-commit.sh`, `pre-push.sh`, `commit-msg.sh`, `pre-merge-commit.sh`, `test.yml`, `build-apk.md`, excluding skip-list and comment lines; positive control `check_apk_size_within_bounds` → `build-apk.md:381`): `check_unawaited_has_error_sink`, `check_migrations_live`, `check_onconflict_live_arbiter`, `check_two_user_cross_account`, `check_snapshot_contract`, `check_test_runtime_budget`. Skip blocks: `pre-commit.sh:327-341` (`:331` unawaited, `:337` snapshot_contract), `test.yml:236-249`.
- Per gate: `unawaited_has_error_sink` — pure `lib/` scan, advisory exit 0 by default (ran: "ADVISORY — 107 unawaited(...) without nearby error sink", exit 0). `snapshot_contract` — runs clean today ("57 keys checked, 4 reader citations checked. OK", exit 0); its allowlist reason ("1 reader-contract violation tracked separately") is STALE. `migrations_live` — Management-API GET, PAT from `supabase/.supabase/supabase access token.txt`, exits 0 SKIP when the file is absent (`:45-55`; absent in every worktree — `new-worktree.sh` copies only `.env`); its header promises "/build-apk Gate 14b" and `grep -ic 14b .claude/commands/build-apk.md` → 0; build-apk Gate 14 (`:231-235`) runs `check_migrations_applied.dart`, a different script. `onconflict_live_arbiter` — live SQL in a ROLLBACK txn via Management API `/database/query`; **403s today** ("Your account does not have the necessary privileges", OI-165 `open_issues.md:3325`, blocked on which token the runner needs). `two_user_cross_account` — 30-line wrapper that `Process.run`s the arbiter with `--sql test/sql/two_user_cross_account.sql`; inherits the 403; spawns bare `'dart'`. `test_runtime_budget` — standalone mode spawns the FULL `flutter test --reporter json`; OI-101 owns its re-arm-or-delete decision (founder scope call).
- The other 9 allowlist entries DO execute somewhere: apk_size `build-apk.md:381`; apk_release_signed `:389`; razorpay_key_flavor `:242`; plan_review_record_exists `test.yml:336` (dedicated job; ALSO in test.yml's case-skip — correct, it must not run in the shallow loop); regression_catalog `pre-commit.sh:400` (merge only); no_deferral_euphemism `pre-commit.sh:303`; closes_oi_cited `commit-msg.sh:74`; hooks_installed runs in pre-commit's dynamic loop (absent from pre-commit's case-skip, present in test.yml's at `:239`). `check_skipped_discipline_budget` is case-skipped in pre-commit, not allowlisted, executes at `pre-commit.sh:292` + CI loop.

### Gate 42 (`check_sot_behavioral_test_paths.dart`)
- `grep -n "existsSync\|File(" scripts/check_sot_behavioral_test_paths.dart` → only `:51-52` (the registry itself). `:103-114` is the only value validation (non-empty, not tbd/todo, not `""`/`''`). `:48-49` `warnOnly = args.contains('--warn-only')`; `:128-129` `problems = [...staleRequired, ...missing]`; `:166-182` exits `strict ? 1 : 0`. Ledger `grandfathered: 2026-08-10` (`gate_test_ledger.yaml:486-487`); NO dedicated test exists (only a parser-fixture mention in `gate_index_lib_test.dart:157,160`). Wired by the glob loop at `pre-commit.sh:324`.
- Pattern to copy: `check_sot_registry_parity.dart:105,151-155` (`File('$projectRoot/$relPath').existsSync()` → `errors.add('[file-missing] $relPath')`). `check_sot_registry_citations.dart` has NO repo-path regex (`sot_citation_lib.dart:107` matches identifier-shaped names) — the path regex is written fresh.
- Ledger promotion is allowed: `gate_test_ledger_lib.dart:205-262`'s `mutation_proven` branch never consults the grandfather set; precedent `check_cron_registry.dart` ("PROMOTED out of the grandfathered set 2026-08-20").
- Subprocess-fixture test precedent: `test/scripts/claude_md_citations_letter_suffix_test.dart:29-66`. ⚠ Spawn dart via the `dartBinOf()` helper at `test/scripts/cron_registry_snapshot_gate_test.dart:33-57`, **never `Platform.resolvedExecutable`** — under `flutter test` that is `flutter_tester` and the spawn HANGS (documented there and at `oi_numbering_lib_test.dart:284`).

### safe_merge (`scripts/safe_merge.sh`)
- `:205-208` compute `_REC_SLUG` (shell re-implementation of `plan_review_record_lib.dart:140-145` `recordSlug`: strip `origin/`, `/`→`-`), `_REC`, and `_REC_CONTENT="$(git show "refs/heads/${BRANCH}:${_REC}" 2>/dev/null || true)"`; the ENTIRE existing precheck is inside `if [ -n "${_REC_CONTENT:-}" ]` (`:208-231`), so an absent record produces zero output. **No tier computation exists anywhere in the script** (0 hits for `blast_radius`). Insertion point: after the closing `fi` at `:231`, before `:234` `echo "[safe_merge] main is caught up …"`; in scope: `BRANCH`, `REPO_ROOT` (cwd is `$REPO_ROOT` from `:73`), `_REC`, `_REC_SLUG`, `_REC_CONTENT`.
- The predicate to mirror is the KEYSTONE GATE's, not the hook's: `git_safety_hook.dart:204-228` merely re-runs `check_plan_review_record_exists.dart` at push time. Gate: tier = max over `git -c core.quotePath=false diff --no-renames --name-only sha^1..sha` (`:309-315`) through `docs/blast_radius.yaml` globs + SECURITY DEFINER content rule (`:256-293`); **record required iff tier ≥ account** (`:617-620`); absent record → fail (`:790-796`); `bpass` ≥ platform (`:833`), `hermes` catastrophic (`:836`).
- Pre-merge there is no merge commit, so the diff is THREE-dot (`main...refs/heads/$BRANCH`, merge-base) — two-dot `main..BRANCH` would report main-only changes as reversals and over-classify a branch cut from an older main. The gate's exemptions (dependabot manifest-only, "bare version bump" at `plan_review_record_lib.dart:215+`) are NOT mirrored: `0768a0ce` was a version bump and the gate still failed it; in an advisory a false positive costs a glance, a false negative costs a red main.
- `test/scripts/safe_merge_test.dart`: `@Timeout(Duration(minutes: 5))` + `library;` (`:20-21`); **12 tests** (CLAUDE.md §7's "(3)" is stale); fixture = bare remote + `primary` clone; `copyScripts` copies ONLY `safe_merge.sh` + `_git_lock.sh` (`:52-57`); invokes `sh scripts/safe_merge.sh <branch>` with `workingDirectory: primary`, `GIT_*` scrubbed, `runInShell: true` (`:33-42`); warnings asserted as substrings of `'${r.stdout}${r.stderr}'` with `exitCode == 0`; `writeReviewPairOnBranch` (`:278-298`) commits the record ON THE BRANCH. **`:426-435` pins SILENCE for a no-record FEATURE-tier branch** (`isNot(contains('unwinding this merge'))`) — with no classifier in that fixture `_PRE_TIER` stays empty and the new block stays silent, so the test remains a true mirror.
- Classifier probe: `blast_radius_from_diff.dart` + `blast_radius_content_rules_lib.dart` + `docs/blast_radius.yaml` copied into a pubspec-less temp dir classify `supabase/migrations/900_x.sql` as `platform` in ~872 ms via the SDK exe.

## 2. Decisions

| # | Decision | Why |
|---|---|---|
| D1 | `scripts/contract_sweep.dart` (runner) + `scripts/contract_sweep_lib.dart` (pure selection). NOT `check_*`. No `gate_test_ledger.yaml` entry. | Enumeration + ledger facts in §1. Mutation-proven per rule 21; results recorded in the plan-review record + CLAUDE.md §7 row. |
| D2 | Range = `origin/main..HEAD` (two-dot, matches `pre-push.sh:157`). Fail-safe to the WHOLE `test/contracts/` subset when: `origin/main` unresolvable, `git diff` errors, registry unreadable, `git grep` errors. | OI-220 + the #47 lesson: uncertainty must not look like a clean sweep. Two-dot over-selects when local main is behind — harmless (more tests). |
| D3 | Three unioned arms. (a) registry: changed path ∈ any `file:` of a concept → its `behavioral_test_path:` and `behavioral_test_path_*:` values (comment-stripped). (b) content-reference: for every changed NON-`test/` file, key = basename-with-extension, except ubiquitous basenames (`index.ts`, `index.dart`, `main.dart`, `mod.ts`, `CLAUDE.md`, `README.md`, `pubspec.yaml`) which use `<parent>/<basename>`; `git grep -l -F <key> -- test/`. (c) changed `test/**_test.dart` files. Only `*_test.dart` paths are run; non-Dart selections (e.g. a `.sql` from (a)) are listed as `skipped (not a dart test)`. | §1 registry + content-reference measurements. `index.ts` alone would match every EF test. |
| D4 | Spawn: `flutter test <files…> --exclude-tags golden`, env `TZ=Asia/Kolkata`, `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` removed, `runInShell: true` (shape of `check_test_runtime_budget.dart:59-62`). Flags: `--warn-only` (exit 0 always, after printing the verdict), `--dry-run` (print selection, no spawn, exit 0), `--range <a>..<b>`, `--flutter-bin <path>` (test injection; documented as test-only). Exit: `warnOnly ? 0 : flutter's exit code`; internal error → fallback selection, and in warn-only → 0. Empty selection with ≥1 changed non-doc file → prints the unmapped file list, exit 0 (baseline signal for OI-220's recall measurement). | Matches CI's invocation (`test.yml:112`) and pre-push's env scrub. |
| D5 | pre-push wiring between `:112` and `:114`: `"$DART_BIN" run scripts/contract_sweep.dart --warn-only \|\| true` (with a 6-line comment). The hard-fail flip removes BOTH `--warn-only` and `\|\| true`. | `pre_push_matches_ci_invocation_test.dart:27` forbids a literal `flutter test` there; `\|\| true` keeps a sweep CRASH from wedging a push during baseline. |
| D6 | OI-220 stays OPEN after this batch, narrowed to the flip: "Blocked on: the `--warn-only` → hard-fail flip after ONE clean batch (B = OI-204); a mis-selection during baseline is a mapping bug fixed before the flip." No new OI. | §4.11.2 + OI-220's own ratified criterion; precedent OI-190 tracks a warn-only gate's flip on the board. |
| D7 | Gate 33's `_allowList` becomes `Map<String, List<GateRunner>>` with typed runners: `file(path)` (the file names the gate on a non-comment, non-case-skip line), `loop(preCommit\|ci)` (gate absent from that file's case-skip set), `manual(OI-NNN)` (the OI heading exists on `docs/audit/open_issues.md` with `- **Status**: OPEN`). Checked STRICTLY from the first commit — no warn-only window. | All entries pass at the moment of the commit (the six are fixed in the same commit), which is OI-155's own "land together" option; the loops pass no flags so a warn-only default would need its own flip. A `manual:` OI that later closes makes Gate 33 red repo-wide — that is the forcing function, same shape as `closes-oi`. |
| D8 | The six: `unawaited_has_error_sink` + `snapshot_contract` → DELETED from `_allowList` and from BOTH case-skip blocks (they run in both loops; both exit 0 today). `migrations_live` → `file('.claude/commands/build-apk.md')` with a real **Gate 14b** section added there (mirroring Gate 14 at `:231-235`; fail-open on absent token is its own design). `onconflict_live_arbiter` + `two_user_cross_account` → `manual('OI-165')`. `test_runtime_budget` → `manual('OI-101')`. The 9 already-running entries convert to their real runner (apk_size/apk_release_signed/razorpay → `file(build-apk.md)`; plan_review_record_exists → `file(test.yml)`; regression_catalog + no_deferral_euphemism → `file(pre-commit.sh)`; closes_oi_cited → `file(commit-msg.sh)`; hooks_installed → `loop(preCommit)`). | §1 per-gate facts. `manual:` is not a deferral: OI-165 (token privileges) and OI-101 (re-arm or delete) are founder-blocked on the board already; the allowlist now MACHINE-CHECKS that state instead of asserting a runner that does not exist. |
| D9 | `_explicitAllowList` (validate_*/audit_*) is untouched. | Different contract: "on-demand, invoked per-artifact by a skill" is true by construction; OI-155's finding is `_allowList`'s FALSE "runs in X" claims. Stated so the ×2 review can dispute it. |
| D10 | Gate 33's inference at `:180-183` uses `mentionsOnLiveLine` (non-comment, non-case-skip line) instead of `contains`. | The fork's open question 3; a comment-only mention must not read as wired — same class as the allowlist prose. |
| D11 | Gate 42: strip `\s+#.*$` from every `behavioral_test_path(?:_[a-z0-9_]+)?:` value and require `File('${Directory.current.path}/$path').existsSync()`; scan `presence_only: true` trailing comments AND `presence_only_reason: \|` block bodies with `\b(test\|docs\|scripts\|supabase\|lib)/[A-Za-z0-9_./-]+` and require existence; both sinks feed `problems` (strict → exit 1, `--warn-only` → exit 0). Existence is the whole contract (the `.sql` at `:68` passes). Fix the tally so "N carry presence_only" counts every `presence_only: true` line. | OI-195 + §1 registry quirks. Scoped to the two `presence_only` surfaces — NOT `description:` prose. |
| D12 | safe_merge absent-record precheck: advisory, fires iff `_REC_CONTENT` empty AND three-dot tier ≥ account via `blast_radius_from_diff.dart -` (same `grep -oE` extraction idiom as `pre-push.sh:167-170`); every failure path silent (no classifier, no yaml, no dart → silent). Message contains `NO plan-review record` and `unwinding this merge`. | §1 safe_merge facts. |
| D13 | Diagnose-docs (full template, `touched_layers_checked`) for OI-155, OI-195, OI-181 — each is a gate that did not do what it claimed. OI-220 is `feat(gates):`, no diagnose-doc. §4.1.5 bug-history grep is part of each fix task. | Rule 22. |
| D14 | Subagent execution: four forks with `isolation: worktree`, each edits + tests + mutation-proves + commits on its own branch via `safe_commit.sh`; NO push, NO merge. Integration by cherry-pick onto `gate-integrity`. The shared files (CLAUDE.md, `gate_test_ledger.yaml`, closure YAML, plan-review record, OI board) have ONE writer: the coordinator, after all four land. | OI-220 rider 2 (execution-mode decision at batch START). OI-86 is a Hive-box collision and does not reach `test/scripts/` subprocess tests. Two agents editing one YAML with the Edit tool can lose an update. |
| D15 | Riders land at integration: CLAUDE.md §4.1.5 item 6 (value-semantics grep, pre-work step); §4.12.7 (execution-mode decision at batch start); §0 pre-push row + §7 row for the sweep; rule 21 Gate 42 wording + re-derived `presence_only` count; §7 safe_merge row (absent-record check + re-run test count); `docs/playbook/common-pitfalls.md` section for the riverpod-3 widget-harness pitfalls, written from `test/widgets/compass_redesign_test.dart`'s CODE (header `:1-20` documents GoogleFonts warmup only; `runAsync` is an inline comment at `:647-650`, empty-box seeding at `:299-300`; `UncontrolledProviderScope` has 0 comment hits — describe it from usage or drop it and say so). `common-pitfalls.md` is sections (`## <title> (YYYY-MM-DD)`), not a table. | OI-220 riders; conversion-on-touch for the counts. |

## 3. Out of scope, stated
- OI-165 (which token the live-SQL runner needs) and OI-101 (runtime-budget re-arm/delete) — founder-blocked; D8 makes their state machine-visible.
- Whether `safe_merge.sh` should also preview a `branch:` field mismatch (`check_plan_review_record_exists.dart:813`) — a fourth shape OI-181 does not name.
- `test/scripts/claude_md_citations_letter_suffix_test.dart` spawning bare `'dart'` and its throwing `tearDown` — pre-existing.
- `check_two_user_cross_account.dart` spawning bare `'dart'` — pre-existing; unreachable until OI-165.
