# gate-integrity — design spec (v2, hardened by plan-review round 1)

**Branch:** `gate-integrity` (base `8ffe28fb`; spec+plan v1 committed `d271c898`, 2026-09-19)
**Batch:** A of the sequence A → B (OI-204 delta sync) → C (OI-154 profile tombstone). Founder-approved 2026-09-19.
**Units:** OI-220 (contract-sweep gate) · OI-155 (six unwired gates + prose allowlist) · OI-195 (Gate 42 never resolves cited paths) · OI-181 (absent plan-review record invisible at merge time).
**Blast radius:** `platform` for the BATCH — via the pinned files it edits (`scripts/pre-push.sh`, `.github/workflows/test.yml`, `scripts/check_gate_scripts_wired.dart`, `scripts/safe_merge.sh`). ⚠ Corrected by round 1: the individual script files are NOT pinned — `scripts/check_sot_behavioral_test_paths.dart`, `scripts/gate_scripts_wired_lib.dart` and the new `scripts/contract_sweep{,_lib}.dart` all classify `feature` under `docs/blast_radius.yaml:332` (`scripts/** → feature`; verified with the classifier 2026-09-19). D16 pins them. CLAUDE.md riders make the batch L-tier per §4.12.6 — same ×2 review + B-pass platform already demands.
**Closure ledger:** `docs/audit/gate-integrity.closure.yaml` (§4.2, ≥4 units).

## 0. Why these four together, and why now

- Zero file overlap between the four units (OI-220 = new `scripts/contract_sweep{,_lib}.dart` + `scripts/pre-push.sh` + `test/scripts/pre_push_analyze_always_e2e_test.dart` (conversion-on-touch) + a `test/contracts/` pin; OI-155 = `scripts/check_gate_scripts_wired.dart` + `scripts/gate_scripts_wired_lib.dart` + `scripts/pre-commit.sh` + `.github/workflows/test.yml` skip blocks + the OI board (one minted entry); OI-195 = `scripts/check_sot_behavioral_test_paths.dart`; OI-181 = `scripts/safe_merge.sh` + `test/scripts/safe_merge_test.dart`). One review cycle instead of four.
- OI-181 is live: `0768a0ce` (Merge branch 'aab-versioncode-bump-44', 2026-09-18) landed without `docs/plan-reviews/aab-versioncode-bump-44.md` (`git cat-file -e 0768a0ce:docs/plan-reviews/aab-versioncode-bump-44.md` → 128) and CI's keystone job went red; `8ffe28fb` repaired it. Third instance (2026-08-30, 2026-09-10, 2026-09-18).
- OI-220 pays for itself on batches B and C, which rewrite the most contract-tested subsystem (sync): the retro that spawned it measured ~2h lost in one batch to a contract regression first seen at pre-push, twice.
- OI-195's `existsSync` makes OI-220's registry arm (which selects tests via `behavioral_test_path:`) trustworthy from day one.

## 1. Verified integration facts (do not re-derive)

Every line number below was read from the worktree by this session or re-measured by a round-1 reviewer with the command shown; corrections from round 1 are marked ⚠.

### Enumeration & naming
- `scripts/pre-commit.sh:324` `for GATE in scripts/check_*.dart` and `.github/workflows/test.yml:234` enumerate every `check_*.dart`. A `check_contract_sweep.dart` would spawn `flutter test` at every commit and in CI's gate loop unless case-skipped in BOTH plus prose-allowlisted — the exact OI-155 mechanism. **The sweep is therefore `scripts/contract_sweep.dart`, not `check_*`.**
- `scripts/gate_test_ledger_lib.dart:135-139` builds `gatesOnDisk` from `check_*.dart` only; `:270-276` flags any ledger key not in that set as *"has a ledger entry but no scripts/<gate> on disk"*. A `contract_sweep.dart:` ledger entry would FAIL `check_gate_test_ledger`. Rule 24 is scoped by its own text to `check_*.dart` gates. **The sweep is mutation-proven per rule 21 instead, recorded in the plan-review record and a CLAUDE.md §7 row, and its wiring is PINNED by a `test/contracts/` test (D5)** — the same trust model as `safe_push.sh`, `retire_worktree.dart` and `batch_close_hook.dart`.
- `check_gate_scripts_wired.dart:156-161` (Gate 33) covers `check_*`, `:199-206` covers `validate_*`/`audit_*`. `contract_sweep.dart` matches neither prefix and is outside Gate 33's contract.

### pre-push
- `scripts/pre-push.sh:91-93` wraps `flutter` with `env -u GIT_DIR -u GIT_WORK_TREE -u GIT_INDEX_FILE`; `:96` `cat > /dev/null` consumes the ref-update stdin (the sweep computes its own range); `:111-112` unconditional `flutter analyze --no-fatal-infos`; `:114` begins the full-suite block; `:139-144` `run_full_suite()` runs `TZ=Asia/Kolkata flutter test test/ --exclude-tags golden`; `:147` `PRE_PUSH_FULL` early return; `:152` origin/main fail-safe; `:157` `RANGE_FILES=$(git diff origin/main..HEAD --name-only …)`; `:167-170` tier extraction; `:172` feature-tier skip. **Insertion point for the sweep: between `:112` and `:114`** — above every `exit 0` path, so it runs for ALL tiers.
- `test/scripts/pre_push_matches_ci_invocation_test.dart:27` pins the FIRST non-comment, non-`echo` line matching `^(VAR=x )*flutter\s+test` in pre-push.sh to CI's invocation; round 1 confirmed the wiring line `"$DART_BIN" run scripts/contract_sweep.dart --warn-only || true` does not match that regex. `hook_gate_placement_test` + `dart_bin_resolver_test` survive the insertion.
- ⚠ **Recursion (round-1 P0).** `test/scripts/pre_push_analyze_always_e2e_test.dart` runs the REAL `scripts/pre-push.sh` in the REAL worktree with a POSIX-form stub `flutter` first on PATH (`:104-122`; env copied from `Platform.environment` at `:57`; its `_cleanEnv()` at `:56-65` strips only `GIT_*/GITHUB_*/PUSH_BEFORE/PRE_PUSH_FULL`, so a `CONTRACT_SWEEP_*` variable survives into the hook). It is the ONLY test that executes `pre-push.sh` with the hook installed (`safe_push_test` installs pre-RECEIVE hooks on the fixture remote; no global `core.hooksPath`). The hermetic-env gate is `test/contracts/gate_e2e_env_hermetic_test.dart` (`_helpers` const at `:34-64`; checks at `:80-88`). Git Bash finds the stub; **the sweep's Dart `Process.runSync('flutter', …, runInShell: true)` goes through cmd.exe, which cannot execute an extensionless stub and runs the REAL flutter** (probe: `where flutter` → stub, `flutter --version` via Dart → "Flutter 3.41.4"). Any branch whose range contains `scripts/pre-push.sh` makes arm (b) select that very e2e (`git grep -l -F pre-push.sh -- test/` → 6 files incl. it) → nested `flutter test` → the e2e → the hook → the sweep → … `Process.runSync` blocks the isolate so the file `@Timeout` cannot fire. Linux CI is unaffected (sh finds the stub). **D4 adds a self-recursion guard and a kill switch; the e2e sets the switch.**
- `DART_BIN` is resolved at `:60-84` via `. scripts/_dart_bin.sh` → `resolve_dart_bin()`, fallback `dart`.

### Registry (`docs/sot_registry.yaml`)
- 140 concepts (`grep -cE '^  - concept:'`), 137 `behavioral_test_path:` lines (138 with the widened sibling-key regex), 135 distinct values, **0 missing** on disk (positive control: an appended fake path reports 1). 17 `presence_only: true` lines (+1 `presence_only: false` at `:1820`); 10 of the 17 ALSO carry a behavioral path, which is why Gate 42's tally prints "7 carry presence_only" (`flushCurrent()` at `check_sot_behavioral_test_paths.dart:76-86` classifies both-fields concepts as behavioral; 133 + 7 = 140). CLAUDE.md rule 21's "6 entries carry it today" is stale.
- ⚠ **`file:` shape (round-1 P1): 896 lines are `- file: <path>` list items and 176 are `- { file: <path>, line: N }` inline maps (ist_sites); ZERO bare `file:` lines** (`grep -cE '^\s+-\s+file:'` → 896; `'^\s+file:'` → 0; `'\{\s*file:'` → 176). The v1 regex `^\s+file:\s*(\S+)` matched nothing. Regex that catches both: `(?:^|[\s{-])file:\s*([^\s,}]+)`; against the real registry it selects 15 paths for `workout_schedule_read_service.dart` incl. `test/contracts/phase_adherence_rate_test.dart` — the retro's positive control (concept `phase_adherence_rate`, `:8359`).
- Key-shape quirks (⚠ line cites corrected): `:826 behavioral_test_path_cqrs:` (sibling key; that concept also has `behavioral_test_path:` at `:809`, so widening changes no verdict); `:4219` 8-space-indented key; `:6350` value is a `.sql` (`test/sql/onconflict_live_arbiter.sql`); trailing `# <id> — …` comments on values at `:3236`, `:6064`, `:6350` — comment-strip before resolving. All scalars. Justification prose: trailing `# …` on the `presence_only: true` line (16 of 17) and one `presence_only_reason: |` block at `:6082`. Repo-shaped paths in that prose today: 4, all exist.
- No pure YAML loader exists; 13 scripts each block-walk the file. `^  - concept:\s*(\S+)` opens a block.
- Content-reference arm: `git grep -l "workout_schedule_read_service.dart" -- test/` → 41 files incl. `phase_adherence_rate_test.dart` (matched via its `package:` import). Basename-with-extension (`sync_workout.dart` → 23) is tighter than the stem (→ 25). ⚠ Data contracts live under `docs/` in BOTH yaml and Markdown: `sot_registry.yaml` → 35 test files, `blast_radius.yaml` → 8, `open_issues.md` → 10, `OPEN_INDEX.md` → 5, `closed_issues.md` → 7, `docs/architecture/sync.md` → 39 — so the doc exclusion is by extension EXCEPT under `docs/audit/` and `docs/architecture/` (D3). `ward_card.dart` / `ward_dashed_border.dart` are referenced ONLY by `test/goldens/**` files, and `flutter test <golden file> --exclude-tags golden` prints `No tests ran.` and **exits 79** (measured with the real flutter). `git grep -l -F <key> -- test/` exits 0 on a match, 1 on none (also 1 for a nonexistent pathspec dir — harmless at the repo root where `test/` exists). Cost, measured with the v2 runner on the real tree: 1.9 s / 17 tests for a 15-file range; 18.5 s / 505 tests for a 366-file range.

### Gate 33 (`check_gate_scripts_wired.dart`)
- `_allowList` at `:37-82` is `Map<String, String>` — gate name → free prose. `:180-183` infers wiring as `content.contains(script) || (dynamic && !caseSkips.contains(script))` — a COMMENT or PROSE mention reads as wired. `--warn-only` at `:137`, but BOTH loops invoke every gate with NO arguments (⚠ `pre-commit.sh:368`, `test.yml:264`). Ledger: `grandfathered: 2026-08-10` (`gate_test_ledger.yaml:343`); `test/contracts/gate_wiring_args_required_test.dart` names the gate (`:11,:61,:65`) but has no red-path assertion; it slices `_allowList` to the first `};` and `test/contracts/apk_release_signed_gate_test.dart:89-91` needs the literal `'check_apk_release_signed.dart':` — keys stay quoted.
- **The six, re-derived mechanically by two independent readers** (grep across `pre-commit.sh`, `pre-push.sh`, `commit-msg.sh`, `pre-merge-commit.sh`, `test.yml`, `build-apk.md`, excluding skip-list and comment lines; control `check_apk_size_within_bounds` → `build-apk.md:381`): `check_unawaited_has_error_sink`, `check_migrations_live`, `check_onconflict_live_arbiter`, `check_two_user_cross_account`, `check_snapshot_contract`, `check_test_runtime_budget`. Skip blocks: `pre-commit.sh:327-341` (`:331` unawaited, `:337` snapshot_contract), `test.yml:236-249`. All 8 live invocation lines in the repo today carry the shape `run scripts/<gate>` (`build-apk.md:242/381/389`, `test.yml:336`, `pre-commit.sh:303/400`, `commit-msg.sh:74`) and NO prose line names a gate on a live line — so an invocation-shaped predicate has zero false negatives today.
- Per gate: `unawaited_has_error_sink` — pure `lib/` scan, advisory exit 0 ("ADVISORY — 107 unawaited(...) without nearby error sink"), 1717 ms. `snapshot_contract` — passes today ("57 keys checked, 4 reader citations checked. OK"), 1678 ms; its allowlist reason is STALE. ⚠ **`migrations_live` — FAILS on the real project** (run from the primary with the 44-byte PAT: exit 1, `local migrations: 139, live migrations: 130`, `FAIL — 125 local migration(s) NOT applied live`): its prefix matcher (own header `:27-32` admits the heuristic) matched 14/139, and `backups/applied_migrations.json` shows **75 `applier: founder`** raw-SQL applies never registered in Supabase's migrations table — it cannot pass by construction; "exits 0 SKIP" holds only when the token file is absent (every worktree — `new-worktree.sh` copies only `.env`). Its header's "/build-apk Gate 14b" exists nowhere (`grep -ic 14b build-apk.md` → 0), and build-apk's "first failure stops the build" (`build-apk.md:36`) means wiring it there would stop every full-gate build. `onconflict_live_arbiter` — live rollback-txn SQL via Management API; **403s** (OI-165, `open_issues.md:3325`). `two_user_cross_account` — 40-line wrapper spawning the arbiter; inherits the 403. `test_runtime_budget` — spawns the FULL `flutter test --reporter json`; OI-101 owns re-arm-or-delete (founder scope call).
- ⚠ The other **8** (not 9) allowlist entries execute somewhere: apk_size `build-apk.md:381`; apk_release_signed `:389`; razorpay_key_flavor `:242`; plan_review_record_exists `test.yml:336` (dedicated job; also in test.yml's case-skip — correct); regression_catalog `pre-commit.sh:400`; no_deferral_euphemism `pre-commit.sh:303`; closes_oi_cited `commit-msg.sh:74`; hooks_installed via pre-commit's dynamic loop (absent from `:328-341`, present in test.yml at `:239`). `check_skipped_discipline_budget` is case-skipped, not allowlisted, executes at `pre-commit.sh:292`.
- The board's status vocabulary is `OPEN / IN_PROGRESS / CLOSED` (`open_issues.md:22-25`); `check_closes_oi_cited.dart:83` documents "OPEN (or IN_PROGRESS) -> CLOSED". Parsers already exist: `parseBoardStatuses(String)` (`check_closes_oi_cited.dart:70`) and `mergedBoardStatuses(...)` (`oi_closure_lib.dart:73-79`, which imports the former with `show`). OI-165 (`:3325-3327`) and OI-101 (`:1499-1501`) are OPEN today. Six CLOSED entries still sit on the open board (OI-153/162/172/176/183/189) — the convention is close IN PLACE, not move; `newlyClosed` (`check_closes_oi_cited.dart:84-97`) fires only on an in-place flip.

### Gate 42 (`check_sot_behavioral_test_paths.dart`)
- `grep -n "existsSync\|File(" scripts/check_sot_behavioral_test_paths.dart` → only `:51-52` (the registry itself). ⚠ Key regexes at `:104-105`; value validation `:103-114` (non-empty, not tbd/todo, not `""`/`''`). `:48-49` `warnOnly`; `:128-129` `problems`; `:166-182` exits `strict ? 1 : 0`. Current PASS line: `[Gate 42] PASS: all 133 SoT concepts have behavioral_test_path; 7 carry presence_only: true (Deno-EF/static). Zero behavioral_test_required TODOs remain.` — note `[Gate 42 WARN] PASS:` already contains `WARN`, so a warn-only test must assert the missing path is NAMED, not that `WARN` appears. Ledger `grandfathered: 2026-08-10` (`gate_test_ledger.yaml:486-487`); NO dedicated test exists.
- Pattern to copy: `check_sot_registry_parity.dart:105,151-155`. ⚠ The ledger's accepted red-path forms (`gate_test_ledger_lib.dart:69-73`) require the literal `exitCode,\s*(1|2|…)` — a record field named `code` (as in `claude_md_citations_letter_suffix_test.dart`, which matches 0 forms) does NOT count. The new test's record field is `exitCode`.
- Ledger promotion is allowed (`gate_test_ledger_lib.dart:205-262` never consults the grandfather set; precedent `check_cron_registry.dart`). ⚠ `namesGate` (`:243-258`) requires a listed `test_path:` file to CONTAIN the gate's name.
- Spawn dart in tests via `dartBinOf()` from `test/scripts/cron_registry_snapshot_gate_test.dart:33-57` — never `Platform.resolvedExecutable` (flutter_tester; hangs).

### safe_merge (`scripts/safe_merge.sh`)
- `:205-208` compute `_REC_SLUG`, `_REC`, `_REC_CONTENT="$(git show "refs/heads/${BRANCH}:${_REC}" …)"`; the ENTIRE existing precheck is inside `if [ -n "${_REC_CONTENT:-}" ]` (`:208-231`). **No tier computation exists anywhere in the script.** Insertion point: after the closing `fi` at `:231` and its `# ----` line, before `:234`; in scope: `BRANCH`, `REPO_ROOT` (cwd from `:73`), `_REC`, `_REC_SLUG`, `_REC_CONTENT`.
- The predicate to mirror is the KEYSTONE GATE's (`git_safety_hook.dart:204-228` merely re-runs it at push time): tier = max over `git -c core.quotePath=false diff --no-renames --name-only sha^1..sha` (`:309-315`) through `docs/blast_radius.yaml` + SECURITY DEFINER content rule (`:256-293`); **record required iff tier ≥ account** (`:617-620`); absent → fail (`:790-796`); `bpass` ≥ platform (`:833`), `hermes` catastrophic (`:836`). Pre-merge: THREE-dot from `refs/heads/main` (⚠ bare `main` resolves a same-named TAG first — the existing `:400-424` test is about exactly that). Exemptions NOT mirrored (`0768a0ce` was a version bump and the gate failed it; OI-222 `:4987-4991`).
- `test/scripts/safe_merge_test.dart`: `@Timeout(Duration(minutes: 5))` + `library;` (`:20-21`); **12 tests**; ⚠ the only helper is `_run(exe, args, cwd)` (`:33-42`) — every git call is `_run('git', …)`, there is no `_git`; `copyScripts` copies ONLY `safe_merge.sh` + `_git_lock.sh` (`:52-57`); `writeReviewPairOnBranch` (`:278-298`) commits the record ON THE BRANCH; `:426-435` pins SILENCE for a no-record FEATURE-tier branch and stays green with the block present (no classifier in that fixture, and feature-tier regardless).
- Classifier + content lib + yaml copied into a pubspec-less temp dir run in ~0.7 s via the SDK exe: `900_probe.sql` / `901_main_only.sql` → platform (`blast_radius.yaml:62`), `feature-only.txt` / `feature.txt` → feature. `_dart_bin.sh`'s execution guard is `$0`-based (`:70-77`) — sourcing it from safe_merge is safe.
- ⚠ Diagnose lineage for Task 4: the precheck commit `ecc01876` cites `d4e9a2`; the INDEX grep returns `c9f4e1`. There is no "2026-08-30 precheck diagnose" by that name.

## 2. Decisions

| # | Decision | Why |
|---|---|---|
| D1 | `scripts/contract_sweep.dart` (runner) + `scripts/contract_sweep_lib.dart` (pure selection). NOT `check_*`. No `gate_test_ledger.yaml` entry. Mutation-proven per rule 21, recorded in the plan-review record + CLAUDE.md §7 row. | §1 enumeration + ledger facts. |
| D2 | Range = **three-dot** `origin/main...HEAD` with `--no-renames` (merge-base, mirrors the keystone gate; a two-dot range reports main-only additions as reversals and would select `_test.dart` files that do not exist on the branch; `--no-renames` keeps the OLD basename visible on a rename — the §4.9 extraction class). Every selected path is filtered by `existsSync`. Fail-safe to the WHOLE `test/contracts/` subset when: `origin/main` unresolvable, `git diff` errors, registry unreadable, any `git grep` errors (exit ≥ 2; exit 1 = no match), or ANY internal exception (try/catch around the body). | OI-220 + the #47 lesson; round-1 F4/F12. |
| D3 | Three unioned arms. (a) registry: changed path ∈ any `file:` (both YAML shapes) of a concept → its `behavioral_test_path:` and `behavioral_test_path_*:` values (comment-stripped). (b) content-reference: for every changed file that is neither a `test/**_test.dart` nor doc-like — where doc-like = `.md`/`.txt` EXCEPT under `docs/audit/` and `docs/architecture/`, whose Markdown files are data contracts with test readers (`open_issues.md` → 10 test files, `docs/architecture/sync.md` → 39, `OPEN_INDEX.md` → 5, `closed_issues.md` → 7; measured 2026-09-19) — so lib/, supabase/, scripts/, `docs/*.yaml`, the board, the architecture docs and `test/` helpers are all keys, while prose (`CLAUDE.md`, plans, specs, diagnose-docs, memory) is not (tests mention `CLAUDE.md` in comments; its contracts are commit-time gates). Key = basename-with-extension, except ubiquitous basenames (`index.ts`, `index.dart`, `main.dart`, `mod.ts`, `pubspec.yaml`) which use `<parent>/<basename>`; `git grep -l -F <key> -- test/`. (c) changed `test/**_test.dart`. Only `*_test.dart` paths outside `test/goldens/**` are run (pre-push never runs goldens; a golden-only spawn exits 79); other selections are reported as `skip`; selected-but-absent paths as `dropped`. ⚠ Round 1's directory→extension correction re-created the class it fixed (it dropped the board readers); round 2 caught it. | §1 measurements; round-1 F3/F9; round-2 registry F2. |
| D4 | Spawn: `flutter test <files…> --exclude-tags golden`, env = parent minus `GIT_*`, plus `TZ=Asia/Kolkata` and **`CONTRACT_SWEEP_NESTED=1`**; `runInShell: true`. **Guards, checked first:** `CONTRACT_SWEEP_SKIP=1` → print `skipped`, exit 0; `CONTRACT_SWEEP_NESTED=1` → print `skipped (nested)`, exit 0. Flags: `--warn-only` (exit 0 always, verdict printed), `--dry-run` (selection only), `--range <spec>`, `--flutter-bin <path>` (test injection). Exit 79 from flutter (`No tests ran`) is reported and treated as 0. Empty selection with ≥1 changed non-doc file → prints the `unmapped` list, exit 0 (baseline recall signal). | Round-1 P0 (recursion) + D2 fallbacks. |
| D5 | pre-push wiring between `:112` and `:114`: `"$DART_BIN" run scripts/contract_sweep.dart --warn-only \|\| true` (+ comment). The hard-fail flip removes BOTH tokens. **A `test/contracts/contract_sweep_wired_test.dart` pins that `scripts/pre-push.sh` invokes `scripts/contract_sweep.dart` on a live line BELOW the analyze line and ABOVE `run_full_suite()`** — with no ledger entry, this is what stops a "tidy" un-wiring it green (the OI-155 class). `test/scripts/pre_push_analyze_always_e2e_test.dart`'s hook env gains `CONTRACT_SWEEP_SKIP=1` (conversion-on-touch; that test is about analyze placement, not the sweep). | Round-1 F1/F11. |
| D6 | OI-220 stays OPEN after this batch, narrowed to the flip, AND its board text's "rule-24 mutation-proven ledger entry" is replaced by the actual trust model (D1 + D5). No new OI for the flip. **Cost line for the flip decision (measured by round 2 with the v2 runner on the real tree):** selection takes 1.9 s and picks 17 tests on a 15-file range; 18.5 s and 505 tests on a 366-file range (`origin/main~40...HEAD`) — near full-suite size. The sweep sits ABOVE the tier check, so on every ≥account push it is additive to `run_full_suite` (the same files run twice) and under `--warn-only \|\| true` it can never short-circuit — during the baseline batch it is added minutes for a printed verdict; after the flip a red sweep saves the full-suite run on exactly the pushes that would have failed it. The flip criterion (one clean batch, mapping bugs fixed first) stays; the baseline batch records real seconds. | §4.11.2; precedent OI-190; round-2 registry F4. |
| D7 | Gate 33's `_allowList` becomes `Map<String, List<GateRunner>>` with typed runners: `file(path)` — the file INVOKES the gate (`run scripts/<gate>` on a non-comment line; prose/comment mentions do not count); `loop(preCommit\|ci)` — the file has the dynamic loop and does NOT case-skip the gate; `manual(OI-NNN)` — the OI's status on the merged boards (`mergedBoardStatuses`, reused, never a third parser) is `OPEN` or `IN_PROGRESS`; `CLOSED`, absent, or an unreadable board all FAIL (fail closed). **Mirror:** an allowlist key with no `scripts/<key>` on disk is a violation (stale entry). Checked STRICTLY from the first commit. | Round-1 F2/F4/F5 (hooks). All entries pass at commit time, verified live (Gate 33 PASS on 97 check_*; OI-165/101 OPEN). |
| D8 | The six: `snapshot_contract` → DELETED from `_allowList` and from BOTH case-skip blocks (runs in both loops; it becomes a live ±15-line-citation blocker — accepted, it passes today). `unawaited_has_error_sink` → `loop('ci')` only: removed from `test.yml`'s skip, KEPT in `pre-commit.sh`'s skip, because pre-commit runs every gate as `>/dev/null 2>&1` (`:368`) and an advisory exit-0 gate there is a 1.7 s no-op with zero possible reader (Gate 22, `check_telemetry_pii_classification.dart`, was wired into BOTH loops in 2026-08-17 — that is the same waste, not a reason to repeat it); CI's log is its only reader and the entry says so. `migrations_live` → `manual('OI-<new>')`: **it cannot pass by construction** (§1); its only documented runner is by hand (`docs/runbooks/restore-drill.md:63`), where it fails; the fix is a founder retire-or-redesign call of OI-101's shape (recommendation: RETIRE — Gate 14 `check_migrations_applied.dart` + `backups/applied_migrations.json` already own "applied live", and the live matcher contradicts the project's own raw-SQL history). Retiring touches six sites (script, ledger entry, allowlist entry, two skip lines, the exists-assertion at `test/contracts/phase_c_oi_closures_test.dart:81-85`, the runbook line). The OI is minted in Task 2 with the measured evidence. `two_user_cross_account` likewise has a by-hand runner at `restore-drill.md:71`; both `manual:` reasons say so. `onconflict_live_arbiter` + `two_user_cross_account` → `manual('OI-165')`. `test_runtime_budget` → `manual('OI-101')`. So **4 of 6 are `manual:`**, and every `manual:` names an OPEN founder-blocked OI that the gate re-checks on every commit. The other 8 entries convert to their real runner (apk_size / apk_release_signed / razorpay → `file(build-apk.md)`; plan_review_record_exists → `file(test.yml)`; regression_catalog + no_deferral_euphemism → `file(pre-commit.sh)`; closes_oi_cited → `file(commit-msg.sh)`; hooks_installed → `loop(preCommit)`). §4.2 tension, stated: a `manual:` entry is not a fix of the gate; it is a machine-checked, self-reopening statement that the gate runs nowhere and WHY — replacing a prose claim that it ran somewhere it did not. | Round-1 F1/F11 (hooks); §1 per-gate facts. |
| D9 | `_explicitAllowList` (validate_*/audit_*) is untouched. | "On-demand, invoked per-artifact by a skill" is true by construction; OI-155's finding is `_allowList`'s FALSE "runs in X" claims. |
| D10 | Gate 33's inference at `:180-183` uses the same invocation-shaped predicate as `file()` runners instead of `contains`. | A comment/prose mention must not read as wired. |
| D11 | Gate 42: strip `\s+#.*$` from every `behavioral_test_path(?:_[a-z0-9_]+)?:` value and require `File('${Directory.current.path}/$path').existsSync()`; scan `presence_only: true` trailing comments AND `presence_only_reason: \|` block bodies with `\b(test\|docs\|scripts\|supabase\|lib)/[A-Za-z0-9_./-]+` (trailing `.,;)` trimmed) and require existence; ONE shared `_missingOnDisk(path)` helper feeds both sinks into `problems` (strict → exit 1, `--warn-only` → exit 0). Existence is the whole contract. Tally counts every `presence_only: true` line and says how many also carry a behavioral path. | OI-195 + §1 quirks; round-1 F8. |
| D12 | safe_merge absent-record precheck: advisory; fires iff `_REC_CONTENT` empty AND three-dot tier ≥ account via `refs/heads/main...refs/heads/$BRANCH` classified by `blast_radius_from_diff.dart -` (same `grep -oE` extraction idiom as `pre-push.sh:167-170`); a NON-empty path list that classifies to NOTHING prints one `NOTE:` line (bad news vs no news); every other failure path silent. Message contains `NO plan-review record` and `unwinding this merge`. | §1 safe_merge facts; round-1 F9/F10f (hooks). |
| D13 | Diagnose-docs (full template) for OI-155, OI-195, OI-181. OI-220 is `feat(gates):`. §4.1.5 grep is part of each fix task; Task 4 cites `c9f4e1` only (`d4e9a2` is the profile-name restore race that commit `ecc01876` reused as a batch id — a false link; the validator does not check `related_bugs`, so nothing mechanical catches it). | Rule 22; round-2 hooks F6. |
| D14 | Subagent execution: four forks with `isolation: worktree`, each edits + tests + mutation-proves + commits on its own branch via `safe_commit.sh`; NO push, NO merge. Integration by cherry-pick onto `gate-integrity` — a CLEAN pick runs NO `pre-commit`/`commit-msg` hook (round 2 probed it: only `prepare-commit-msg` + `post-commit` fire; the `CHERRY_PICK_HEAD` exemptions at `check_commit_from_worktree.dart:76-81` / `check_closes_oi_cited.dart:126-130` serve the CONFLICT path, where `--continue` runs both). The gates ran on each branch; the coordinator's Task 5 commit is the first gate run over the union. The three fix units WILL conflict on the regenerated `docs/diagnoses/INDEX.md` (latest-first table, same base) — regenerate + `--continue`, never hand-merge. Shared files (CLAUDE.md, `gate_test_ledger.yaml`, `docs/blast_radius.yaml`, closure YAML, plan-review record, the OI board's closures) have ONE writer: the coordinator. The OI-155 fork mints ONE board entry (the migrations_live OI) — a board APPEND, which the coordinator's later in-place status edits do not collide with. | OI-220 rider 2; OI-86 is a Hive collision and does not reach `test/scripts/`; round-1 F8 (hooks). |
| D15 | Riders + conversion-on-touch at integration: CLAUDE.md §4.1.5 item 6 (value-semantics grep); §4.12.7 (execution-mode decision at batch start); §0 pre-push row (sweep) and its gate counts ("90 exist" → re-run `ls scripts/check_*.dart \| wc -l` = 97 today; "14 case-skipped" → recount after D8); rule 21 Gate 42 wording + re-derived `presence_only` count; §7 rows (sweep; safe_merge with the absent-record check + re-run test count); `pre-commit.sh:134-136,:310-312` "14 case-skipped" comments (Task 2); `docs/playbook/common-pitfalls.md` section from `compass_redesign_test.dart`'s CODE. Board closures IN PLACE (`- **Status**: CLOSED · 2026-09-19 · diagnose <id> · commit <sha>`). | OI-220 riders; round-1 F10c/e. |
| D16 | Pin `scripts/contract_sweep.dart`, `scripts/contract_sweep_lib.dart`, `scripts/gate_scripts_wired_lib.dart`, `scripts/check_sot_behavioral_test_paths.dart`, and — because Gate 33's `manual:` verdicts now read them — `scripts/oi_closure_lib.dart` + `scripts/check_closes_oi_cited.dart` as `platform` in `docs/blast_radius.yaml` (beside the `scripts/pre-push.sh` pin at `:164`), so a later edit gutting any of them cannot classify `feature` and skip review + the full suite. | Round-1 F6; round-2 hooks F10 (the mirror). |
| D17 | Lenses for the review rounds, by their real IDs: L24 gate-strictness, L37 empty/null-shape readers, L52 gate scripts wired, L25 intra-document drift, L20 diagnose-doc pairing, L8 contract-test coverage, L54 context-artifact cost (CLAUDE.md 121,895 B vs soft band 135,697 — riders fit; OPEN_INDEX has ~600 B headroom, closing three OIs helps). | Round-1 F12 (the v1 plan named L7/L31, which are different lenses). |

## 3. Out of scope, stated
- OI-165 (which token the live-SQL runner needs), OI-101 (runtime-budget re-arm/delete), and the new migrations_live retire-or-redesign OI — founder-blocked; D8 makes each state machine-visible and self-reopening.
- Whether `safe_merge.sh` should also preview a `branch:` field mismatch (`check_plan_review_record_exists.dart:813`). Residues of the absent-record precheck, stated in its diagnose-doc: it fires only on the `safe_merge.sh` path (`gh pr merge` and a raw `git merge` bypass it); an `origin/foo` spelling is silent (as the existing bpass precheck); the classifier's content rule reads the WORKING TREE pre-merge, so a SECURITY DEFINER migration on the branch classifies `platform` not `catastrophic` — still ≥ account, the warning fires, the tier in the message can understate.
- D7's predicate is "invocation-shaped line" (`run scripts/<gate>` on a non-`#` line), not "prose never counts": a skill doc or runbook sentence carrying that literal DOES count, deliberately — a skill is executed by an agent reading it. The fixture pins both halves.
- `check_two_user_cross_account.dart` and `claude_md_citations_letter_suffix_test.dart` spawning bare `'dart'`; the latter's throwing `tearDown` — pre-existing.
- Why most `check_*.dart` files classify `feature` under `scripts/** → feature` while the loops that run them are platform — a policy question for the board, not this batch (D16 pins only the files this batch edits).
