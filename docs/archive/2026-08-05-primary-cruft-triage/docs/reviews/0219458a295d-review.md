---
reviewed_at: 2026-06-12T22:05:00+05:30
staged_against: 0219458a295d
blast_radius: catastrophic
reviewer: claude-sonnet-via-bpass-subagent
agent_id: a08ac50aaf1d58374
lens_set: [auth_correctness, exception_flow_correctness, blast_radius_dpdp, secrets_in_tree, unawaited_no_error_sink, regression_test_adequacy]
findings_count: 1
findings_by_severity: { P0: 0, P1: 0, P2: 1, false_alarm: 6 }
verdict: accepted
---

# Code Review (B-pass) — 0219458a295d — delete-account auth + audit-insert fix (e8a1c3 + d5b2f8)

Catastrophic-tier change to the DPDP §17 erasure Edge Function. Fresh context-blind
Sonnet reviewer; full findings below.

## Verdict: CLEAN — 0 bugs (1 P2 future-proofing note, no action)

## Findings

1. **auth correctness — false_alarm** (index.ts:124-133). `createClient(SUPABASE_URL,
   SERVICE_ROLE) + getUser(token)` is correct: GoTrue validates the passed JWT and
   returns that token's user; `userId` comes exclusively from `userRes.user.id`. No
   path validates an attacker's token as a different user; forged/expired tokens
   return an error caught at the 401. Cross-account safe.

2. **audit try/catch never rethrows — false_alarm** (index.ts:463-496). The catch only
   logs (+ ORPHAN_BILLING). No `throw` / `return jsonError`. Control always reaches
   the 200 response.

3. **other PostgREST `.catch()` chains — false_alarm**. The remaining `.catch()`
   (index.ts:315) is on a native `fetch()` (OneSignal DELETE), which is a real Promise.
   The attempt-counter uses `.then(({error})=>...)` (valid on thenables). No builder
   `.catch()` in live code.

4. **blast_radius / DPDP — false_alarm**. Auth + confirmation-token both gate before any
   erasure; `deleteUser` is awaited with an error check (500 before audit). No PII in
   error bodies (`{error, request_id}` only; `userErr.message` logged server-side only).

5. **secrets_in_tree — false_alarm**. No credential-shaped literals; secrets via
   `Deno.env.get`. Diagnose-docs contain only 8-hex request_ids + an already-erased test
   user id.

6. **unawaited/no-error-sink — false_alarm**. The fire-and-forget attempt-counter (pre-existing,
   untouched) has a `.then` error sink.

7. **regression test pins the fix — false_alarm**. All 4 assertions (comment-stripped)
   would FAIL on the old code and PASS on the new. **P2 (note only):** the d5b2f8
   `src.contains('error: auditErr')` is a substring check — sound here (one post-strip
   occurrence), a regex would be marginally more future-proof.

## Founder triage
Accepted — clean catastrophic-tier review; both fixes mechanically correct + live-proven
(the test1 erasure ran the auth path successfully; boot-verify confirms v6).
