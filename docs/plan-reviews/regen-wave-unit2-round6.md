# OI-166 Unit 2 (v6) — plan review round 6

**Plan reviewed:** `docs/audit/oi166-unit2-plan-v6.md` (v6 = v5 edited in place to close round 5's
five gaps; see `round5.md`).

**Verdicts:** ground-truth reviewer **not-verified** · design reviewer still running at the time
this record was written (folded in below once returned — see the note at the bottom; this record
is written from the ground-truth pass alone so the real finding isn't lost waiting on the second).

## Real finding, not a citation nit: gap 3 (C's `'week'` field) was only half-closed

Round 5 named the `'week'` field as a drift the codebase itself calls "the whole subject of
OI-166." My round-5→v6 fix replaced exactly ONE of its writers
(`regenerate_plan_planner.dart:278`, the workout-day row). The ground-truth reviewer grepped the
whole file for `weekIdx + 1` and found **three** occurrences, not one:

- `:278` — the workout-day row (fixed).
- `:310` and `:321` — both calls into `_restEntry(dateStr, resolvedPhase, weekIdx + 1, dayOfWeek,
  weekPlan.weekCharacter)`, whose 4th positional parameter (`week`) is stamped verbatim at
  `_restEntry`'s own `:352` (unfixed).

Every rest day generated by a mid-phase AI-coach regen would still carry the old, unguarded week
number after the round-5 fix — the same drift, on a different row type. `feedback_mistake_
guard_without_its_mirror`'s exact shape: a finding names a SITE, not the CLASS. Fixed in v6 by
computing one shared `effectiveWeek` local at the same scope as the existing `weekPlan` local
(`:215-217`, before the inner day loop at `:227`) and using it at all three sites.

## Also corrected (citation drift, not logic)

- `holdWeek`'s deload-cadence line: cited `:266` (a comment line), actual code is `:268`.
- The doc-comment citation for "whole subject of OI-166" was `:1256-1262`; the phrase itself
  completes on `:1263` — widened to `:1256-1263`.
- The header's own brace-expansion (`round{1,2,3,4,5}.md`) implied `round5.md` existed before this
  round wrote it — self-contradiction, now resolved (round5.md exists, and this file extends the
  citation to `round{1,2,3,4,5,6}.md`).

## Ground-truth tally (from this round's pass)

8 exact matches, 3 near-matches within tolerance (all now tightened above), 1 material gap (the
`_restEntry` sites) — now closed in v6. All previously-verified core-mechanism citations
(`:358-360`, `:435-439`, the two real-caller `startDate` pass-throughs, the no-earlier-`_planKey`-
write claim) re-confirmed clean against the current file state.

## Design reviewer's verdict (on the pre-fix v6 text — ran concurrently, findings folded in below)

**Not-converged.** Independently confirmed the same `'week'`-field gap (1-of-4 sites, citing the
same `:278/:310/:321/:352` census) — good corroboration, not a new finding. Two additional real
gaps, both closed in v7:

- **C's `current_plan` write has no specified location anywhere in the document.**
  `RegeneratePlanPlanner.plan()` builds the `Phase` object and discards it; the actual Hive write
  happens in a different file (`tool_dispatcher.dart`) with no way to obtain it. Confirmed by
  grepping `tool_dispatcher.dart` and `workout_write_service.dart` for `current_plan`/`_planKey` —
  zero hits in both. Same failure shape as B's round-5 gap, recurring for C, unaddressed.
- **B's own `'week'` field** (5 stamp sites in the file total, 2 — `:473`, `:502` — inside this
  fix's loop) was never addressed; only C's was fixed. Flagged as "suspicious rather than
  reassuring" given the document otherwise enumerates every change explicitly.
- (Lower-confidence, resolved by inspection rather than a plan change): confirmed no import-cycle
  or static-init hazard from C reading `WorkoutScheduleReadService`, confirmed the first-generation
  blob write isn't suppressed by the new gate, confirmed the splice degrades safely on a null
  existing blob.

## Status

Both round-6 findings sets closed in v7 (`docs/audit/oi166-unit2-plan-v7.md`) — C's write site
fully plumbed (5 sites, mirroring the existing `rawSchedules` cache pattern), B's `'week'` field
closed via loop-variable redefinition rather than a separate patch. Round 7 dispatched on v7.
