# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# ICANBEFITTER Master Reference
> Single source of truth for all coding agents. Read this before touching any file.

---

## 0. DEVELOPMENT COMMANDS

### Environment Setup
Copy `.env.example` → `.env` and fill in Supabase URL, anon key, and Razorpay key ID.
Environment variables are injected **at build time** via `--dart-define-from-file=.env` (NOT flutter_dotenv — the package was removed). Every `flutter run` / `flutter build` command **MUST** include this flag or the app will crash with "No host specified in URI".

```
SUPABASE_URL=https://dedsavbjuwgarrhphgnl.supabase.co
SUPABASE_ANON_KEY=<anon key>
RAZORPAY_KEY_ID=rzp_test_<key>   # use rzp_test_ for dev flavor
```

### Run / Build (two flavors: `dev` and `prod`)
> ⚠️ **CRITICAL**: Every command below includes `--dart-define-from-file=.env`. Without it, SUPABASE_URL compiles to an empty string and auth will crash.

```bash
# Run dev flavor on connected device
flutter run --dart-define-from-file=.env --flavor dev -t lib/main.dart

# Run prod flavor
flutter run --dart-define-from-file=.env --flavor prod -t lib/main.dart

# Release APK (prod)
flutter build apk --dart-define-from-file=.env --flavor prod --release -t lib/main.dart

# Release App Bundle (Play Store)
flutter build appbundle --dart-define-from-file=.env --flavor prod --release -t lib/main.dart

# Web (no flavor needed)
flutter run --dart-define-from-file=.env -d chrome
flutter build web --dart-define-from-file=.env
```

### Gradle Configuration
`android/gradle.properties` controls JVM memory. Current safe defaults for 16GB system:
- `-Xmx4G` heap (NOT 8G — causes silent OOM crash)
- `-XX:MaxMetaspaceSize=2G`
- `-XX:ReservedCodeCacheSize=256m`
- `org.gradle.parallel=true` + `org.gradle.caching=true`

If build hangs silently with no output, check `android/hs_err_*.log` for JVM crash dumps. Use `/build-apk` skill for the full automated pipeline.

### Tests
```bash
# All unit tests (no device required)
flutter test

# Single test file
flutter test test/bmr_calculator_test.dart

# All integration tests (requires connected Android device + .env)
flutter test --dart-define-from-file=.env integration_test/app_test.dart --flavor dev

# Single integration flow
flutter test --dart-define-from-file=.env integration_test/flows/workout_log_flow_test.dart --flavor dev
```

Unit tests live in `test/`. Integration tests (require Hive + real device) live in `integration_test/flows/`.

### Lint & Analysis
```bash
flutter analyze
```

### Pre-commit hook
A bash hook at `scripts/pre-commit.sh` blocks any commit while
`flutter analyze --no-fatal-infos` or `flutter test` are failing. It is
NOT version-controlled into `.git/hooks/` automatically — every fresh
clone must install it once:
```bash
# macOS / Linux: any shell
# Windows: run from Git Bash (bundled with Git for Windows)
sh scripts/setup-hooks.sh
```
The setup script copies `scripts/pre-commit.sh` to
`.git/hooks/pre-commit` and chmods it executable. Bypass for a
single commit: `git commit --no-verify` (use sparingly — CI runs the
same gate). See CODING RULE 20 for the no-deferred-failures policy.

### Riverpod Code Generation
The project has `riverpod_generator` installed but providers are currently written manually using `flutter_riverpod` directly (no `.g.dart` files). If you add `@riverpod` annotations, run:
```bash
dart run build_runner build --delete-conflicting-outputs
dart run build_runner watch   # watch mode during development
```

### Edge Functions — host-shell deploy (preferred for any function with nested `_shared/tools/...` or payloads >100KB)

```bash
cd "C:/Upendra/Claude Code/Fitness App"
node .claude/emit_payload.js <fn> --auto --functions-dir <worktree>/supabase/functions
node .claude/deploy_via_api.js dedsavbjuwgarrhphgnl <fn> .claude/_payload_<fn>.json <verify_jwt>
```

- **Token:** auto-resolved from `supabase/.supabase/supabase access token.txt` (gitignored). Generated 2026-04-20 against fitness-app account.
- **Byte-identical to git** (no MCP path-mangling, no hand-trim risk). First used Phase C.5 → ai-proxy v43; now standard for all redeploys.
- **Path scheme:** all shared imports MUST use `from "../_shared/..."` (parent dir), NOT `from "./_shared/..."`. The OLD MCP `deploy_edge_function` tool silently mangled the wrong path; the new flow doesn't. Audit your edits.

`mcp__ba7b5e8e__deploy_edge_function` (the legacy MCP path) still works for small single-file functions but is unsafe for the AI coach tools bundle. Do not use `supabase` CLI — it is logged into the wrong account (personal, not fitness app account). See §2a for account details.

---

## 1. PROJECT IDENTITY

**App:** ICANBEFITTER — personalised fitness & nutrition platform for young professionals (22-35) in India.
**Model:** Freemium. ₹349/month or ₹2,999/year for PRO.
**Architecture:** Offline-first. Hive = primary. Supabase = backup + AI + community growth.

---

## 2. TECH STACK

| Layer | Technology |
|---|---|
| Frontend | Flutter (Android + Web → iOS later) |
| State Management | Riverpod (with code generation) |
| Local Storage | Hive (offline-first, primary for all reads/writes) |
| Auth | Supabase Auth (Email + Google OAuth + Phone OTP) |
| Database | Supabase Postgres (46 tables — backup + AI + community) |
| Storage | Supabase Storage (exercise images, progress photos PRO) |
| AI Coach (all tiers) | Single Edge Function `ai-proxy` → Gemini 2.5 Flash. Free: 30-day trial 15 msg/day. PRO: unlimited. Server-side gate. |
| Food AI | Gemini 2.5 Flash (text analysis) + Gemini 2.5 Flash Lite (scan meal, cart auditor) |
| Weekly AI Report | Gemini 2.5 Pro (PRO-only, deepest reasoning) |
| Plan Generator | Dart (local, queries Hive exercise_library, zero API cost) |
| Payments | Razorpay (WebView checkout → webhook → Supabase → poll → Hive) |
| Telegram Bot | Separate project (OpenClaw VPS, @ICanbeFitterBot) — NOT in this repo |
| Health | Google Fit / Health Connect / Samsung Health |

---

## 2a. SUPABASE PROJECT — CONFIRMED IDENTITY

> ⚠️ CRITICAL: There are TWO Supabase projects on this account. ALWAYS use the one below. NEVER guess.

| Field | Value |
|---|---|
| **Project ID** | `dedsavbjuwgarrhphgnl` |
| **Project name** | myfitnessjourney1988@gmail.com's Project |
| **Region** | ap-southeast-1 |
| **DB host** | `db.dedsavbjuwgarrhphgnl.supabase.co` |
| **Confirmed by** | Querying actual tables — `users`, `exercise_library`, `food_database`, `workout_logs`, `ai_coach_interactions`, `subscriptions` etc. all present |

**The OTHER project** (`krcrkntuwutvnmdnkfqf`, named "icanbefitter") is a **different app entirely** (blog/content platform — `posts`, `members`, `media` tables). Never touch it.

**Rule: Before ANY Supabase operation, confirm project_id = `dedsavbjuwgarrhphgnl`.**

### Supabase Access — TWO SEPARATE ACCOUNTS

The user has **two Supabase accounts** with different logins. These are NOT the same account.

| Account | Org ID | Org Name | Projects | Access via |
|---|---|---|---|---|
| **myfitnessjourney1988@gmail.com** | `hwwukmntixflgbxkwavm` | (default) | `dedsavbjuwgarrhphgnl` ✅ FITNESS APP, `krcrkntuwutvnmdnkfqf` (blog) | **MCP only** (auto-authenticated) |
| **Upendra's personal account** | `dsvxqvpitnpumftnsnwe` | ICANBEFITTER Supabase | `tjjmtscmwzvlzpbvgtbv` (Upendra-Prasad's Project), `zvwepplqqflhgubwalee` (ICANBEFITTER AI V1) | **CLI only** (`supabase login`) |

⚠️ The Supabase CLI (`supabase` command) is logged into the **personal account**, NOT the fitness app account. CLI commands like `supabase secrets set` will NOT work against the fitness app project unless re-authenticated.

**For Edge Function secrets / admin operations:** Use MCP tools (auto-authenticated to the correct account) or the Supabase Dashboard logged in as `myfitnessjourney1988@gmail.com`.

### Credentials (Edge Function Secrets)

| Secret Key | Value | Status |
|---|---|---|
| `ONESIGNAL_APP_ID` | `fd37a411-121e-4022-9929-2af68c2371f5` | ✅ Set |
| `ONESIGNAL_REST_API_KEY` | *(set in dashboard, not committed to code)* | ✅ Set |
| `GEMINI_API_KEY` | *(set in dashboard, not committed to code)* | ✅ Set |
| `CEREBRAS_API_KEY_1` | *(already set)* | ✅ |
| `CEREBRAS_API_KEY_2` | *(already set)* | ✅ |
| `CEREBRAS_API_KEY_3` | *(already set)* | ✅ |
| `RAZORPAY_KEY_SECRET` | *(already set)* | ✅ |

### Firebase / OneSignal

| Field | Value |
|---|---|
| Firebase project | AVYA |
| Firebase Sender ID | `194342788570` |
| OneSignal App ID | `fd37a411-121e-4022-9929-2af68c2371f5` |
| `google-services.json` | ✅ In `android/app/google-services.json` |
| Google Services Gradle plugin | ✅ Configured in `settings.gradle.kts` + `app/build.gradle.kts` |

---

## 3. SCREENS (5 Tabs)

| Tab | Screen | Key Features |
|-----|--------|-------------|
| 🏠 Home | Dashboard | Streak, weekly calendar, quick actions, today's workout, nutrition snapshot, weight sparkline, PR snapshot |
| 🏋️ Train | Workouts | Phase plan, week selector, active workout mode, exercise swap, template builder, copy week |
| 🥗 Nutrition | Food Logging | BMR/TDEE, 2-tab Log Food (AI + Scan), food search (5K DB), AI analysis (PRO), saved meals, water tracking, diet plan generator + PDF export |
| 💬 AI Coach | Chat | In-app chat, Telegram toggle, quick prompt chips, reasoning tab (PRO), photo/video upload (PRO) |
| 👤 Profile | Settings | Bio stats, goal card, edit profile, health sync, reports, subscription, logout |

---

## 4. DATA ARCHITECTURE (OFFLINE-FIRST)

[MIGRATED to docs/architecture/sync.md] — see for full content.

---

## 5. DIRECTORY STRUCTURE

[MIGRATED to docs/reference/directory-structure.md] — see for full content.

---

## 6. CODING RULES (NON-NEGOTIABLE)

1. **Hive-first for ALL reads/writes.** Never block UI on Supabase response. Supabase writes are background/async.
2. **Riverpod only** for state management. No `setState` for shared state. Use `@riverpod` annotation + code generation.
3. **Hive boxes:** Register ALL adapters in `main.dart`. Open ALL boxes before `runApp()`.
4. **Repository pattern** for all data access. Never call Supabase or Hive directly from widgets.
5. **subscription.gate()** for ALL PRO features. Never use inline `isPro` checks in widgets.
6. **Phase 1 is ALWAYS free.** Never gate it, even if subscription check fails.
7. **PaywallSheet** is the ONLY paywall UI. Never create custom paywall modals.
8. **Plan generator = local Dart.** Queries Hive exerciseBox. Never calls any API.
9. **Never expose API keys client-side.** All AI calls go through Supabase Edge Functions.
10. **DM Sans font everywhere.** No system fonts. Use `GoogleFonts.getFont('DM Sans', ...)`.
11. **Electric Cyan #00D4FF** for all accent/CTA. Never use green (#00e5a0) — that's the old spec.
12. **Dark theme only.** Background hierarchy: `#07090e` (bg) > `#0e1219` (card) > `#161d28` (input).
13. **All screens must handle:** loading state (skeleton), error state (retry), empty state.
14. **Never modify `plan_generator.dart`** without explicit instruction.
15. **Import paths:** Use relative imports within features. Use `package:` imports for shared/ and core/.
16. **Edge Function SSRF:** Never fetch arbitrary user-supplied URLs server-side. Allowlist Supabase Storage prefix only.
17. **Release error handling:** Use `kDebugMode` guard — show detailed errors only in debug, generic message in release.
18. **Edge Function input limits:** Enforce message (5K chars) and snapshot (10K chars) limits server-side on ALL AI endpoints.
19. **Server-side subscription verification:** High-value features (`phases_2_to_12`, `ai_coach_unlimited`, `progress_photos`) must call `verifyFromServer()` in `gate()`. Local-only check is insufficient.
20. **No deferred test failures.** Failing tests on `main` are P0 blockers for the next batch. The label "pre-existing failure" is banned. If a test fails on `main`, investigate and either fix it FIRST in its own commit on `main` OR document why it cannot be fixed (filed as a tracked bug in `MEMORY.md` with explicit rationale + owner + ETA). The pre-commit hook (`scripts/pre-commit.sh`) blocks any commit while `flutter test` reports failures; do not bypass with `--no-verify` to "ship something else first".
21. **Regression test required for every fix.** Every bug fix MUST ship with a test that FAILS on `main` without the fix and PASSES with it. PR review is blocked otherwise. Source-grep tests (e.g. tests that scan a file for a forbidden string pattern) count. The acceptable forms: contract test under `test/contracts/`, write-to-read round-trip under `test/<service>/`, or a targeted unit test alongside the fixed file. Cite the test path in the commit message.
22. **Bug fixes require a diagnose-doc.** Every commit on `main` whose subject matches `^(fix|bug|regression)(\([^)]*\))?:` MUST reference a `docs/diagnoses/<date>-<slug>-<id>.md` file via `closes-diagnose: <bug-id>` in the commit body. The doc must pass `dart run scripts/validate_diagnose_doc.dart <path>`. Subagents dispatched for investigation MUST receive `docs/agent_brief_preamble.md` as their prompt prefix; their output must pass `dart run scripts/validate_agent_diagnose_stanza.dart <path>` before main thread accepts the proposed fix. Per-commit waiver: `regression-test-skipped: <reason>` in commit body skips the diagnose requirement but logs to `docs/skipped-discipline.md` (timestamp + SHA + reason) and counts toward batch-level cut-corner accounting. The interactive walkthrough is at `/diagnose-bug`. Canonical L3 checklist: `docs/discipline.md`. Source-of-truth registry: `docs/sot_registry.yaml`. Pre-commit hook (`scripts/pre-commit.sh`) enforces this gate; `/build-apk` Gate 10 enforces it pre-build for every commit since the last shipped APK.

23. **No stopping mid-batch.** When the founder gives a multi-task instruction ("fix everything", "address each and everything", "continue"), Claude runs through to completion. Only valid stopping conditions: (a) the WHOLE batch is genuinely done, (b) BLOCKED on a user action only the user can perform (keystore password, Dashboard secret rotation, destructive op not pre-authorized), or (c) the user sends a new interrupting instruction. **Banned excuses:** "context is at X%", "responsible handoff", "fresh session pickup", "should I continue?", "want me to proceed?". Context management is Claude's job — use TodoWrite, dispatch focused subagents, compact when needed, but do not use context as an excuse to stop work the user asked for. See project memory `feedback_no_stop_until_done.md` for full directive. Codified 2026-05-11 after a 4th repeat of the pattern. **Documentation is non-negotiable** — every fix gets a diagnose-doc per rule 22 + memory file update for any NEW pattern / mistake / preference + CLAUDE.md update for any NEW invariant.

---

## 7. DATABASE SCHEMA (46 Tables — Supabase Postgres)

[MIGRATED to docs/architecture/database.md] — see for full content.

---

## 8. LOGGING TYPES (Drives Active Workout UI)

[MIGRATED to lib/features/train/CLAUDE.md] — see for full content.

---

## 9. DESIGN SYSTEM

[MIGRATED to lib/shared/widgets/wardroom/CLAUDE.md] — see for full content.

---

## 10. SUBSCRIPTION GATE PATTERN

[MIGRATED to docs/architecture/subscription.md] — see for full content.

---

## 11. AI ARCHITECTURE

[MIGRATED to docs/architecture/ai.md] — see for full content.

---

## 12. PLAN GENERATOR

[MIGRATED to lib/shared/repositories/plan_engine/CLAUDE.md] — see for full content.

---

## 13. HOME SCREEN LAYOUT (Priority Order)

[MIGRATED to lib/features/home/CLAUDE.md] — see for full content.

---

## 13a. ONBOARDING (stepped flow — default since PR Y–AB, expanded over PR AI 2026-04-20, APK Test #1 2026-04-24, APK Test #2 2026-04-25)

[MIGRATED to lib/features/onboarding/CLAUDE.md] — see for full content.

---

## 14. BUSINESS RULES

[MIGRATED to docs/architecture/business-rules.md] — see for full content.

---

## 15. SYNC SCHEDULE

[MIGRATED to docs/architecture/sync.md] — see for full content.

---

## 16. PAYMENT FLOW

[MIGRATED to docs/architecture/payment.md] — see for full content.

---

## 17. EXERCISE LIBRARY

[MIGRATED to docs/reference/exercise-library.md] — see for full content.

---

## 18. FOOD DATABASE

[MIGRATED to docs/reference/food-database.md] — see for full content.

---

## 19. COMMON BUGS TO AVOID

| Bug | How to Avoid |
|---|---|
| [MOVED to lib/core/services/CLAUDE.md] Hive box not open | Open ALL boxes in main.dart before runApp() |
| [MOVED to lib/core/services/CLAUDE.md] Adapter not registered | Register ALL Hive adapters before openBox() |
| Null exercise data | Always null-check: `exercise?.name ?? 'Unknown'` |
| Plan crash on empty | Guard: `plan?.workouts?.isNotEmpty == true` |
| [MOVED to root §3 (Milestone 6)] Wrong import path | Use relative within features, package: for shared/core |
| Accent color wrong | #00D4FF (Electric Cyan), NEVER #00e5a0 (old green) |
| isPro inline check | ALWAYS use subscription.gate() |
| [MOVED to supabase/functions/CLAUDE.md] API key in client | ALL AI calls through Edge Functions |
| Water not resetting | Check date on app launch, reset if new day |
| [MOVED to lib/shared/widgets/wardroom/CLAUDE.md] Font fallback | Always use GoogleFonts.getFont('DM Sans', ...), never default font |
| Phantom PRO status | Force-clear localActivationAt after grace period on network error |
| [MOVED to lib/features/home/CLAUDE.md] Steps/sleep showing stale data | Filter health data by BOTH date AND type (`step_log`, `sleep_log`). Legacy `steps_today` guarded by `stepsToday == null && steps_date == todayStr`. Chat-logged sleep read from `sleep_logs` list as fallback. |
| AI insight from old chat | Compute insights from local schedule data, not last AI message |
| [MOVED to lib/features/home/CLAUDE.md] Stats grid empty | Fall back to top 4 exercises from allExercisePRs when key lifts (bench/squat/deadlift/OHP) have no data. Unit derived from loggingType (kg/reps/s/km). Adaptive layout: 1→full, 2→row, 3→2+1, 4→2+2. |
| [MOVED to lib/features/home/CLAUDE.md] Prediction card truncated | Home: maxLines 4 + "Read More →" opens full bottom sheet. Shareable: capped at 500 chars. |
| JWT expired during payment poll | Refresh JWT before each verify-payment retry |
| Onboarding sync fails silently | Retry sync with JWT refresh on failure |
| Daily snapshot not pushing | pushSnapshot() must be wired into checkAndSync(), fires on every app launch |
| AI chat "Session refreshed" error | JWT auto-retry: on auth error, refresh token + single inline retry (NOT recursive — old recursive caused 30+ duplicates) |
| Days/week change not rescheduling | `generateAndScheduleFromDate()` in WorkoutScheduleService — deletes future non-completed entries, preserves completed, regenerates from today |
| Image upload RLS violation | Storage path must be `$userId/$timestamp.jpg`, NOT `chat-media/$userId/...` — bucket name is already set by `.from('chat-media')` |
| [MOVED to lib/features/ai_coach/CLAUDE.md] Mic stops after 2-3 seconds | `pauseFor: 5s`, `listenFor: 60s`, `ListenMode.dictation`, `partialResults: true` via `SpeechListenOptions` |
| Stale "Chat/Reasoning" toggle in AI coach header | Removed 2026-04-18. If you see this pill in a fresh APK, the `_buildStatusPill` in `ai_coach_screen.dart` didn't ship — single-pill header is the current design. |
| SSRF via ai-media-proxy | Only allow `${SUPABASE_URL}/storage/v1/object/` prefix URLs. Never fetch arbitrary user-supplied URLs server-side. |
| Null expiry grants PRO | `isPro()` returns `kDebugMode` when `expiresAt` is null — release builds treat null as free. Never remove this guard. |
| [MOVED to supabase/functions/CLAUDE.md] Promo code enumeration | `validate-promo` requires JWT auth. Never expose promo discount_pct to unauthenticated callers. |
| Subscription bypass via Hive | `gate()` calls `verifyFromServer()` for high-value features (`phases_2_to_12`, `ai_coach_unlimited`, `progress_photos`). Never rely on local-only check for these. Any feature that writes to Storage or spends cloud resources on the user's behalf MUST be server-verified. |
| Oversized AI payload abuse | All AI Edge Functions enforce message (5K chars) + snapshot (10K chars) limits server-side. Client limits are advisory only. |
| Error widget leaks stack trace | `ErrorWidget.builder` shows generic message in release (`kDebugMode` guard). Never show `exceptionAsString()` to production users. |
| future-prediction broken import | Import shared modules as `../_shared/openrouter.ts` in source, but deploy with `./_shared/openrouter.ts` (MCP places files in source/ subdirectory). |
| [MOVED to lib/features/train/CLAUDE.md] Warm-up sets counted in completedSets | `completedSets` getter filters out `warmUpSets` keys. Exercise name matching uses exact-first, fuzzy only for names >= 6 chars. |
| [MOVED to lib/features/train/CLAUDE.md] WarmupCooldownSection RangeError | `didUpdateWidget` resets `_checked` list when `widget.exercises.length` changes. Always guard list length on rebuild. |
| Workout receipt shows stale/wrong data | `WorkoutReceiptData.fromExerciseLogs(date)` is the SINGLE source of truth. Deduplicates exercises by name, sums sets, takes max weight. Never hand-build receipt data from a widget's local state. |
| Workout log edit path missing | `EditWorkoutLogSheet` is the single edit surface. Exposed from 4 places: WorkoutReceiptSheet Edit button, Home "View Card", calendar day detail, Train expanded view. Save recomputes `volume_kg`, chronologically rescans PR flags for that exercise, fires `pushSnapshot()` + `syncWorkoutData()`, invalidates all workout/home providers. Never build a second edit path — it will drift from this one. |
| Scan meal saves 0 kcal | `_ScanResultEditor` always computes `total_calories` from live item sum with per-item Atwater fallback (`kcal > 0 ? kcal : 4P+4C+9F`). Never read `result['total_calories']` directly — AI responses often omit it while per-item kcal is populated. |
| [MOVED to lib/features/nutrition/CLAUDE.md] Scan meal result not editable | `_ScanResultEditor` replaces the old read-only `_buildResult`. All scan results are mutable: editable meal name, per-item name/kcal/P/C/F/Fi, +Add Item, X Delete. Total recomputes on every keystroke via `onChanged: (_) => setState(...)`. |
| [MOVED to lib/shared/widgets/wardroom/CLAUDE.md] Hidden tap-to-edit targets | Any row that responds to tap-to-edit MUST show a visual affordance (e.g., pencil icon at 14px, `textSecondary.withValues(alpha: 0.7)`). Invisible tap targets will not be discovered. |
| AI "temporarily unavailable" masks real error | `AiService._extractError()` pulls `{"error": "..."}` from non-200 Edge Function responses. Never throw raw "status X" — the server-side error body contains the actionable reason (message too long, snapshot too large, PRO required). |
| AI snapshot exceeds 10KB server limit | `AiService._compactContext()` trims in priority order: `step_history_7d` → `weight_trend` → `nutrition_trend` → `exercise_history` → `personal_records` → `coach_notices` → truncate `coaching_notes` → drop `fitness_summary`. Target <9500 bytes for JSON overhead buffer. Historical queries routinely blow past 10KB without this. |
| [MOVED to lib/features/ai_coach/CLAUDE.md] "Restart the app" error copy | Never use. Map server errors to actionable user messages in `ai_coach_provider.dart`: `Message too long` → "shorten it", `Snapshot too large` → "try a shorter question", `Image too large` → "max 5 MB", `502/503/504` → "model temporarily unavailable, try in a minute". |
| [MOVED to supabase/functions/CLAUDE.md] Edge Function leaks stack trace | Every catch block MUST return `{error: "Internal server error", request_id: <8-char hex>}` and log `console.error("[fn-name] request_id=X", err)` server-side. Never `JSON.stringify(err)` into the response body. Validation errors (400s) are the only exception — they ARE user-actionable and safe to return verbatim. |
| Webhook double-processes payment | `razorpay-webhook` MUST pre-SELECT by `razorpay_payment_id` before INSERT and catch Postgres 23505 (unique_violation) as success. Promo redemption runs ONLY when `alreadyProcessed === false`. Razorpay retries webhooks for 24h — without this, every retry duplicates the subscription row and burns a promo use. |
| Nutrition changes don't reach AI coach | Every nutrition mutation (logFood, addWater, decrement, urine color, updateFoodLog, deleteFoodLog, relogSavedMeal, AI breakdown, scan meal save, barcode save) MUST call `unawaited(SyncService.instance.syncNutritionData()) + unawaited(SyncService.instance.pushSnapshot())`. Missing either = AI coach gives advice based on stale/missing meals until next app launch. |
| Custom exercise invisible to AI coach | `CreateCustomExerciseSheet._save` fires `unawaited(SyncService.instance.pushSnapshot())` after `customBox.put`. Without this, the AI coach doesn't learn about the new exercise until app restart. Same pattern applies to any write that creates a new entity the AI should know about. |
| food_text_analysis unlimited abuse | `ai-proxy` enforces per-day rate limit on `food_text_analysis` channel (50/day free, 200/day PRO) counted via `ai_coach_interactions` rows. Server is the source of truth; client has no enforcement. Never rely on "it's just text" to skip rate limiting — text calls hit the same Gemini quota as vision. |
| Fixed-delta kcal ignores user pace | `BmrCalculator.calculateTargets` requires `pacePreference` (slow/balanced/aggressive). Deficit/surplus back-computed from `current_kg × pace_rate × 7700 / 7`. Never reintroduce fixed `+300 / -500` deltas — they ignore the user's chosen pace. |
| [MOVED to lib/features/nutrition/CLAUDE.md] Missing projection on MY TARGETS | Both `profile_screen._buildNutritionTargets` and `nutrition_screen._buildProjectionSubtitle` must read `current_weight_kg`, `target_weight_kg`, and `pace_preference` from the user profile. Projection only shown for `lose_fat`/`build_muscle` goals with non-zero gap. |
| Food log delete with no undo | `nutrition_screen._confirmAndDeleteFoodLog` is the ONLY delete surface. Stashes log before delete; `restoreFoodLog` writes back at deterministic key `flog_<loggedAt>_<foodNameHash>`. Never call `deleteFoodLog` directly from a widget. |
| [MOVED to root §3 (Milestone 6)] Gradle build hangs silently | Check `android/gradle.properties` — `-Xmx` must be ≤4G on 16GB system. 8G causes OOM with no terminal output. Check for `android/hs_err_*.log` crash dumps. Also remove stale `flutter/bin/cache/lockfile` if Flutter commands hang on lock. Use `/build-apk` skill. |
| [MOVED to lib/shared/repositories/plan_engine/CLAUDE.md] Plan generator picks wrong-target exercise | Cascade attempt3 drops `target_focus` + `exercise_type`, keeping only `movement_pattern` — results in a push instead of a chest-specific push. Root causes: (a) exercise library pool too shallow for the slot's triple, or (b) for Phase 1 beginners, `suitable_for` too restrictive (needs "Beginner" + `is_foundational: true`). Fix: either expand library `suitable_for` on the missing exercise OR adjust `split_resolver.dart` slot ordering so beginners don't hit the shallow pool at P1/P2. Verify with `dart run test/plan_generator/sample_plans_report.dart` (target: 0 attempt3/universalPool/none). |
| [MOVED to lib/shared/repositories/plan_engine/CLAUDE.md] Plan generator returns wrong number of exercises | `VolumeFilter` uses `slots.take(targetCount(experience, daysPerWeek))` — depends on `split_resolver` emitting enough P1-P5 slots in priority order. If a split returns fewer slots than the advanced target (10 for 3-day), users get truncated output silently. When adding/reordering a split, count slots and confirm it covers the advanced case. |
| [MOVED to lib/shared/repositories/plan_engine/CLAUDE.md] Pike Push Up assigned to rear delt slot | Sign that cascade exhausted `attempt1-4` and fell to `universalPool`. Indicates too many slots of the same muscle/pattern/type across the week — library pool depth insufficient. Cap rear delt slots to 3/week, lateral delt to 3/week, front delt to 1/week (current library depth). Fix in `split_resolver.dart`, NOT by editing the universal pool. |
| [MOVED to lib/features/home/CLAUDE.md] Free user stuck on day 29 with empty schedule | Check `WorkoutScheduleService.isPhaseExpired()` returns true AND `todayWorkoutProvider` is null → `home_screen._buildTodayRow` must render `PlanExpiredCard` (3 doors: Upgrade / Build custom / Re-do Week 4). PRO users auto-generate next Phase on splash via `splash_screen._autoGenerateNextPhaseForPro()` so they never land here. Added 2026-04-18 per audit H9. |
| [MOVED to lib/features/profile/CLAUDE.md] Progress-photo upload fails with PhotoQuotaException | Daily cap exceeded. Free: 2/day, PRO: 5/day. Enforced at `ProgressPhotoRepository.capture` by counting today's `progress_photos` rows for the user BEFORE pick. UI should catch `PhotoQuotaException`, surface the paywall for free users (`feature: 'progress_photos'`) or a "come back tomorrow" snackbar for PRO. Image quality differs by tier too: 2048/85% free, 3000/95% PRO. |
| Cerebras/OpenRouter calls anywhere | ALL AI traffic moved to Gemini on 2026-04-18 (commit `7646200`). `_shared/openrouter.ts` is deleted; `_shared/gemini.ts` is the only helper. If you see `api.cerebras.ai` or `openrouter.ai` URLs in a new Edge Function, it's regression — route through `geminiChat(...)` instead. |
| `ai-proxy-pro` or `video-status` 410 Gone | Both retired (2026-04-18). `ai-proxy-pro` merged into `ai-proxy` (single Gemini endpoint with server-side `isPro` gate). `video-status` deferred with the video-share feature. Callers should not re-add either; if the video feature ever un-defers, rewrite `video-status` with JWT + user_id filter before deploying. |
| [MOVED to lib/core/services/CLAUDE.md] Hive file bloat over time | `HiveService` implements `WidgetsBindingObserver` and runs `box.compact()` on **8** mutation-heavy boxes (user / workout / nutrition / health / custom / coach / sync / notifications) every 7 days on `AppLifecycleState.paused`. Gated via `configBox['last_compact_at']`. Per-box + global telemetry via `ErrorTelemetry.recordNonFatal` (reasons `hive_service_maybe_compact_box` / `hive_service_maybe_compact`). User-scoped box switch is race-safe via `HiveUserSession._sessionLock` + `Hive.isBoxOpen()` guard. Verified GREEN by audit 2026-05-17 / OI-17. |
| [MOVED to supabase/functions/CLAUDE.md] food_text_analysis 429 when user is below daily cap | Trigger `trg_food_text_rate_limit` on `ai_coach_interactions` (migration 024, 2026-04-18) enforces the 50/day free / 200/day PRO cap atomically. Insert-first pattern — `ai-proxy` inserts a placeholder row BEFORE calling Gemini. If trigger raises `food_text_daily_limit_reached` (SQLSTATE P0001), return 429. Do NOT re-add a separate check-then-insert pre-check; the trigger is the single source of truth. |
| [MOVED to lib/features/onboarding/CLAUDE.md] Stepped onboarding bounces back to Welcome on every tap | `GoRouter._authRedirect`'s `isOnOnboarding` check MUST use `location.startsWith('/onboarding')`, not `location == '/onboarding'`. Sub-routes (`/goal`, `/stats`, `/plan`) would otherwise be redirected back to Welcome on every navigation. Fixed in commit `17faa86`. |
| [MOVED to root §3 (Milestone 6)] Worktree APK build fails with "Did not find .env" | `.env` is gitignored and not copied when creating a new git worktree. Before `flutter build apk` in a new worktree, copy from the main: `cp "C:/Upendra/Claude Code/Fitness App/.env" <worktree>/.env`. Without it `SUPABASE_URL` compiles to empty string and auth crashes at startup. |
| [MOVED to lib/shared/widgets/wardroom/CLAUDE.md] Fraunces title emphasis not italic-gold | Don't style an entire `Text` widget italic — the non-emphasized leading/trailing words become italic too. Use `RichText` with inline `TextSpan`s carrying `fontStyle: FontStyle.italic`, `color: AppColors.accent`, `fontWeight: FontWeight.w500` on the emphasised span only. Pattern baked into `WardDispatchHeader`. |
| [MOVED to lib/shared/widgets/wardroom/CLAUDE.md] Wardroom palette drift | PRs K–Q used README hex values (rounded for print); PR R (commit `174ff21`) reconciled `colors.dart` to the JSX `const W = {}` in `Knowledgebase/Avya App redesign/design_handoff_wardroom/src/wardroom-tokens.jsx`. The JSX is the truth. Never back-port README hex values into `colors.dart`. |
| [MOVED to lib/features/home/CLAUDE.md] Streak banner fires at 3 PM for a morning lifter | `StreakWarningBanner.shouldShow` (and mirror in `home_provider.StreakWarningEligibilityNotifier._evaluate`) clamps threshold to `[18, 23]`. Handoff is an **evening-only** nudge. Don't re-lower the floor to 15 — an early-riser (6 AM median → raw 9 AM) would surface the banner before dinnertime. Both callsites must stay in sync. |
| [MOVED to lib/features/nutrition/CLAUDE.md] Diet-plan meals not visible on nutrition screen | `TodaysMealsCard` renders "FROM YOUR DIET PLAN" hint on empty slots via `dietPlanProvider` (reads `configBox['saved_diet_plan']`). If hints don't show after a user saves a plan, confirm `diet_plan_screen._savePlan` still calls `ref.invalidate(dietPlanProvider)` after `saveDietPlan`. Tap-to-log pre-fill depends on `showFoodSearchSheet(initialQuery: planned.firstFoodName)` — don't drop the initialQuery argument. |
| [MOVED to lib/features/profile/CLAUDE.md] Weekly Report sparkline dips to 0 between weigh-ins | By design only for calories/protein/workouts (zero-fill = genuinely no activity). Weight series is **forward-filled** from last known — if you see it dropping to zero on un-weighed days, `weeklyReportDataProvider` has regressed. |
| AI coach greets "Good morning." with no name | `userProfileProvider['full_name']` is null or still the bootstrap 'User' placeholder. Greeting falls back silently; no bug. If a real name is in Hive but the greeting still reads generic, verify `userProfileProvider` is returning the latest map (should invalidate on profile edits). |
| Superset A / rest timer still shows cyan after palette rotation | `train_provider.supersetColor()` and `RestTimerData.timerColor` used hardcoded `Color(0xFF00D4FF)`. PR AH.C1 swapped to `AppColors.accent`/`warn`/`bad`. Any new hardcoded color literal in a shared provider is a token-hygiene regression — grep `0xFF00D4FF` / `0xFF07090e` / `0xFFeef2f7` periodically. |
| Profile completeness nudge stuck on "Injuries" for new users | `completeOnboarding` in `onboarding_provider.dart` MUST copy `injuries`, `diet_preference`, `body_fat_percent`, `start_date` (as `startDateKey` — don't collide with the DateTime-typed `startDate` used for scheduling), and `city` from `state.answers` into the saved profile map. Before 2026-04-24 these were set via `setAnswer` in `plan_screen` but never persisted → `userBox['profile']['injuries']` was null → `profileCompletenessProvider.val is List` check failed → "Injuries" flagged missing indefinitely. Fixed on `feat/onboarding-train-nutrition` 2026-04-24. |
| Injuries round-trips to cloud as `"[none]"` string | `onboarding_provider._syncOnboardingToSupabase` must pass `profile['injuries']` directly (a List<String>), NOT `.toString()` it. `supabase_flutter` serializes the List to a Postgres `text[]` literal. Pre-2026-04-24 code called `.toString()` → string `"[none]"` landed in the cloud `text` column → cross-device restore merged it back into Hive as a String → completeness check broke again. Paired with migration 033 which moved the column from `text` → `text[]`. |
| RECOMP / PERFORM wrap to 2 lines on Goal screen | `WardRadioRow` left-label column was fixed at 44 dp; with `AppTypography.mono` 10sp + 2px letter-spacing, 6+ char codes overflowed. Fixed 2026-04-24 by widening to 56 dp and adding `maxLines: 1, softWrap: false, overflow: clip`. If you add a new `rowKey` longer than 7 chars, either shorten it or bump the column width again. |
| Cross-account Hive leak on fresh sign-up | Android Auto Backup was default-enabled (no `allowBackup="false"` / `dataExtractionRules` in AndroidManifest), so Hive files survived reinstall on any device signed into the same Google account — prior user's templates / logs / coach memory showed up on a fresh account. Fixed 2026-04-24 with `data_extraction_rules.xml` excluding `app_flutter/` + `splash_screen._runDeferredInit` startup check that clears Hive + signs out when `userBox['profile']['id']` ≠ `currentUser.id`. Both layers required; removing either re-opens the leak. |
| Train screen week chips / "No stats yet" card narrow | Both had children sized to content because parents used `CrossAxisAlignment.start` or fixed-width scrollable chips. Fixed 2026-04-24: `week_selector.dart` swapped `ListView.separated` for `Row`+`Expanded`; `stats_grid.dart` parent Column flipped cross-axis to `stretch`. Any future similar "card sitting narrow on Train" regression will be the same class of bug. |
| Fiber invisible to AI coach | Hive has always stored `total_fiber` on `nlog_*` rows and the UI showed a fiber bar, but `nutrition_logs` had no `total_fiber` column (migration 003 omission) and `_getTodayNutrition()` only summed cal/P/C/F. AI coach couldn't reference fiber intake. Fixed 2026-04-24: migration 034 adds the column; `_syncNutritionLogs` projects it; `_getTodayNutrition` returns `fiber_g` + `fiber_target_g: 30` in the snapshot. Gemini picks up the keys automatically via JSON-stringified snapshot — no prompt edit needed. Historical rows default to 0 since `nutrition_log_items` has no fiber column to backfill from. |
| Nutrition page's "Search 5,000+ foods" is a lie | Actual seed is 93 per §18. Copy was trust-breaking. Corrected 2026-04-24 to `"Search foods"` in both `nutrition_screen.dart` and `food_search_sheet.dart` empty state. If the library ever grows past a few hundred items, revisit. |
| PRO + "renews <date>" visible on fresh free-tier account | Same Auto Backup leak class as the template/Hive bug: a previous PRO user's `configBox['isPro']=true` + `configBox['expiresAt']=<end_date>` survived via Google Drive backup onto a new sign-up. The Supabase `subscriptions` table had no row for the new user, but the UI read `subInfo.expiresAt` directly and showed the old account's renewal date. Fixed 2026-04-24 on three layers: (1) Auto Backup disabled via `data_extraction_rules.xml`; (2) `isPro()` now runs Hive-profile.id vs session.id check on every call and force-downgrades on mismatch; (3) `_downgradeLocally` wipes `_expiresAtKey`, `_planKey`, `localActivationAt`, `_lastVerifiedKey` — not just the `_isProKey` flag — so stale display data can't survive. All three layers required to close the class. |
| [MOVED to root §3 (Milestone 6)] Built APK via `flutter build apk` directly | Always use the `/build-apk` skill. `flutter build apk` can hang silently on this machine (16 GB system, `-Xmx4G` Gradle heap) without emitting output. The skill does pre-flight cleanup (stale `flutter/bin/cache/lockfile`, Gradle caches, JVM crash dumps at `android/hs_err_*.log`) that prevents the hang. Direct Bash `flutter build apk` skips that and occasionally costs 30+ min of debug. See memory `feedback_use_build_apk_skill.md`. |
| Mutation writes Hive but skips sync — AI coach sees stale context | Every write path that crosses a synced domain MUST fire `unawaited(SyncService.instance.syncX())` + `unawaited(SyncService.instance.pushSnapshot())` after the Hive put + provider invalidation. Audited 2026-04-24 (PR-FIX-1, commit range `cee054a`..merged): 10 gaps closed — `completeWorkout`, `submitWorkoutDraft`, `DeleteNutritionLogNotifier.delete`, `edit_profile_screen._save`, `swap_sheet._onConfirm`, `_logSleep`/`_logMeasurement`, `BiometricNotifier.logSleep`, `saveMealPreset`/`deleteSavedMeal`, `addCustomFood`, `extractAndSaveCoachingNotes`, splash `checkAndSync`. Regex-based regression tests live in `test/sync/sync_gap_test.dart` — do not delete. |
| Raw `Hive.box('name')` in a cold-start-reachable path | Always use `HiveService.instance.<nameBox>`. Raw `Hive.box()` throws `HiveError: Box not found` if evaluated before `HiveService.init()` finishes (slow device, deep-link cold start). Closed 2026-04-24 (PR-FIX-2, merged) in `sign_in_screen`, `terms_modal`, `ai_coach_repository` (4 sites), `progression_resolver`. Regression tests in `test/safety/`. |
| `featureActiveWorkoutMode` is PRO on paper, free in practice | Both START WORKOUT entry points in `train_screen.dart` now route through `SubscriptionService.instance.gate(AppConstants.featureActiveWorkoutMode, ...)`. If you add a new entry point to the active workout flow, you MUST gate it — `test/subscription/active_workout_gate_test.dart` asserts at least 2 gate calls + zero direct `context.go('/train/active-workout')` without a gate. Closed 2026-04-24 (PR-FIX-3). |
| Onboarding "ADJUST PLAN" silently skipped Details screen | `plan_screen.dart` "ADJUST PLAN" now routes to `/onboarding/details` (was `/onboarding/stats`, bypassing the whole Details step). Stats BACK button now carries current controller values (`_weight`, `_targetWeight`, `_height`, `_bodyFat`, `_activity`) forward so edits survive round-trip. Plan targets card reads `days_per_week` from `widget.data` rather than hardcoding from goal. Closed 2026-04-24 (PR-FIX-4). |
| [MOVED to lib/core/services/CLAUDE.md] Force-unwrap `!` on map keys or `.first` on possibly-empty lists | Null-safe the map read (`(m['k'] as num?)?.toDouble() ?? 0.0`) and always guard `.first` with `isNotEmpty`. Closed 2026-04-24 (PR-FIX-2) in macros maps (home_provider + nutrition_provider), `exercise_type.first` (3 files), `sentences.last`, `options.keys.first`, `diet_plan_screen` shuffle result, `todayDay!` in train_screen. |
| "Failed to generate referral code" on fresh accounts | `referral_codes.user_id` FK pointed at `public.users(id)`, but the Invite-Friends flow fires before onboarding sync populates `public.users` — so new testers hit silent FK violations on all 5 insert retries. Migration 035 (2026-04-24, APK-test-1-batch) drops the FK and re-adds it against `auth.users(id)` + adds `UNIQUE(user_id)`. This is the ONLY user-scoped FK pointing at `auth.users` — do not normalize it. |
| Prediction card renders raw JSON | Gemini occasionally returns JSON-shaped responses (`{"predictions":[{...}]}`) even when the prompt asks for plain prose. Two-layer defense: (1) both prediction prompts in `prediction_service.dart` + `onboarding_provider.dart` carry explicit "DO NOT return JSON. DO NOT wrap in code fences. DO NOT include keys like predictions/timeframe/summary"; (2) `PredictionNotifier._sanitisePredictionText` in `ai_coach_provider.dart` detects `{`/`[`/` ``` ` prefixes, JSON-parses, extracts `summary`/`tagline`/`text`/`prediction`/`predictions[0].summary`, and WRITES the cleaned value back to Hive so the decode runs at most once per stored value. Fallback strips common JSON syntax. Never render `configBox['prediction_text']` without this guard. |
| Forgot-password link invisible on email sign-in screen | Pre-2026-04-24 the "Forgot password?" link lived only inside `_buildWelcomeView` (the pre-email, three-button landing view) in `sign_in_screen.dart`. Once a user tapped CONTINUE WITH EMAIL and committed to the email form, the link was gone — exactly when they'd realize they forgot the password. Fixed in APK-test-1-batch: duplicated the `GestureDetector → ForgotPasswordSheet.show` into `_buildEmailView` directly below the SIGN IN button, guarded by `if (!_isSignUp)` so it doesn't show during sign-up. Web redirect (to `icanbefitter.vercel.app/reset`) unchanged; in-app deep-link completion still deferred (F2). |
| Swap → "+ ADD EXERCISE" opens a picker instead of a create form | Pre-2026-04-24 the `__ADD_MODE__` sentinel in `active_workout_screen._showSwapSheet.onAdd` called `_showExercisePickerSheet` which opened the existing-exercise search sheet — user then had to pick/search and manually re-swap after. Fixed via `_openCreateAndAutoSwap`: on the sentinel, opens `CreateCustomExerciseSheet` directly. On save, auto-swaps the new exercise into the captured slot (preserves weight/rest from the original), invalidates `todayWorkoutProvider + currentPlanProvider + calendarWeekProvider`, and shows a 5s snackbar with UNDO that calls `swapExercise(index, original)` to restore. Do NOT revert to the picker path — it's a 2-tap vs 1-tap regression. |
| Custom exercise created but not visible in My Submissions | Two distinct failure modes: (a) `MySubmissionsScreen`/`_MySubmissionsBody` filters to `submitted_to_library=true`, so DRAFT exercises (user didn't tick "Share with AVYA community") never appear there — by design; (b) fire-and-forget `unawaited(SyncService.instance.syncCustomItemsNow())` can be interrupted by the user immediately backgrounding the app. The UX fix is D6 on Train: `_buildYourExercisesSection` uses `ValueListenableBuilder<customBox>` so the new exercise shows up instantly as a chip with `DRAFT`/`PENDING`/`APPROVED` badge regardless of submission state. The sync fix is observability: `_projectCustomExercise` already whitelists columns (since 2026-04-18), and `_syncCustomItems` now logs the payload in `kDebugMode` + the exercise name on errors. |
| Plan-screen preview numbers disagreed with saved profile | `_computeTargets` used to run a reduced goal-only formula (`weight × 32 + 250` for build_muscle, `weight × 2` for protein) that ignored body_fat, height, activity, and pace. Saved profile went through `BmrCalculator.calculateTargets` with all inputs → different numbers on preview vs DB. Fixed in APK-test-1-batch: preview calls the canonical `BmrCalculator.calculateTargets` with every input from `widget.data` (weight, height, DOB-derived age, sex, activity, goal, pace, target_weight, body_fat_pct). Weight delta is now `target - current` rounded, with a `HOLD` special case when abs(diff) < 0.5. Only `days_per_week` keeps a goal-based fallback (for legacy chat-flow deep-links). |
| Submissions entry confusing — two separate rows | Pre-APK-test-1-batch Profile showed "Review Community Items" (bottom sheet) + "My Submissions" (route) as two separate rows under SHARE & GROW. Testers kept tapping "Review Community Items" expecting to see their own submissions too. Consolidated into a single `Submissions` row at `/profile/submissions` which opens `SubmissionsScreen` with a 2-segment pill toggle: MY SUBMISSIONS + COMMUNITY REVIEW. Uses a handrolled pill (not Material `TabBar`) styled like `WardChip` since the old coach-screen `_buildStatusPill` primitive was deleted 2026-04-18. Legacy `/profile/my-submissions` route kept for deep-link safety — will be retired after one release cycle. |
| Logout → re-sign-in dumps user back to onboarding | Splash navigated to `/home` or `/onboarding` BEFORE `unawaited(checkAndSync())` finished restoring Hive from cloud. Router checked `configBox['onboarding_completed']` (cleared during logout) → false → sent to `/onboarding`. Fixed in APK-test-2-batch (Q1): new `RestoringScreen` at `/restoring` is the post-auth gate. SELECT `user_id, onboarding_completed_at FROM user_profile` runs in parallel with `restoreFromCloud()`. Three-way branch: onboarded user awaits restore → `/home` (15s timeout safety); mid-onboarding user resumes at first missing step; new user routes to `/onboarding/mission-brief`. Migration 036 added the explicit `onboarding_completed_at` column so the gate has a cloud-side source of truth that survives logout-wipe. Never revert to the splash-only flow — the cascade fixes B3 sync verification, F2 restore, F3 "Account not synced" coach error all in one. |
| Prediction card shows YAML-style key:value | Gemini interpreted "no JSON, no code fences" as "use a different structured shape" and emitted flat YAML: `outcome_3_months: weight_kg:77.5. body.` The first parse guard (APK Test #1, JSON-only) had an early-return at `trimmed.startsWith('{') \|\| '['` that passed YAML through unmodified. APK Test #2 / F4 extends `_sanitisePredictionText` with a third branch: regex `^[a-z_][a-z_0-9]*\s*:` finds 1+ snake_case key:value lines, picks the longest prose value (>20 chars + spaces), or falls back to stripping keys and joining values. Prompt also hardened to forbid "colon-separated keys" (not just JSON). Triple defense: prompt → JSON guard → key:value guard. Cleaned text written back to Hive so decode runs once per stored value. |
| Stale "Legs B scheduled" insight after regen + complete | `aiInsightProvider` reads from `WorkoutScheduleService.getScheduleForDate(now)` — no automatic Riverpod invalidation when Hive schedule changes. After plan regenerate or workout completion, the home AI insight card kept showing the pre-mutation insight. Fixed APK-test-2-batch / F5: explicit `ref.invalidate(aiInsightProvider)` added to `edit_profile_screen._save` regen path AND `train_provider.completeWorkout`. Day rollover already invalidated it correctly. Source-grep regression test in `test/providers/ai_insight_invalidation_test.dart`. |
| V4 plan generator drops 8→4 exercises on edit-profile regen | `edit_profile_screen.dart:1621` read `profile['detected_experience_level']` — a key NEVER WRITTEN by onboarding or profile updates. Defaulted to `'beginner'` → `VolumeFilter.targetCount(beginner, 5) = 4`. Fixed APK-test-2-batch / F6: read `profile['fitness_experience']` (canonical key written by onboarding step 04 + edit_profile itself); fallback bumped from `'beginner'` to `'intermediate'` to match onboarding's pre-selected default. |
| Logging type lost through swap | `workout_schedule_service.dart` `swapExerciseInDay` fell back to `'weight_reps'` whenever the replacement payload didn't carry `logging_type` — even when the replacement existed in `exerciseBox` or `customBox` with a different type. Symptom: swap from Handstand Hold (timed) to Handstand Pushup → KG/REPS columns instead of timed. Fixed APK-test-2-batch / F7: new `LoggingTypeResolver.resolve(exercise, exerciseLibrary, customLibrary)` checks payload → customBox → exerciseBox by name BEFORE falling back. Caller still falls back to `original['logging_type']`, then `'weight_reps'` as last resort. |
| Adding 3rd set wipes set 2's typed values | `_ExerciseCardState.didUpdateWidget` called `_disposeControllers() + _initControllers()` — full rebuild — on every set count change. Any in-flight typed text (not yet committed to provider) was wiped because the new controllers came up empty. Fixed APK-test-2-batch / F8: didUpdateWidget now appends new TextEditingControllers when count grows (preserving indices `[0..oldCount-1]`) and disposes trailing ones when count shrinks. Symmetric for all 4 controller lists (`_weightControllers, _repsControllers, _durationControllers, _distanceControllers`). Exercise-name change still does full rebuild (correct behavior for swap). |
| WK 17 wrong on home header | Home header used calendar-year week math: `((now - Jan 1) / 7).floor() + 1`. For 2026-04-25 that's WK 17 — meaningless to a user starting Phase I last week. Fixed APK-test-2-batch / F9: replaced with `WorkoutScheduleService.getCurrentWeekNumber()` (the canonical plan-relative week, already used elsewhere). |
| `?defaultDur` invalid syntax in `_projectCustomExercise` | The map literal had `'default_duration_secs': ?defaultDur,`. Originally thought to be invalid Dart causing silent custom-exercise sync failures (B3 cascade). **Update:** Dart 3.4+ added `use_null_aware_elements` lint making `?identifier` a VALID nullable-element shorthand for `if (x != null) 'key': x`. The codebase has 9+ instances of this pattern in `ai_coach_repository`, `workout_repository`, `edit_profile_screen`. APK Test #2 / F1 still replaced this specific instance with the explicit `if (defaultDur != null)` form for stylistic consistency with surrounding code. The original "no ?identifier" contract test at `test/contracts/edge_function_safety_test.dart` was retired since the syntax is now legitimate. |
| Active workout gated as PRO | Was killing the entry-level experience without driving conversions — every fitness app lets you log workouts for free. Fixed APK-test-2-batch / Q6: `featureActiveWorkoutMode` removed from `_highValueFeatures`. All `gate(featureActiveWorkoutMode, ...)` calls in `train_screen.dart` replaced with direct navigation. Constant kept as `@Deprecated` so legacy callers don't break. PRO conversion levers unchanged (phases 2–12, AI coach unlimited, photos, weekly report, voice, etc.). Lock-down test in `test/subscription/high_value_features_test.dart`. |
| Referral code lifetime ambiguous to receiver | APK Test #2 / Q4 locked: codes expire 7 days from generation (single 7-day clock everywhere). Migration 037 added `referral_codes.expires_at TIMESTAMPTZ DEFAULT (now() + interval '7 days')`. `getOrCreateReferralCode()` filters by `.gt('expires_at', now())`; `regenerateReferralCode()` forces a fresh 7-day code (used by REGENERATE button when the current code expires). `redeem-referral` Edge Function v10 validates: format → expiry → self-referral → receiver signup ≤ 7 days → idempotency (UNIQUE on `referee_id`) → `redeem_referral_atomic` RPC. Both sides get +7 days PRO. WhatsApp share copy: "Use my code AVYA-XXXXXXXX within 7 days → 7 days of PRO, free." |
| [MOVED to lib/features/train/CLAUDE.md] Phase 2-12 invisible to free users | Train screen previously capped the week selector at 4 weeks — free users couldn't see what they were paying for. APK Test #2 / Q7 extends the selector to 12 weeks (3 phases) with PHASE I / II (PRO) / III (PRO) headers + lock glyph on weeks 5–12 for free users. Tap any locked week → `/train/preview?phase=II&week=5&day=1` renders a real workout via `previewPlanProvider` (calls `PlanGenerator.instance.generateV4()` with the user's actual profile — goal/equipment/days/experience). State-aware banner: "Complete Phase I to unlock Phase II — your AI coach generates the next 4 weeks the moment you finish." Free users see UPGRADE TO PRO bottom CTA + cross-link to `/train/roadmap`. |
| Today card title truncates and macros are wasteful | "LEGS B · Rela…" was getting cut off because the right-column macro grid ate too much horizontal space, AND each macro tile took 3 vertical lines (eyebrow / number / bar). APK Test #2 / Q9 fixes both: column ratio shifted from ~50/50 to 60/40 (left wider). Title gets `maxLines: 2` — never truncates. Each macro tile collapses to 2 lines: row 1 has `EYEBROW` (mono 10sp left) + `1820/2983` (Fraunces 16sp right-aligned), row 2 has the bar full tile width. Number format: `1820/2983` (no spaces around `/`), `kg` lowercase, `k` abbreviation on STEPS target ≥ 1000. Completed state: DONE chip + VIEW CARD button paired in left column (anchored to workout, never floats over macros — was the obs #15 ambiguity). Best-lift on its own line below. Eyebrow stays inside left column per user's explicit "don't add a top-row eyebrow" feedback. |
| Receipt summary line hides per-set progression | Old format was cumulative: `Dumbbell Curl 4 sets · 21 reps · 23kg`. The 23kg was best across sets, hiding the actual progression (10×10, 15×7, 20×5, 22.5×3). APK Test #2 / Q10 changes per-exercise rendering to bracketed chip Wrap. Each chip: `10 kg × 10 reps` (weight_reps), `× 10 reps` (bodyweight), `+10 kg × 8 reps` (weighted bodyweight), `60 secs` (timed), `15 secs · 2 km` (cardio). 1px `line2` border, 6dp radius, 12sp DM Sans. Wrap when full. Quote line is now category-aware: 30 quotes in `assets/data/workout_quotes.json` tagged by category (push/pull/legs/core/full_body/cardio/arms/general). `QuotePicker.pickForCategory(category, seed)` filters by category, falls back to `general`. Selection seeded by workout_log_id so the same workout always gets the same quote. |
| Train empty-state cards waste 280dp | MY TEMPLATES + YOUR EXERCISES empty cards were tall WardCard with centered icon + title + subtitle (~140dp each, ~280dp total wasted vertical space). APK Test #2 / Q11: replaced with single-line hint text below section header: `No templates yet — tap + CREATE to build one.` / `No custom exercises yet — tap + CREATE to add one.` Gold-accent + bold `+ CREATE` word inside the hint is tappable via `GestureDetector`, calls the same handler as the section header pill. Two affordances, both compact. Saves ~250dp total. |
| Auth sub-views (email form, phone OTP, forgot password) had no AVYA branding | Welcome screen had a beautiful gold-ringed AVYA seal + tagline; tap "Continue with email" and the next screen was a bare form — broke visual continuity (user obs #23). APK Test #2 / Q3: new `AuthHeader` widget at `lib/features/auth/widgets/auth_header.dart` adds compact letterhead (36dp gold-ring AVYA seal + mono "RECRUIT REGISTRY" eyebrow + 60dp gold rule + Fraunces title) to all sub-views. Welcome retains its full hero — only sub-views inherit the compact header. |
| Privacy/Terms standalone modal interrupts post-logout flow | Pre-APK-test-2 the `TermsModal` fired on every cold launch when Hive was wiped, even for returning users who'd already accepted. APK Test #2 / Q2: (a) Welcome screen has inline footer "By continuing, you agree to our [Privacy Policy] and [Terms]" linking to external URLs; (b) signup form has a pre-checked checkbox above SIGN UP button gating the button (uncheck = button disabled); (c) returning users have `users.terms_accepted_at` synced FROM cloud TO Hive on `_ensureLocalUser`, so `TermsModal` skip gate (`accepted != null && version == AppConstants.termsVersion`) succeeds without them needing to re-accept. Pre-checked default is the common Indian fintech pattern (CRED, Zerodha, Razorpay). Tapping SIGN UP with a visible-and-tickable checkbox is the affirmative action under DPDP. |
| Founder credibility lost in cold sign-up flow | New users had no idea who built AVYA or why the plans were trustworthy — just a generic onboarding flow. APK Test #2 / Q5: new step 00 `MissionBriefScreen` at `/onboarding/mission-brief` inserted between sign-up and Identity (new users only — returning users skip via the restore flow). 96dp gold-ringed founder photo (`assets/founder/upendra.jpg`, converted from HEIC) + locked copy: *"I built AVYA because every fitness app I tried treated me like a number. The plans you'll see in this app aren't algorithmic guesses — they're shaped by 14 years of military training and certified coaching practice. The AI executes the playbook. The playbook is mine. Jai Hind!"* Italic-gold emphasis on "aren't algorithmic guesses" and "The playbook is mine." Subtle inline Instagram link "Daily wins on Instagram → @icanbefitter" (handle is gold + underlined, opens IG app via `instagram://user?username=icanbefitter` with web fallback). Single primary CTA `CONTINUE →` (BEGIN ENLISTMENT already lives on Welcome). Plus micro-references on Plan screen ("Plan shaped by 14 years of disciplined coaching." below REPORT FOR DUTY) and AI coach (`is_first_ever_message: true` flag passed in context for first-message system-prompt prefix). |
| Orphan `public.users` blocks every server write for re-signups | `public.users.id` had no FK to `auth.users(id)`. When an auth user was deleted (test cleanup, account wipe), the `public.users` row was left orphaned — and `public.users.email` is `UNIQUE`, so the next signup with the same email failed `_ensureLocalUser` upsert with **23505 (unique_violation)** silently. All FK-bound writes (`user_profile`, `ai_coach_interactions`, `user_custom_exercises`, etc.) then failed with **23503 (foreign_key_violation)** because their `user_id` FK pointed at `public.users(id)` which never got created. Every error swallowed by `unawaited()` fire-and-forget. APK Test #3 / Bug A (2026-04-26): user `upendra.prasad@thinkingcode.com` had 0 server rows after 48h of testing despite onboarding completing locally. Migration 039 fixed: (a) `ALTER TABLE public.users ADD CONSTRAINT users_id_fk_auth FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE`, (b) `handle_new_auth_user()` trigger that auto-INSERTs into `public.users` on every `auth.users` INSERT, (c) `_ensureLocalUser` catch block now posts 23505/23503 to `log-client-error` Edge Function instead of silent debugPrint. Verified end-to-end: orphan auth-delete → public.users CASCADE → fresh signup with same email → success. Regression test: `test/contracts/auth_provider_error_surfacing_test.dart`. |
| Edit Profile Save writes Hive but never `user_profile` in Supabase | `edit_profile_screen._save` called `userProfileProvider.notifier.updateProfile(updates)` → `UserRepository.updateProfileFields(fields)` → Hive only. Snackbar says "Saved" but cloud row stays empty/stale forever, breaking AI coach context (rolling-context Edge Function reads from cloud, not Hive). APK Test #3 / Bug B (2026-04-26). Fix: add `unawaited(SyncService.instance.syncProfileNow(supaUserId))` + `unawaited(SyncService.instance.pushSnapshot())` after `recalculateTargets()`. Regression test: `test/sync/edit_profile_sync_test.dart`. Per CLAUDE.md §15 fire-and-forget pattern — every mutation must propagate. |
| AI hallucinates day-of-week + invents stats | ai-proxy v47 had no day-of-week injection in the system prompt; Gemini guessed. APK Test #3 / Bug C (2026-04-26): user asked "What's my workout today?" on Sunday — got back "today, Monday" + invented "100% skip Monday workouts" (user had 0 `workout_logs` rows). Fix: ai-proxy v48 prepends `Today is <Day>, <YYYY-MM-DD> (IST). When the user asks about "today", use this exact date and weekday.` to system prompt + appends explicit anti-fabrication rule forbidding percentages/trends about user behavior unless `recent_logs` / `coach_notices` / `nutrition_trend_7d` / `meals_today` actually has data supporting it. Regression test: `test/contracts/ai_proxy_day_injection_test.dart`. |
| [MOVED to lib/features/nutrition/CLAUDE.md] Diet plan over-delivers protein 2-3× target | Plan C's anchor-protein-per-meal algorithm fixed the 32% deficit (the original APK Test #3 bug) but introduced over-delivery: cut 243%, maintain 276%, build 183%. Excess protein costs calories, makes meals expensive, breaks the calorie balance. Three-part fix in commit `c7288d3`: **Part A — smart Pass 2 filler exclusion** (when slot anchor already meets ≥90% of slot's protein target, exclude 'pulses' for lunch/dinner and 'dairy' for breakfast from filler pool). **Part B — Pass 4 surplus trim** (mirror of Pass 3 inverted: if total > 115% target, swap highest-protein non-anchor item for lower-protein filler in same calorie band ±20%, up to 12 swap iterations, anchors protected). **Part C — Pass 1 anchor cap** (anchor protein hard-capped at 1.5× slot's protein target; random pick from in-band pool; fallback: highest-under-cap, NOT smallest-above-floor — that one starves veg slots). Test assertions pin protein in [95%, 115%] band. All 4 archetypes (cut 98% / maintain 112% / build 96% / vegan 102%) now in band. |
| `_foodLibraryVersion` re-seed mechanism missing | Pre-APK-test-3 the `seed_service.dart` had version-check infrastructure for exercises (`_exerciseLibraryVersion = 4` + `_exerciseVersionKey`) but NO equivalent for foods. So a bumped `food_database.json` would never re-seed on existing installs — they'd stay on the old 93 items forever. APK Test #3 / V2 fix: added `_foodLibraryVersion = 2` + `_foodVersionKey` constants + `needFoodUpgrade` check in `seedIfNeeded()` mirror of the exercise pattern + version write in `_seedFoods()`. Bumping `_foodLibraryVersion` triggers automatic re-seed on next app launch via `putAll()` (idempotent). |
| food_database from 93 to 1431 items (V2 expansion) | APK Test #3 (2026-04-27): user observed 93 items felt sparse for "premium" experience. Multi-source build: 93 preserved seed + 699 manual_indian_staples_v2 + 228 manual_western_v2 + 225 openfoodfacts_india_v2 + 186 manual_fitness_v2 = **1431 total**. Build script: `.claude/build_food_db_v2.js` (Node 18+, no deps). External APIs (USDA SR Legacy, OpenFoodFacts) were rate-limited mid-build; the manual_western_v2 dataset filled the gap with the same coverage. Schema additions: `is_veg` + `is_vegan` booleans on every item (`is_vegan=true` implies `is_veg=true` — asserted in test). Migration 041 mirrors V2 to cloud; applied as 11 chunks (`supabase/migrations/041_chunks/`) because the 31524-line single-file version exceeds the MCP `apply_migration` payload limit. Each chunk idempotent (`ON CONFLICT (id) DO UPDATE`). Validation tests in `test/nutrition/food_database_v2_test.dart` — 6 tests pinning count ≥1000, all required fields, no duplicates, 12 critical anchor names, 93 seed preserved with original IDs (F0001-F0093), `is_vegan ⇒ is_veg`. Diet plan archetype tests still in [95%, 115%] band — V2 didn't break the algorithm. **Lesson learned during V2 build**: haiku model unreliable for novel SQL constraint reasoning (it hallucinated "invalid arrays" + "non-hex UUIDs" in chunks 03/04 — both false; chunks were valid `'{}'::text[]` + well-formed UUIDs). Always use sonnet+ for migration apply tasks. |
| RankService idempotency relies on UNIQUE(user_id, rank_code) | APK Test #3 / Plan B forever-friend system: `rank_promotions` table has `UNIQUE (user_id, rank_code)`. `RankService.evaluateAndPromote()` is fire-and-forget on every workout completion + splash cold start — same rank can fire 10 times, but only the first INSERT succeeds (others get 23505 which the service catches). Don't drop the UNIQUE constraint. Don't add `ON CONFLICT DO UPDATE` either — promotions are write-once historical events, not state. The denormalized `user_profile.current_rank_code` is what gets updated. Server-side cron `evaluate-rank-promotions` (migration 040, 18:30 UTC nightly) catches users who don't open the app — re-runs the same idempotent logic from Postgres data. Streak math context: `WorkoutRepository.calculateCurrentStreak()` walks back through `schedule_<date>` keys; rest days are invisible (don't break, don't increment); only `status=='completed'` increments. Streak freezes are HIVE-ONLY (`streak_freezes_available`/`..._used_dates`/`..._last_refill` never leave device). Rank gate evaluation MUST call `calculateCurrentStreak()` fresh — never trust a cached number. |
| Phase II/III roadmap preview shows 6/7/8 exercises while Today card shows 8 | `previewPlanProvider` in `train_provider.dart` had a fallback default of `'beginner'` for `fitness_experience` when reading `user_profile`. Onboarding's pre-selected default is `'intermediate'`. So users who DIDN'T explicitly set experience to beginner saw inconsistent previews. APK Test #3 / Plan B Task 12 (commit `21b9498`): bumped fallback to `'intermediate'`. Today card already used the actual stored value, so it was correct; only the preview was substituting beginner default. Closes the 4-vs-6-vs-8 mismatch flagged in the original obs 1 brainstorm. |
| [MOVED to lib/core/services/CLAUDE.md] Hive `path_provider` MissingPluginException in unit tests | `HiveService.init()` calls `Hive.initFlutter()` which uses `getApplicationDocumentsDirectory` from path_provider — fails in pure unit tests with `MissingPluginException(No implementation found for method getApplicationDocumentsDirectory on channel plugins.flutter.io/path_provider)`. Fix in tests that need Hive boxes (e.g., `test/ai_coach/meals_today_snapshot_test.dart`): add `TestWidgetsFlutterBinding.ensureInitialized()` + mock the channel via `TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger.setMockMethodCallHandler(MethodChannel('plugins.flutter.io/path_provider'), (call) async => tempDir.path)` BEFORE calling `Hive.init(tempDir.path)` and `HiveService.instance.init()`. Required pattern for any Hive-touching unit test. |
| APK Tests #4–#6 architectural patterns (2026-04-27 to 2026-05-01) | Full retrospectives + smoke notes in `docs/superpowers/notes/`. Key patterns introduced: (1) **WorkoutWriteService** (`lib/core/services/workout_write_service.dart`) — sole writer for all workout Hive mutations; per-(date,exerciseName) mutex via `synchronized`; 60s per-set dedup; 3-tier cloud sync (workout_logs + workout_log_exercises + workout_log_sets). (2) **NutritionWriteService** (`lib/core/services/nutrition_write_service.dart`) — sole writer for all nutrition mutations; per-source counter increment; per-item cloud sync to nutrition_log_items. (3) **IST-throughout rule** — all date keys, cloud `date` columns, counter resets use IST (UTC+5:30) via `lib/core/utils/ist_date.dart` helpers. (4) **StatSnapshotService** (`lib/core/services/stat_snapshot_service.dart`) — auto-snapshots on onboarding + rank promotions; `user_stat_snapshots` table (migration 044). (5) **WardRankInsignia / WardRankPill** — CustomPaint for 11 Indian Navy ranks + accordion pill at top of Profile replacing the Edit Profile button. (6) **11-rung rank ladder** — Lt inserted at ordinal 7 (W130, 2 thick gold stripes); migration 045 applied; hybrid sailor/officer gate model (streak-primary sailor, completion-rate-primary officer). (7) **4 pre-existing test failures** — `rank_service_test` LS/PO/SubLt static gate mirrors + `sync_gap_test` DeleteNutritionLogNotifier; not caused by Test #4–#6 work; deferred to Test #7. Never write directly to Hive workout or nutrition boxes — always go through the Write Services. |
| WriteService field rename silently breaks consumers — receipt shows "0 sets", AI snapshot misses meals/exercises | The Test #6 `WorkoutWriteService` rewrite renamed Hive `exlog_*` keys to align with cloud column names (`set_number`, `sets`) but several readers still queried the old names (`sets_completed`, `sets_detail`). Receipt rendered "0 sets" for everything (Test #7 ship). APK Test #8 contract tests (`test/contracts/<x>_write_to_read_contract_test.dart`) surfaced 4 MORE production drifts in `ai_coach_repository.dart` — `_getMealsToday` / `_getNutritionTrend7d` / `_getThisWeekWorkouts` / `_getPersonalRecords` all iterated `box.values` and filtered by missing `'id'` / `'type'` fields, silently dropping every row written by the new WriteServices for ~10 days. **Class fix pattern:** if a consumer iterates `box.values` and string-filters a field on the value, switch to `box.toMap()` + Hive key-prefix check (`nlog_*` / `exlog_*`). **Doc:** see CLAUDE.md §15 "Hive field-name contract" sub-section for the consumer enumeration. **Process gate:** every WriteService field rename must update consumers in the same PR + add or update a round-trip test in `test/contracts/`. |
| APK Test #8 (2026-05-03) — receipt fix, rank chip relocation, sharp+flush profile cards, contract tests, wake-time push | Branch `feat/apk-test-8-batch` `1cbc78e` (not yet merged). 5 themes shipped: (A) `WorkoutReceiptData.fromExerciseLogs` reads `set_number`/`sets` with legacy fallback. (B) Rank chip moves into banner-overlap row (avatar L · `SD2` chip CENTER · GO PRO R); tap opens `RankServiceRecordSheet` with current rank + streak + freeze count + last 5 promotions + roadmap link; inline `WardRankPill` removed. (C) Sharp 6dp outer corners + flush stack on Daily Goals → My Targets profile cards; edit-profile defaults `'non_veg'`→`'veg'` and session_duration `?? 90`. (D) `test/contracts/workout_write_to_read_contract_test.dart` + `nutrition_write_to_read_contract_test.dart` lock writer↔reader contracts; CLAUDE.md §15 "Hive field-name contract" sub-section enumerates consumers. (E) Migration 046 splits morning-alert delivery into `morning_alert_deliver_late` (`*/15 22-23 UTC`) + `morning_alert_deliver_early` (`*/15 0-6 UTC`); new `morning_alert_pick_quarter` RPC filters users by `wake_up_time` matching current 15-min IST quarter, NULL fallback to 07:00 IST; morning-alert v21 deployed. **Spec correction:** `AppRadius.sharp` = 2dp, `AppRadius.card` = 6dp — Theme C used `AppRadius.card` to match the documented "6 dp" visual target. Don't auto-substitute "sharp" for "6 dp" in future specs without checking `lib/core/theme/spacing.dart`. |
| APK Test #11 (2026-05-04) — full-sweep audit, 13 themes, merge `c57fdaa` | Merged to main 2026-05-04. 5 parallel audit agents + 1 deep-dive food-logging agent surfaced 27 P0s + 13 P1s; closed all 13 themes in one batch. Migrations 047 (additive: streak_freezes columns + notifications_inbox + saved_diet_plans) + 049 (5 community FKs CASCADE→SET NULL + account_deletion_log + onesignal_player_id). 12 Edge Functions deployed including new `delete-account` (DPDP §17 hard-delete with Razorpay-abort + OneSignal unsub + Storage purge + audit log). New singletons: `WaterTargetService` (replaces 4 hardcoded 3000 ml sites; floor 2.5 L per founder direction; manual override at `userBox['water_target_override_ml']`), `SubmissionsRepository` (Theme K — 4 widgets extracted from direct Supabase). New shared module `_shared/captain_manual.ts` (Theme G1 — 6 proactive triggers now Captain-voiced via `captainPrompt('proactive')` with English fallback). Plan generator V4 cascade gaps: 38 → 0 (Task 4.2 expanded library by 9 exercises + fixed 10 attribute gaps). Test suite: **1057 pass / 0 fail / 11 skip** (was 919 / 4 fail — 4 pre-existing fails closed). Source: `docs/audit/2026-05-04/` + `docs/superpowers/plans/2026-05-04-apk-test-11-plan.md`. APK build deferred per founder ("after few observations"). |
| Hardcoded `3000` for water target | Read via `WaterTargetService.instance.currentTargetMl()` or `ref.watch(waterTargetProvider)`. Floor 2.5 L (founder direction 2026-05-04 — no one should be told they only need 2 L), ceiling 4.0 L. Manual override stored at `userBox['water_target_override_ml']`. Pre-Test-#11 the value was computed correctly during onboarding + synced to cloud, but 4 UI sites (`hydration_card`, `home_screen`, `nutrition_screen`, `water_quick_sheet`) all hardcoded 3000 ml — a 60 kg user saw "100% at 3 L" while target was 2.1 L. Now wired end-to-end. |
| [MOVED to supabase/migrations/CLAUDE.md] `null user_id` rows from migration 049 pseudonymization | After Test #11 migration 049, FKs on `user_custom_exercises`, `user_custom_foods`, `community_reviews` (note: column is `reviewer_id`), `food_corrections`, `promo_code_uses` are `ON DELETE SET NULL`. When an account is hard-deleted via `delete-account`, these rows survive with `user_id = NULL` ("deleted user" pseudonymization for community signal preservation). **Read consumers MUST tolerate NULL.** Already-fixed in Test #11 cleanup: `promote-community-item` now guards `if (source.user_id)` before `notifySubmitter`. Any new consumer that joins on user_id must add the same guard or the query risks silent skip / false negative. |
| Hand-rolled `'YYYY-MM-DD'` from device-local `DateTime.now()` | Always use `istDateStr(DateTime.now())` from `lib/core/utils/ist_date.dart` (client) or `istDateStr()` from `supabase/functions/_shared/ist_date.ts` (Edge Functions). Test #11 B1+B2+M3 swept 8 sites; Test #11 final cleanup caught 3 more in `conversational_log_handler.dart` (sleep / measurement / workout draft), `nutrition_provider.relogSavedMeal`, `search_mode_body._relogFromHistory`. Symptom: users in IST 00:00–05:30 silently write prev-day rows that mismatch IST-correct readers (`_getMealsToday`, counter resets, etc.). Class fix pattern: search for `DateTime.now().toIso8601String().substring(0, 10)` and replace; UTC timestamps for `created_at` / `updated_at_ms` stay UTC. |
| [MOVED to lib/features/ai_coach/CLAUDE.md] AI breakdown card "didn't log" but data was saved | Test #11 L1 (founder-reported). Data WAS saved correctly to Hive + cloud with `items[]`. Bug was UI: no snackbar / haptic / toast on save → card just disappeared → user assumed failure. Fixed: `AiBreakdownNotifier.saveMeal` now returns `Future<WriteResult>`, card pops `Meal saved ✓` snackbar with `HapticFeedback.lightImpact()`. Plus `_saving` guard prevents double-tap race. **Pattern lesson:** every save action that mutates data MUST give the user a confirmation signal, even if the success is "just" a card vanishing. Compare canonical patterns at `scan_meal_section.dart:445-465` and `food_search_sheet.dart:519`. |
| `FoodLogNotifier.logFood` writes Hive without `items[]` (Theme C deferred Test #10) | Closed in Test #11 / Theme C1. The legacy `nutrition_provider.dart:790-835` wrote a flat-totals `nlog_*` row with no `items[]` array; cloud `_syncNutritionLogs` projection silently wrote 0 rows to `nutrition_log_items` for those rows; AI coach + weekly-report saw "no items" server-side. `FoodLogNotifier.logFood` now delegates to `NutritionWriteService.logMeal` with a single `FoodItem` built from the food map (per-100g macros scaled by `quantityG / 100`). Both consumers (`search_mode_body` tile-tap + `conversational_log_handler._logFood`) now produce items[] correctly. |
| [MOVED to lib/features/nutrition/CLAUDE.md] Counters increment on save not API call | Pre-Test-#11: `UsageCounterService.increment(...)` was called from inside `NutritionWriteService.logMeal`. A free user analysing 50 AI-text meals without saving saw "50 remaining" while the server's `trg_food_text_rate_limit` Postgres trigger had counted every Edge Function call. Test #11 M1+M2: counters now increment at the API-call site (`food_logger_section._analyse`, `ScanMealNotifier.scanImage`, `cart_auditor_section.analyseCart`, `tool_dispatcher._executeLogMealByText`). Save sites no longer increment. Cart auditor counter (previously dead code — no callsite passed `NutritionWriteSource.cart` to `logMeal`) is now wired. |
| Cross-device restore loses freezes / inbox / diet plan | Closed in Test #11 / Theme A. Pre-Test-#11 these were Hive-only: paying users lost streak freezes on every reinstall, notifications inbox vanished, saved diet plan vanished. Migration 047 added cloud columns/tables; `SyncService.{syncFreezes, syncNotificationsInboxEntry, syncSavedDietPlan}` push on every Hive mutation; `restoreFromCloudForUser` pulls all 5 surfaces (freezes + inbox + diet plan + rank_promotions + coaching_notes) atomically + folds `SubscriptionService.verifyFromServer(force: true)` as the final step. See §15 "Restore-completeness sync" for the full method list. |
| OneSignal player_id never written by client | Closed in Test #11.1 (2026-05-05). `auth_provider._syncOneSignalPlayerId(userId)` now reads `OneSignal.User.pushSubscription.id` after `OneSignal.login()` and upserts to `user_progress.onesignal_player_id`. Fire-and-forget (failure logs but never blocks auth). Idempotent: same id → no-op upsert. `delete-account` Edge Function's push-unsub path now has a real player_id to act on. |
| User-scoped data leaks across signOut → signUp via shared `configBox` | Closed in Test #10.1 (6 critical keys) + Test #11.1 (25 sweep keys, 2026-05-05). Every user-scoped key now lives in per-user `userBox` via `MigratedKey.read/write/delete` (in `lib/core/services/migrated_key.dart`). `UserConfigMigrator` (gated by `migrationBox['config_to_user_migration_v2_done']`) one-shot-migrates legacy configBox values into userBox on first authenticated launch post-update. Two keys deliberately stay shared and are pinned by regression test in `test/safety/config_to_user_migration_test.dart`: `pending_referral_code` (pre-auth → post-auth crossing) + `logout_in_progress` (set during signOut tear-down, read by `main.dart` cold-launch bootstrap before any session opens). When adding a new user-specific Hive key, default to `MigratedKey.write/read` — never write directly to `HiveService.instance.configBox` for anything user-scoped. Bumping `UserConfigMigrator._flagKey` (e.g. `_v2_done` → `_v3_done`) is required to re-trigger migration on devices that already ran a prior version. |
| `istDateStr(istNow().subtract(...))` double-shifts +5:30 | `istNow()` returns a DateTime that is already shifted to IST wall clock (with `isUtc=true` flag). Passing it back into `istDateStr` triggers a second +5:30 shift inside `istDateOf`. Symptom: code that should return today's IST date returns IST+5:30 (next day after ~18:30 IST). Found in `ai_coach_repository._getYesterdayWorkout` + `_getWeekLookahead` (Test #11.1 fix). Always pass raw `DateTime.now()` (or any local/utc DateTime) to `istDateStr` — it handles the IST conversion internally. Reserve `istNow()` for cases where you need the IST instant directly (date math, comparison) without then re-formatting through `istDateStr`. Tests must use the same pattern: `istDateStr(DateTime.now())`, never `DateTime.now().toIso8601String().substring(0, 10)` (UTC date) or `istDateStr(istNow())` (double-shift). |
| `formatDateKey` was UTC/local, not IST | Closed in Test #12 / Task A-1 (2026-05-06). `lib/core/utils/date_utils.dart formatDateKey()` extracted `date.year/.month/.day` directly — UTC date for UTC inputs, local for local. Disagreed with `WorkoutWriteService.istDateStr` (always IST) at the IST midnight boundary: rows written under IST May 5 indexed under formatDateKey's "May 4" → receipt for May 4 contained May 5 exercises (founder feedback APK 11.1). Fix: helper now `return istDateStr(date);` — single edit fixes all 14 callsites. Pinned by `test/contracts/format_date_key_ist_test.dart` (UTC 20:00 May 4 → "2026-05-05"). When introducing a new date helper anywhere in `lib/core/utils/`, route it through `istDateStr` not raw `.year/.month/.day`. |
| PRO upgrade pills don't unlock after payment | Closed in Test #12 / Themes Critical C-1..C-4 (2026-05-06). Two stacked failure modes: (a) `verifyFromServer` was downgrading the optimistic `isPro=true` whenever it ran before the Razorpay webhook fired (within seconds of payment, a splash-time verify or gate check would call `_downgradeLocally`); (b) 25+ widgets called `SubscriptionService.instance.isPro()` directly instead of `ref.watch(subscriptionInfoProvider).isPro` — they cached false at first paint and didn't rebuild when Razorpay success invalidated the provider. Fix part 1: 10-min `paymentInFlightUntil` grace window written by `markPaymentInFlight()` (called from `_handlePaymentSuccess`); `verifyFromServer` checks `isPaymentInFlight` and suppresses downgrade while open. Fix part 2: build-method callsites in `week_selector` / `phase_roadmap_screen` / `preview_workout_screen` / `settings_screen` / `profile_screen.prediction_card` swept to `ref.watch(subscriptionInfoProvider).isPro`. Service-layer / async / callback callsites stay direct (not reactive surfaces). New `isVerifying` field on `SubscriptionInfoData` exposes the grace window so the profile dossier shows "CONFIRMING ⟳ AWAITING WEBHOOK CONFIRMATION" copy until webhook arrives or grace expires. Pinned by `test/subscription/payment_grace_window_test.dart`. |
| Receipt for completed workout shows other days' / sessions' exercises | Closed in Test #12 / Theme A (2026-05-06). Two distinct problems: (a) `formatDateKey` was UTC/local-not-IST so date keys disagreed with WriteService — see entry above; (b) `WorkoutReceiptData.fromExerciseLogs(date)` aggregated every exlog logged that IST date with no per-session scoping. Fix part 2: `WorkoutWriteService.logExercise` now stamps `workout_log_id` (default `wlogKey(date)`) on every exlog row; `fromExerciseLogs(date, workoutLogId: ...)` filters by that field when supplied. Legacy rows without the field always pass through (graceful upgrade — no historical receipts vanish). Multi-session days get `sessionLabel: "SESSION N"` automatically. Pinned by `test/contracts/receipt_scoping_test.dart`. |
| Active-workout swap kept old `logging_type` | Closed in Test #12 / Theme D-1 (2026-05-06). `train_provider.swapExercise` was a pure state replacement — `newExercises[i] = newExercise` with no logging_type validation. If the picker handed back an exercise without `logging_type` (or with the wrong one), the slot UI kept rendering the OLD columns. Swap timed → weight+reps left the slot showing DURATION seconds. Fix routes the incoming exercise through `LoggingTypeResolver.resolve(...)` (the same resolver introduced in Test #2 / F7 for the schedule-screen swap). Pinned by `test/train/swap_logging_type_test.dart`. The same swap path also used to keep stale `checkedSets` / `warmUpSets` / `setInputValues` for the swapped index — now wiped on swap so OLD exercise's checked sets don't get logged under the swapped-IN exercise's name. |
| Train expanded view + Receipt rendered exercises in different formats | Closed in Test #12 / Theme E (2026-05-06). Train screen showed single summary line (`4 sets · 33 reps · 110 kg`); receipt showed per-set chip Wrap (`20 kg × 10 reps` × N). Two different widgets, easy to drift. New shared primitive `lib/shared/widgets/wardroom/ward_set_chips.dart` (`WardSetChips`) is the single source of truth — exported via Wardroom barrel. Both surfaces now delegate to it. Pinned by `test/widgets/ward_set_chips_test.dart` (chip-text format for all 5 logging types + empty/fallback paths). When adding a new "what was logged" rendering surface, route through `WardSetChips`; do not inline a chip widget. |
| Active workout REPS pre-filled with summed value (e.g. 85) instead of per-set | Closed in Test #15.3 / Bug 1 `a8f1c2` (2026-05-12). After Test #6 WorkoutWriteService rewrite, `reps_completed` on Hive `exlog_*` became the SUM of per-set reps; `weight_kg` became max-across-sets. `_getLastPerformance` at `lib/features/train/providers/train_provider.dart:74` kept reading them as per-set values, then `active_workout_screen._initControllers` wrote the sum into every set's TextEditingController. Sibling `lastSets` read `sets_completed` but writer at `workout_write_service.dart:617-619` normalizes incoming `sets_completed` → `set_number` so reader was silently null. Fix: reader extracts `log['sets'][0]['reps']` + `[0]['weight_kg']` (first set) with `set_number` ?? `sets_completed` fallback; legacy rows without `sets[]` fall through to null. Contract test in `test/contracts/last_performance_per_set_contract_test.dart` pins SEMANTICS (sum vs first-set) including malformed sets[] shapes (empty, non-map elements, null reps/weight). |
| AI text food analysis / AI coach logMealByText shows "AI is temporarily unavailable" | Closed in Test #15.3 / Bug 2 `7c4e1a` (2026-05-12). Yesterday's Test #15.1 single retry @ 1.5s on Edge Function 502/503 was undersized — Supabase Edge Function cold-start can take 20+ seconds. Bumped `lib/core/services/supabase_service.dart callFunction` to 2 retries on schedule `[1500, 4000]` ms (~5.5s window). Extracted `retryColdStart` as `@visibleForTesting` helper that takes an injectable invoker so behavioral tests can simulate runtime semantics (`test/contracts/edge_function_cold_start_retry_behavioral_test.dart` covers 2×502+200, 502+500-stops-retry, 400-immediate-rethrow, 3×502-exhaust). Each retry attempt emits `ErrorTelemetry.logEvent('edge_function_cold_start_retry', ...)` for ops visibility. 401-recursion guard from 2026-04-07 preserved. |
| AI coach reports planned workout when user partially completed | Closed in Test #15.3 / Bug 3 `a13a01` (2026-05-12). `_getTodayWorkout` at `lib/features/ai_coach/repositories/ai_coach_repository.dart:1335` read `workoutBox.get('schedule_<istToday>')` and emitted the planned `exercises[]` verbatim. When user partially completed, schedule.status flipped to `completed` but `exercises[]` stayed at the full plan, so Gemini reported it as completed-as-prescribed. Reader now reads from `exercise_log_index_<istToday>` + `exlog_*` rows via `_buildWorkoutSnapshotForDate` helper (LOGGED only — Option A locked with founder). Symmetric fix applied to `_getYesterdayWorkout`. `top_set_weight_kg` omitted for non-weighted logging types (`bodyweight_reps` / `timed` / `cardio`) so Gemini doesn't say "0 kg top set on Hanging Leg Raise." Pinned by `test/contracts/today_workout_reads_logged_contract_test.dart`. Test #8 Theme D drift audit didn't cover this method (no field-rename trigger — the structural plan-vs-logged mismatch was invisible). |
| Template-day workout name reverts to plan-generator default after fresh install | Closed in Test #15.3 / Bug 4a `9e2c1a` (2026-05-12). Cloud `scheduled_workouts` has only `template_id` — no `workout_name` / `exercises` columns (by design). Pre-fix, `SyncService._restoreScheduledWorkouts` pulled the row without JOINing to `workout_templates`, so on fresh-install restore the Hive `schedule_<date>.workout_name` was repopulated by plan-generator's default (e.g. "PUSH A") even though founder had assigned "Leg Day A" template the day before. Receipt + logged exercises rendered correctly (came back via `workout_log_exercises` restore) but day card header was stale. Fix: restore embeds `template:template_id (name, workout_type, template_exercises(*))` via PostgREST and hydrates Hive's `workout_name`, `workout_focus`, `exercises[]`, `type='custom_template'`. Plan-generator entries (template_id IS NULL) left untouched. **Theme A Restore-completeness sync class** (same shape as Test #11's freezes/inbox/diet-plan gaps) — any Hive surface paying users lose on reinstall must have cloud table + sync write + restore method + contract test. Pinned by `test/contracts/restore_template_schedule_test.dart`. |
| Scheduling a template onto an already-completed day silently fails | Closed in Test #15.3 / Bug 4b `8f3d22` (2026-05-12). `WorkoutScheduleService.assignTemplateToDate` at `:1441` had a history-guard at `:1455` that silently `return`ed when `existingMap['status'] == 'completed'`. No snackbar, no telemetry. Caller's "Scheduled for N days" toast was a lie. Fix: return type flipped to `Future<AssignTemplateResult>` with sealed subclasses `AssignTemplateOk` + `AssignTemplateRejected(reason)` and enum `AssignTemplateRejectionReason { alreadyCompleted, templateMissing }`. Callers (`train_screen._scheduleTemplate`, `template_builder_screen._buildScheduleRow`) collect rejected dates per reason and surface "N days already completed" snackbar before the success toast (suppressed when all skipped). `tool_dispatcher._executeScheduleTemplate` already pre-checks status at dispatcher level — type change is backward-compatible. Telemetry op_type `template_assign_rejected_completed`. Pinned by `test/contracts/template_schedule_completed_day_test.dart`. |
| Timed exercises (Handstand Hold, Jump Rope) render "0s" on day card after restore | Closed in Test #15.3 / Bug 4c `6e1b45` (2026-05-12). Subtle root cause despite cloud `workout_log_sets.duration_secs` carrying correct values: `ExerciseSet.fromMap` at `lib/core/services/write_result.dart:102` only read canonical field `duration_sec`. Restored data flows through `SyncService._restoreExerciseLogs:2842-2853` which writes the legacy field name `duration_seconds`. When restored data hit any path that round-trips through `fromMap` (e.g. `WorkoutWriteService.logExercise` merge at `:96-99` or `editLog` recompute at `:643-647`), the field silently became `null` → next persist wrote per-set `sets[]` without duration → WardSetChips fallback rendered "N sets · 0s." Fix: `fromMap` accepts both `duration_sec` AND `duration_seconds`. 4-layer contract test (`test/contracts/timed_exercise_render_contract_test.dart`) covers active-workout write, render, edit, restore re-persist. Adjacent gap NOT fixed (deferred): EditWorkoutLogSheet `_ExerciseEditRow.fromLog:865` reads `log['sets_detail']` (legacy) not `log['sets']` (canonical) — restored timed logs show empty duration on edit. Separate batch. |
| `PostgrestException` 23505 spam on `upsert_exercise_log` / `upsert_nutrition_log` | Closed in audit 2026-05-12 P0-A + P0-B `3f8a91`. PostgREST's `onConflict` is a hint about which constraint to target. Passing `'id'` translates to `ON CONFLICT (id) DO UPDATE`. PostgreSQL still evaluates every unique constraint; if a DIFFERENT one trips first you get 23505 instead of a merge. Live schema has partial UNIQUEs `uniq_workout_log_exercises_wlog_ex_set` and `uniq_nutrition_logs_user_date_meal` that ARE the natural identity. Same exercise/meal re-logged under a slightly different Hive key produced a new deterministic id → no id collision → natural unique tripped → 23505 → orphaned per-set / per-item rows. 31 + 16 errors per 24h before fix. Fix: switch `onConflict` to the natural-key columns. Pinned by `test/contracts/sync_onconflict_natural_key_test.dart`. |
| [MOVED to supabase/functions/CLAUDE.md] Cron jobs send `Authorization: Bearer null` → 401 every tick | Closed by user action 2026-05-12 (audit P0 Vault fix). Cause: `private.morning_alert_get_service_key()` reads `vault.decrypted_secrets WHERE name='service_role_key'`; the Vault row had never been populated, so the function returned `NULL` and every cron job sent `Bearer null` → Edge Function gateway 401. 12 cron jobs affected (0 successful invocations across thousands of attempts). pg_cron reports "succeeded" for `net.http_post()` dispatches regardless of HTTP response — symptom invisible from `cron.job_run_details`. Fix: Dashboard → Settings → Vault → add secret named exactly `service_role_key` with the project's service_role JWT. **Don't add new cron jobs that hardcode the anon JWT** — always resolve via `private.morning_alert_get_service_key()` (migration 061 P1-D retrofitted `rolling-context-nightly` + `streak-guardian-daily` to this pattern). Server-side cron execution telemetry is still a gap — `client_errors` doesn't capture these failures (audit 2026-05-12 M2 follow-up). |
| Cloud upsert succeeds for per-set / per-item rows but parent row silently fails | Sub-class of the previous entry. Per-set rows in `workout_log_sets` and per-item rows in `nutrition_log_items` run in their own inner try-block; the outer parent-row catch posts telemetry but the loop continues. If `onConflict` is wrong on the parent, the children land without a parent → AI coach + weekly report can't see them. **Class fix rule:** every cloud upsert whose table has a natural UNIQUE must target that natural key, not `id`. Don't trust `id` as the conflict target unless the table has ONLY one unique constraint (the PK). |
| `0.0.0+release` in `client_errors.client_version` for every prod row | Closed in audit 2026-05-12 P2-A. Was a placeholder pending `package_info_plus` wiring. Replaced with `AppConstants.appVersion` constant which mirrors pubspec.yaml `version:` (manually kept in sync — `/build-apk` skill bumps both). Two callsites updated (`SyncService._currentClientVersion` + `ErrorTelemetry._currentClientVersion`). Future-proofing: when `package_info_plus` is wired, swap the constant for `PackageInfo.fromPlatform()` read at startup. |
| `ai_coach_interactions` shows 4× row inflation (`channel='in_app'` dupes) | Closed in audit 2026-05-12 P2-B. `ai-proxy` Edge Function writes the authoritative `ai_coach_interactions` row with `channel='app'` on every chat turn. Pre-fix, `SyncService._syncCoachInteractions` walked Hive `coachBox` and re-upserted each entry with `channel='in_app'`, producing 81 phantom rows vs 22 real rows in prod (79% noise). Fix: skip when `entry['id']` is a real UUID (server already wrote it); fall through with `channel='in_app_orphan'` only for the rare pre-server entry case. The orphan channel is intentionally distinct so future audits can detect if this path becomes hot. |
| Widget crash `'String' is not a subtype of type 'int?'` shows no stack in `client_errors` | Closed in audit 2026-05-12 P2-G. `ErrorWidget.builder` in `lib/app.dart` was sending only the 200-char exception string to `ErrorTelemetry.logEvent`. The stack was already captured for Crashlytics but not bridged to the server-side log. Now composes exception (200 chars) + `--stack--` separator + first 400 chars of `details.stack` so triage works without a Crashlytics seat. |
| `workout_templates.last_used_at` always NULL in cloud | Closed in audit 2026-05-12 P3-C. Cloud column was projected by `_syncWorkoutTemplates` and read by "recently used templates" sort, but nothing on the Hive side ever wrote it. `WorkoutScheduleService.assignTemplateToDate` now stamps `last_used_at = now()` on the parent template Map after the schedule write; caller's fire-and-forget `syncWorkoutData()` ships it to cloud on the next tick. Non-fatal failure mode — `last_used_at` is purely a sort hint. |
| `workout_logs.rpe` always NULL in cloud / weekly report shows "N/A" | Closed in audit 2026-05-12 P2-F. Cloud column existed; the client just never sent the field in `_syncWorkoutLogs`. Now: `if (log['rpe'] != null) 'rpe': log['rpe']`. Backfill is a one-shot SQL job (deferred) — historical rows stay NULL. Future logs carry the value. |
| [MOVED to root §3 (Milestone 6)] Master Audit / multi-agent surveys produce false-positive findings | Lesson from audit 2026-05-12 — **3 of 21 Master Audit findings were false alarms** (P1-A `coaching_notes`/`coach_notes` already correct; P1-B sync ordering already fixed in Test #14 / Bug B.1; P1-F terms columns existed on `users` not `user_profile`). Each was caught only by verifying live SQL + reading the actual code path. Rule: **never apply a multi-agent audit finding without first reading the cited file:line AND verifying claimed cloud state via live `information_schema` query.** Cite the verification SQL in the audit report so the next person doesn't re-do the work. Codified in `feedback_audit_findings_require_live_verification.md`. |
| Adding a Supabase MCP migration without updating `backups/applied_migrations.json` | Same pair-update rule from `feedback_migration_apply_record_pair.md` — every `apply_migration` MCP call MUST be paired with a `backups/applied_migrations.json` update in the same commit. Gate 14 of `/build-apk` enforces this pre-flight; clean discipline is to never split the pair in the first place. Audit 2026-05-12 migration 061 → applied_migrations.json updated in same docs commit. |
| Cross-account leak after signOut + signUp on same session (Edit Profile shows previous user's data) | Closed in Test #15.3 / Bug 5 `c4055a` (2026-05-12 afternoon). Founder signed up as sumitt@gmail.com on a session previously holding Upendra. Cloud `user_profile` had Sumit's correct data (175cm/75kg/2001-01-01); Edit Profile screen showed Upendra's (174/77.8/1988). Test #15.1 / Bug C's HiveUserSession+Lock correctly isolated the Hive layer, but **47 user-scoped Riverpod Notifier/Provider `build()` bodies** read Hive once and cached state — none watched any auth state source. On signOut+signUp, Hive boxes switched correctly via `HiveUserSession.openForUser`, but the Riverpod cache survived → stale state served to UI. **Fix:** new `authUserIdTokenProvider` at `lib/features/auth/providers/auth_invalidation_provider.dart` watches `currentUserProvider` + `authStateProvider` and emits `user.id ?? '<anon>'`. **56 user-scoped provider `build()` bodies add `ref.watch(authUserIdTokenProvider)` as their first line.** On auth change, the token re-emits → every downstream provider rebuilds against now-correctly-namespaced Hive. Exempt: `auth_provider.dart` + `auth_invalidation_provider.dart` (can't self-watch) + `referral_code_stash_provider.dart` (intentionally shared pre-auth crossing per CLAUDE.md §15). Source-grep contract test in `test/contracts/auth_invalidation_contract_test.dart` walks `lib/features/*/providers/` and fails on any provider that reads `UserRepository|HiveService.user*Box|MigratedKey|SubscriptionService|Supabase user-filtered tables` without watching the token. Behavioral test in `test/contracts/user_scoped_provider_rebuilds_on_auth_change_test.dart` simulates user-A → user-B switch and asserts `userProfileProvider` rebuilds. **Same writer/reader drift family as Tests #6 → #15.3 — just at the Riverpod state cache layer instead of Hive.** See `feedback_writer_reader_field_drift_recurring.md`. |
| EditWorkoutLogSheet shows empty fields when editing a timed (or any modern) exercise log | Closed in Test #15.3 / Bug 6 `e1f8a2` (2026-05-12 afternoon). `_ExerciseEditRow.fromLog` at `lib/features/train/widgets/edit_workout_log_sheet.dart` read only `log['sets_detail']` (pre-Test-#6 legacy field name). Modern `WorkoutWriteService.logExercise` writes canonical `log['sets']`. Editing a timed exercise log surfaced empty duration inputs because the sheet fell back to aggregate view when canonical was absent. **Fix:** dual-name reader — `(log['sets'] as List?) ?? (log['sets_detail'] as List?)`. Per-set duration also reads both `duration_sec` (canonical, post-Bug-4c) and `duration_seconds` (legacy, post-restore) — same pattern as `ExerciseSet.fromMap` from Bug 4c (`6e1b45`). `_ExerciseEditRow`/`_SetEditRow` renamed to `EditLogExerciseRow`/`EditLogSetRow` + `@visibleForTesting` for the contract test. 8 contract tests across 5 groups. Other readers of `sets_detail` audited — `workout_receipt_card.dart`, `train_screen.dart`, `workout_repository.dart`, `logging_type_repair_migrator.dart` — all already use `log['sets'] ?? log['sets_detail']` dual-name fallback. Edit sheet was the only outstanding gap. |
| Cloud `workout_log_exercises.duration_seconds` aggregate column always NULL/0 for timed exercises | Closed in Test #15.3 / Bug 7 `a2b3c4` (2026-05-12 afternoon). Cloud column existed but `SyncService._syncExerciseLogs` projection never populated it. Consumers worked around by summing per-set `workout_log_sets.duration_secs` — column was dead schema data, no analytics value, future Edge Functions joining on the table would see 0. **Fix:** projection now computes `duration_seconds = sum(per-set durations)` when `logging_type ∈ {timed, cardio}`, reading both `duration_sec` (canonical) and `duration_seconds` (legacy) per-set field names (same dual-name pattern as Bug 4c / Bug 6). Non-timed/cardio types write 0 (deterministic for downstream `SUM()` queries; column type is `integer NULL` per live schema check). 4 source-grep tests pin the projection block. Cardio durations confirmed stored in seconds via `active_workout_screen._captureSetValues` — no unit conversion needed. |
| Cross-account profile leak during live signOut+signUp on same session | Closed in APK Test #15.4 / B1 (2026-05-12). c4055a (Test #15.3 / Bug 5) made 56 user-scoped Riverpod providers `ref.watch(authUserIdTokenProvider)` so the cache invalidates on auth change, but the token re-emitted the moment Supabase fired `signedIn` — BEFORE `_ensureLocalUser` awaited `HiveUserSession.openForUser(newUid)`. Providers rebuilt against the previous owner's namespaced Hive (`userBox_<upendraHash>`) and cached Upendra's profile under the new sumit token. Cold start unaffected (splash awaits `openForUser` before UI mounts). **Fix is two-layer.** Layer A (correctness, `lib/core/services/guarded_box.dart`): `wrapUserScopedBox` returns `GuardedBox.empty(authUid)` when `Supabase.auth.currentUser.id != HiveUserSession.currentOwnerFullId`; the empty wrapper short-circuits all reads to null/empty/0/false/true and throws `StateError` on writes. Telemetry event `guarded_box_disagreement` per occurrence. Layer B (liveness, `lib/core/services/hive_user_session.dart` + `lib/core/services/hive_session_owner_provider.dart` + `lib/features/auth/providers/auth_invalidation_provider.dart`): `HiveUserSession.currentOwnerListenable` is a `ValueNotifier<String?>` mutated under `_sessionLock` from the 3 locked methods; `hiveSessionOwnerProvider` wraps it; `authUserIdTokenProvider` returns `'<anon>'` until authUid agrees with hiveOwner. Effect: providers rebuild to `<anon>` (Layer A serves empty) → listenable fires when `openForUser` completes → token re-emits real userId → providers rebuild against correctly-namespaced Hive. Pinned by `test/contracts/auth_invalidation_timing_test.dart` + `test/contracts/wrap_user_scoped_box_disagreement_test.dart`. closes-diagnose: 7bd154. |
| `exlog_*` Hive key duplicates — 3 rogue writer formulas bypass canonical `WorkoutWriteService.exlogKey` | Closed in APK Test #16.1 (2026-05-16). Founder's Train screen showed 26+ exercise rows for a day where he logged 4 exercises. `WorkoutWriteService.exlogKey` (line 823) is the canonical formula — UUID v5 of `lowercase+trim(name)` over IST date. **Two rogue writers** were producing different keys: (a) `sync_workout.dart:587` `_restoreExerciseLogs` wrote `'exlog_${dateStr}_${nameNormalized.hashCode}'` — Dart `String.hashCode` is non-deterministic across runs/devices, so same exercise produced different keys on every restore. (b) `workout_repository.dart:1133` `logSetWithPrRescan` (AI coach `logPR` tool) wrote `'exlog_${ms}_${hashCode}'` — every call produced a new key. Both fed `exercise_log_index_<date>` alongside canonical UUID-v5 keys → reader iterated index → rendered all entries → visible duplicates. **Same writer/reader drift class as Tests #6 → #15.4 — 7th instance.** "View Card nothing happened" from home calendar was a same-class symptom: `WorkoutReceiptData.fromExerciseLogs` filter rejected rogue-key rows that lacked `workout_log_id` → empty receipt → button no-op. **Fix:** (1) Both rogues now route through `WorkoutWriteService.exlogKey(date, name)`. (2) `ExlogKeyMigrator._migrationKey` bumped `_v7` → `_v8` so existing rogue rows re-key + merge into canonical (60s set-level dedup; idempotent). (3) `restoring_screen.dart` fires `unawaited(SyncService.instance.syncWorkoutData())` once after migrator runs so cloud catches up. (4) `workout_receipt_card.dart` fallback: when `exercise_log_index_<istDate>` is null/empty, scan `exlog_*` rows by stored `date` field (closes the UTC-vs-IST index gap that broke View Card). (5) New build **Gate 17** `scripts/check_exlog_key_canonical.dart` — source-grep fails if any code outside `WorkoutWriteService` constructs an `exlog_*` key directly. Pinned by `test/contracts/exlog_key_canonical_test.dart` + `test/contracts/exlog_migrator_handles_rogue_shapes_test.dart` + `test/contracts/receipt_legacy_rows_fallback_test.dart`. closes-diagnose: a16c1a. |
| Chat duplicates 3× — placeholder dedup gap + no client circuit-breaker | Closed in APK Test #16.1 (2026-05-16). Founder retried "Analyze with AI" 3× during a Gemini 502 storm. Each tap: client wrote new Hive `coachBox` entry + `ai-proxy` inserted a `pending` placeholder in `ai_coach_interactions`. Gemini failed each time → placeholders stayed `pending` forever. Sync later pushed client Hive entries as `in_app_orphan` siblings. Cloud accumulated **18 phantom rows** for one message (9 `food_text_analysis` + 9 `in_app_orphan`, 6 distinct groups). 3 visible duplicates in chat scroll. No dedup anywhere. **Fix four layers:** (1) Client `ai_coach_repository.saveUserMessagePending` adds 60s dedup window — same `(user_message, mode, media_url)` within 60s of an existing entry → UPDATE/skip instead of new INSERT. (2) Server `ai-proxy:207-260` `food_text_analysis` placeholder reservation SELECTs existing `pending` row for same `(user_id, channel, user_message)` within 60s; UPDATEs `created_at` on hit instead of inserting duplicate. (3) Migration 066 one-shot cleanup: DELETE `pending` rows in `[now() - 30d, now() - 5min)` window keeping `min(id)` per `(user_id, user_message, channel)` group. 10 of 18 rows deleted (verified). (4) `AiBreakdownNotifier` circuit-breaker — after 3 consecutive `502/503/504/service_error` for same text, 4th tap returns "AI overloaded — try again in 5 minutes" inline without contacting ai-proxy. 5-min cooldown; success/`clear()`/idle resets. Pinned by `test/ai_coach/coach_writer_dedup_test.dart` + `test/ai_coach/circuit_breaker_test.dart` + `test/contracts/cleanup_pending_migration_safety_test.dart`. closes-diagnose: a17bc3. |
| Photo analysis returns 500 (not 502) — not retryable + broken-image bubble | Closed in APK Test #16.1 (2026-05-16). Edge Function logs showed `ai-media-proxy POST 500 execution_time 14.5s` when founder sent a photo. +25's `retryColdStart` schedule covers `502/503/504` only — **500 falls through immediately**. Worse, the chat bubble already rendered with a broken-image placeholder, suggesting the photo URL never made it into the bubble payload. **Root cause:** ai-media-proxy's generic catch at lines 487-498 returned 500 for EVERY thrown error including SSRF rejects (lines 119), Storage 404s (line 129), oversize images (lines 135/140), bad JSON body (line 211), and Postgres failures (lines 257/290/444). All five paths now distinguish: SSRF/oversize/bad-body → **400 validation**, Storage 404 → **400 storage** ("Image upload incomplete — please retry"), Storage 5xx/network → **502 upstream** (retry-eligible), Gemini empty content → **502 upstream**, genuine bugs → **500 internal** with `request_id`. Response body adds `error_type` field so client can dispatch. **Chat-bubble render contract:** `ChatBubble` adds `mediaFailed` + `onMediaRetry` props; renders red-bordered photo-failed tile with "Tap to retry" when `mediaFailed=true` OR `mediaUrl` null/empty OR `CachedNetworkImage.errorWidget` fires. `ai_coach_provider.markLastUserMediaFailed()` helper fires on `isStorageFailure` catch branch. **Intentionally NOT modified:** `supabase_service.retryColdStart` trigger set — adding 500 would amplify error rates during real server incidents. Instead, the fix reclassifies upstream-class 500s as 502 so the existing budget naturally absorbs them. Pinned by `test/contracts/ai_media_proxy_status_code_classification_test.dart` + `test/widgets/chat_bubble_photo_failure_test.dart`. ai-media-proxy v17 deployed. closes-diagnose: 913261. |
| `log-client-error` rate limit silently drops telemetry past 100/24h — observability blackout | Closed in APK Test #16.1 (2026-05-16). `log-client-error/index.ts:142-147` had `DAILY_RATE_LIMIT = 100`. When count exceeded, function returned `200 {ok:true, rate_limited:true}` without INSERTing. Founder's 47-row 42P10 storm pushed him past the limit by 04:10 UTC; **every subsequent client-side error since (hundreds of log-client-error 200s in Edge Function logs) was silently dropped**. We had zero visibility on +25's failures. Hidden bug — could have shipped indefinitely without detection. **Fix:** (1) Limit bumped 100 → **2000** (40× safety margin). (2) New `HIGH_PRIORITY_OP_TYPES` allowlist (17 entries — `crash_*`, `auth_failure_*`, `42P10`, `23505`, `23503`, `permission_denied`, `gate16_violation`, `writer_reader_drift_*`, `sync_failure_dead_letter`, etc.). HIGH ops ALWAYS insert, bypassing the limit. LOW ops share the 2000 budget. (3) Response body adds `next_window_at` ISO timestamp (24h-from-oldest-event) so client backs off precisely. (4) Response also adds `priority_lane: "high"|"low"` for telemetry-pipeline debugging. (5) Client `ErrorTelemetry` honors `rate_limited` — sets in-memory `rateLimitedUntil`; LOW ops short-circuit before network when cooldown active; HIGH ops always POST (server priority lane handles). Crashlytics leg always runs (separate sink). **Class rule:** any rate-limited telemetry sink MUST return distinguishable signal AND client MUST honor it; tracked in `feedback_observability_silent_drop.md`. Pinned by `test/safety/error_telemetry_rate_limit_test.dart` (24 cases) + `supabase/functions/log-client-error/index_test.ts` (Deno 10 cases). log-client-error v7 deployed. closes-diagnose: 9d12af. |
| PostgREST 42P10 — `ON CONFLICT` arbiter rejection on partial unique indexes | Closed in APK Test #16 (2026-05-15). Audit 2026-05-12 P0-A/P0-B `3f8a91` switched `onConflict` from `'id'` to natural-key columns (e.g. `'user_id,date,exercise_name'`) targeting partial UNIQUE indexes whose `WHERE (col IS NOT NULL AND ...)` predicate the planner cannot statically prove from the table schema when ANY arbiter column is NULLABLE. Result: every upsert hit `42P10 "there is no unique or exclusion constraint matching the ON CONFLICT specification"` and silently failed. Founder's 2026-05-15 testing produced **0 cloud rows in `workout_logs` / `workout_log_exercises` / `workout_log_sets` across 48 hours** (47 client_errors rows in 60 s for one user). Three tables affected: `workout_logs` (nullable `date`, `exercise_name`), `workout_log_exercises` (nullable `workout_log_id`), `nutrition_logs` (nullable `meal_type`). `workout_log_sets` was unaffected — already non-partial UNIQUE + all NOT NULL. **Fix:** migration 064 backfilled the 4 nullable columns with writer-contract-aligned defaults (verified 0 actual NULL rows pre-flight; defensive no-op), `ALTER COLUMN ... SET NOT NULL`, dropped the 3 partial UNIQUE indexes, recreated as non-partial. **Belt-and-suspenders:** client guards at 4 upsert sites in `sync_workout.dart` / `sync_nutrition.dart` skip the upsert + emit `sync_skipped_null_natural_key` telemetry if any natural-key column is null. **New permanent gate:** `scripts/check_onconflict_live_arbiter.dart` + `test/sql/onconflict_live_arbiter.sql` runs every `INSERT ... ON CONFLICT` shape from sync/ against the live schema inside a rollback transaction; any 42P10 fails the check. Closes the writer→DB-target drift test gap (was source-grep only). Pinned by `test/contracts/sync_natural_key_guard_test.dart` + `test/contracts/sync_onconflict_natural_key_test.dart`. closes-diagnose: 76c8f4 + 9f4ab2 + 25e91d. **Class rule:** any new partial UNIQUE index that's intended to back `ON CONFLICT` MUST have all arbiter columns NOT NULL, OR the index must be non-partial. See `feedback_partial_unique_arbiter_trap.md`. |
| AI "temporarily unavailable" on first-tap photo / food-text analysis | Closed in APK Test #16 (2026-05-15). Test #15.3 / Bug 2 widened `retryColdStart` schedule to `[1500, 4000]ms` (~5.5 s) but Edge Function logs from 2026-05-15 09:33 IST show 3× `ai-proxy 502` with `execution_time` 6.1 / 6.6 / 7.2 s — the function itself times out before Gemini answers, well past the retry budget. Also `ai-media-proxy` callsites (`AiService._directMediaHttpCall` + `_directHttpCall` web/CORS fallbacks) bypassed `retryColdStart` entirely. **Fix:** bumped schedule to `[2000, 6000, 12000]` (3 retries, ~20 s window). Added 504 to retry-trigger status set (was 502 + 503 + cold-start). Wrapped both `_directHttpCall` and `_directMediaHttpCall` in new `_retryHttpColdStart` helper with same gate. Worst-case user-perceived wait: ~20 s on cold-start (vs. hard error pre-fix). Telemetry op_type `edge_function_cold_start_retry` per attempt for ops visibility. Pinned by `test/contracts/edge_function_cold_start_retry_behavioral_test.dart` (5 new cases: 3×502+200 succeeds, 4×502 exhausts, 504 retries, 502+500 stops retry, 504+500 stops retry). closes-diagnose: c01d57. |
| Swap exercise picker doesn't surface restored custom exercises | Closed in APK Test #16 (2026-05-15). Founder reported searching "Single Leg Front" in active-workout SWAP EXERCISE sheet returned "No matching exercises found" despite `Single Leg Front Lever` being present in cloud `user_custom_exercises` (2 rows for upendra). Root cause: `SyncService._restoreCustomExercises` writes cloud rows to `customBox` keyed `custom_exercise_<id>` but the cloud table has no `type` column — restored entries land WITHOUT `type:'exercise'`. Reader `ExerciseRepository.getCustomExercises` filtered `if (ex['type'] == 'exercise')` → silently dropped every restored entry across swap sheet, active-workout add-picker, template builder. Writer/reader drift class again. **Two-layer fix:** (a) reader at `exercise_repository.dart:322` now iterates `customBox.keys` and accepts entries whose key starts with `custom_exercise_` OR whose `type` is `'exercise'` — unblocks already-restored devices; (b) writer `sync_community._restoreCustomExercises` stamps `item['type']='exercise'` before put — belt-and-suspenders for legacy readers. Swap sheet empty-state now fires only when BOTH library AND custom results are empty (pre-fix the "No matching exercises found" string rendered above non-empty custom rows). Custom entries show with gold-bordered `CUSTOM` badge. Pinned by `test/widgets/swap_sheet_custom_exercises_test.dart` (8 tests). closes-diagnose: a5d29c. |
| [MOVED to supabase/functions/CLAUDE.md] `pr-detection` cron loops 401 every 15 min despite audit P1-D fix | Closed (operational fix flagged to founder) in APK Test #16 (2026-05-15). Audit 2026-05-11 C-4 / 2026-05-12 P1-D `private.morning_alert_get_service_key()` retrofit was applied correctly to every cron entry — the `cron.job.command` for `pr-detection` (jobid 9) USES `Bearer ' || private.morning_alert_get_service_key()`. The Vault row IS populated (219-char real JWT). **Actual root cause:** the in-function gate at `supabase/functions/pr-detection/index.ts:56` compares `token === Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")`. Between 2026-05-11 and 2026-05-15 the Vault-stored JWT and the env-injected `SUPABASE_SERVICE_ROLE_KEY` drifted (likely Supabase platform-side rotation or someone re-saved Vault). Equality check fails → 401. **Operational fix (founder-only):** Dashboard → Settings → API → copy current service_role JWT → Dashboard → Settings → Vault → edit row `service_role_key` → paste → save. **Class fix deferred:** replace the brittle env-equality check in `_shared/cron_auth.ts` with a JWT signature+role decode. Migration 065 ships a sanity-audit DO block that warns on hardcoded JWTs or `app.settings.service_role_key` (which returns NULL on this project — flagged jobid 7 `promote_community_item_daily` as separate deferred bug). Same shape affects every C-4-gated function (re-engagement, plateau-alert, protein-gap-alert, workout-window-closing, streak-guardian, evaluate-rank-promotions, i-see-you-callout, clean-orphan-media, expiry-reminder, weekly-recap-ready) — all latent on the same drift. closes-diagnose: 5a65bd. |
| Muster answers don't reach Edit Profile or plan generator | Closed in APK Test #15.4 / B2 (2026-05-12). `MusterScreen` (post-onboarding 5-question chat) wrote every answer to `coachBox` only — the AI coach saw them, but Edit Profile (which reads `userBox['profile']`) saw defaults `injuries=['none']` / `physique_focus='balanced'`. Founder reproduced after sumit signup ("shoulders as injury, legs as focus" never reached profile). Same writer/reader drift class as Test #6 → #15.3. **Fix scope (4 changes shipped together):** (1) `MusterScreen` Q1 (`why_now`) + Q2 (`definition_of_winning`) dropped per founder direction — "stupid questions" with low signal, high friction; remaining 3 questions renumbered, progress bar 5→3 dots. `InductionService._allowedMusterKeys` retained those keys so legacy data round-trips on read. (2) `InductionService.recordMusterAnswer` now calls new `_bridgeToProfile` after the coachBox write — switches on key, writes the corresponding profile field via `UserRepository.updateProfileFields`, fire-and-forget `syncProfileNow + pushSnapshot` per CLAUDE.md §15. Mappings: `known_injuries→injuries`, `typical_wake_time→wake_up_time`, `preferred_workout_time→preferred_workout_time` (new column, migration 063), `body_part_priorities[0]→physique_focus`. (3) Q5 rewritten from 8-chip multi-select to 4-chip single-select matching `physique_focus` enum (balanced / glutes_legs / chest_shoulders_arms / strength) — enables identity 1:1 bridge. Stored as 1-element List in coachBox so AI context reader is unchanged. (4) One-shot `backfillMusterToProfileIfNeeded` runs from `_ensureLocalUser` after `UserConfigMigrator.runIfNeeded`, gated by `migrationBox['muster_bridge_backfill_v1_done']`. Only writes to default-valued profile fields — never clobbers user edits; skips legacy multi-select `body_part_priorities`. Pinned by `test/contracts/muster_profile_bridge_test.dart` + `test/contracts/muster_question_count_test.dart` + `test/contracts/muster_bridge_backfill_test.dart`. closes-diagnose: 8c4ee3. |
| **AUDIT 2026-05-17 — OI-11..OI-18 closure batch + cron telemetry helper** | Operational hardening batch — no user-visible bug fixes. Closures: **OI-11** live-verified the 4 stale cron Edge Function deploys (pr-detection / evaluate-rank-promotions / clean-orphan-media / promote-community-item) silently running the pre-audit-2026-05-16-E.14.C env-equality JWT compare; retrofitted `promote-community-item` to `isAuthorizedCronCall(req)`, redeployed all 4 (v5→v6 / v5→v6 / v4→v5 / v9→v10); post-deploy SQL confirmed `proactive_pr_detection` jobid 9 succeeded 2/0 and `morning_alert_deliver_early` jobid 17 also recovered (was flagged still-401 in pre-batch memory but Vault refresh + own deploys cleared it). **OI-12** RLS audit of all 47 public.* tables — 0 P0 cross-user-read leaks; 2 P1 functional gaps (added `client_errors_select_own` policy via migration 069); 5 P2 advisory findings documented in open_issues.md. **OI-15** new `supabase/functions/_shared/cron_telemetry.ts` helper (`logCronStart` / `logCronEnd`) wired into the 4 cron functions above; migration 069 scheduled `cron_call_log_cleanup_daily` at 03:30 UTC (7-day retention via `cleanup_cron_call_log()`). Pinned by `test/contracts/cron_telemetry_adoption_test.dart` (17 tests). **OI-13** verified all 74 skipped tests are intentional Phase 7 scaffolds awaiting Phase 8 device-CI harness; no action. **OI-14** audited input-validation parity across 18+12 Edge Functions; agent's P1 finding on `assess-body-composition` was a false alarm (request_id IS returned at line 192) — verified live per `feedback_audit_findings_require_live_verification.md`. **OI-16** verified ProGuard agent finding was false alarm — `minifyEnabled` is NOT set in `release` buildType (Flutter default `false`), so keep rules are dormant + APKs +25/+26 ship cleanly. **OI-17** Hive compaction verified GREEN (8 boxes covered including `notificationsBox`; CLAUDE.md docstring updated from "7" → "8"). **OI-18** Storage audit: 4 buckets / 20 objects / 4.01 MB / $0 monthly cost; 0 orphans; `coach-media` bucket missing despite delete-account reference (tracked as OI-23) + bucket-level size/MIME caps absent (OI-24). Migration 069 applied. 8 remaining cron Edge Functions still un-wired with telemetry → OI-21 follow-up. Source: `docs/audit/open_issues.md` OI-11..OI-18 closure sections. No diagnose-docs (operational + audit closures; no `^fix:` commit subjects). |
| **HERMES AUDIT 2026-05-17 — Phase D methodology infra + lens-scan follow-ups (OI-42 + OI-43)** | Final Hermes batch — methodology infrastructure (5 permanent gates) + 8-lens parallel scan with follow-up OIs. **OI-42** shipped 5 new permanent gate scripts: `check_doc_internal_consistency.dart` (Gate 18, L25), `check_schema_payload_parity.dart` (Gate 19, L22), `check_unawaited_has_error_sink.dart` (Gate 20, L34 advisory), `check_restore_round_trip_coverage.dart` (Gate 21, L39), `check_telemetry_pii_classification.dart` (Gate 22, L40 advisory). Gates 20+22 default to advisory mode pending 50+ extant findings (tracked OI-44). Gate 19 caught 6 NOT-NULL columns across 7 audited tables — all currently covered. Gate 21 catches 16 sync methods with paired restore (+ allowlist for orchestrators). **OI-43** dispatched 8 parallel subagents across lenses L26/L27/L28/L30/L31/L36/L37/L38. Zero P0 surfaced. Findings filed as 8 follow-up OIs (OI-44..OI-51) in `docs/audit/open_issues.md` with effort estimates + fix shapes. Headline findings: L26 10 CQRS violations (isPro/gate/checkAndUnlock), L27 4 unguarded races (UsageCounterService critical), L28 3 client-side-only invariants (AI text/scan meal/onboarding), L30 5 unsanitized prompt interpolations (morning-alert + future-prediction high-risk), L31 3 cron functions O(all users) at 10K scale, L36 ZERO gaps (all 8 critical paths hardened — closed as OI-49 GREEN), L37 23 risky readers (6 crashes + 17 silent-wrong), L38 3 unwired SDK clear-on-signOut (Crashlytics + OneSignal + Razorpay callbacks). Phase D total: 5 gate scripts + 8 follow-up OI entries + 1 closed-without-fix verification (OI-49). Closes the full Hermes audit batch — 16 of 18 OIs CLOSED with ship + deploy, 1 closed-by-verification (OI-49), 1 deferred-to-future-batch (OI-25). closes-oi: OI-42 + OI-43 + OI-49. |
| **HERMES AUDIT 2026-05-17 — Phase C P2 process + writer/reader (OI-32..OI-41 minus OI-36..OI-37)** | Same Hermes audit, 8 P2s + a verification (OI-32) shipped together. **OI-32** delete-account Storage purge was non-recursive — nested user paths survived account deletion. New `listAllObjectsRecursive` DFS helper + chunked `.remove()`. delete-account v2→v3. **OI-33** APK size gate silent-skipped when artifact missing — added `--release` flag for FAIL-on-missing mode. **OI-34** new `scripts/check_migrations_live.dart` hits Supabase Management API to verify migrations applied live (companion to snapshot-based Gate 14). **OI-35** CLAUDE.md §2 ("21 tables") + AGENTS.md drifted from §7 ("46 Tables"); both corrected; new permanent `scripts/check_doc_internal_consistency.dart` Gate 18 pins drift pairs (table count, hive box count). **OI-38** StreakFreezeNotifier.build() was a Riverpod write-on-read anti-pattern — `_refillIfNewWeek()` extracted into `StreakProgressService.refillIfNewWeek()`; invoked from `DayRolloverObserver._doRolloverWithRef`. **OI-39** train_provider's `_getLastPerformance` + `exerciseHistoryProvider` iterated workoutBox inline — new `WorkoutReadService.logsForExercise(name)` cross-date scan; both delegate now. **OI-40** paywall_sheet_phase_variant CTA was `Navigator.pop()`-only (dead-end for free users hitting Phases 2-12 lock) — escalates to canonical `showPaywallSheet(context, feature: 'Phases 2-12')` for one purchase pipeline. **OI-41** Profile read cached `current_streak_weeks` while Home + Rank used live `WorkoutRepository.currentStreak()` — Profile migrated to canonical source; cached field stays for weekly-aggregate reports (different semantic). 15 new contract tests in single consolidated `phase_c_oi_closures_test.dart` + 8 diagnose-docs. 1 Edge Function deploy (delete-account). closes-diagnose: a2d0e1 + c84e33 + 1c3401 + d0c352 + 5fe338 + 39ead9 + 40c401 + 41507e. |
| **HERMES AUDIT 2026-05-17 — Phase B P1 security + writer/reader (OI-28 + OI-30 + OI-31 + OI-36 + OI-37)** | Same Hermes audit, 5 P1s shipped together with shared deploy cycle. **OI-28** ai-media-proxy SSRF: `fetchImageAsBase64` now requires `authUserId` + asserts `path.startsWith(${authUserId}/)` matching the Storage RLS shape. New `parseStorageUrl` helper + `ALLOWED_BUCKETS` allowlist {chat-media, coach-media, progress-photos}. 403 authorization on path mismatch. ai-media-proxy v17→v18. **OI-30** clean-orphan-media was deleting from `coach-media` (long-term consented per migration 070) instead of `chat-media` (transient). Migration 071 renamed RPC `find_orphan_coach_media` → `find_orphan_chat_media`; Edge Function flipped both the bucket + RPC call. clean-orphan-media v5→v6. Bucket was empty at deploy time (consent UI deferred per OI-25) so no data loss. **OI-31** 3 cron Edge Functions (expiry-reminder, morning-alert, rolling-context) had telemetry-only wiring (OI-21) but lacked `isAuthorizedCronCall(req)` auth gate — public POST could trigger Gemini fan-out / OneSignal push fan-out / cost burn. Mirrored clean-orphan-media's canonical gate pattern. 3 deploys. Scope narrowed by live `cron.job` query — the 6 other "unwired" service-role functions Hermes flagged are NOT in active cron (compute-coach-signals uses RPC directly; weekly-recalc/report/snapshot/beat-my-coach/future-prediction are verify_jwt=true with own JWT validation; admin-* are 410 Gone). **OI-36** NutritionProvider.deleteFoodLog bypassed NutritionWriteService — 10th writer/reader drift instance. recent_deletes audit-log behavior moved INTO `NutritionWriteService.deleteLog` (new `writeAuditLog: bool = true` param). Notifier now delegates. **OI-37** RankService updated cloud `user_profile.current_rank_code` but local Hive served stale rank to all rank widgets until next sync. Mirrored NutritionWriteService.onStateChanged pattern — `RankService.onStateChanged` callback wired in app.dart to `ref.invalidate(userProfileProvider)`; service updates local Hive via `UserRepository.updateProfileFields` after the cloud write. 5 contract tests (45 cases total: ai_media_proxy_user_scope + cron_auth_adoption + nutrition_delete_routes + rank_service_local_profile_update). 5 diagnose-docs `5e055f` / `c1ea30` / `c4031b` / `d1e7e6` / `4a37e7`. closes-diagnose: same. |
| **HERMES AUDIT 2026-05-17 — Phase A P0 payment-blockers (OI-26 + OI-27 + OI-29)** | External Hermes cross-check on 2026-05-17 evening surfaced 3 P0 payment-blockers our in-house audit missed. **OI-26** razorpay-webhook used `supabaseClient` on line 301 (H-19 idempotency pre-SELECT) but declared the const on line 431 → temporal dead zone `ReferenceError` on every webhook invocation that wasn't an early-return. Razorpay retried for 24h with the same TDZ. **OI-27** migration 052 (2026-05-13) made `razorpay_signature` NOT NULL; verify-payment validates via REST API not HMAC and never sent the column → 23502 not_null_violation on every fallback path after webhook lag. **OI-29** verify-payment ownership check `if (notesUserId && notesUserId !== userId)` was fail-open when `notes.user_id` was absent (legacy/manual payments). Combined: paying user → webhook threw TDZ → fallback threw NOT NULL → PRO never unlocked. **Fix:** moved const to line 300 before first use; added `razorpay_signature: 'verified_via_api:' + paymentId.substring(0,12)` sentinel to both insert payloads; replaced single ownership check with two-step (400 missing + 403 mismatch). Razorpay-webhook v17→v18 + verify-payment v12→v13 deployed. 3 contract tests pin: handler-scoped declaration-before-use order, schema-vs-payload parity for every NOT NULL column on subscriptions, two-step guard order. 3 diagnose-docs `9a7c14` / `b3e052` / `c8f229`. **Class lesson:** introduced lens registry `docs/audit/LENS_REGISTRY.md` with 41 canonical lenses; today's in-house audit didn't cover L21 (Edge Function semantic correctness), L22 (schema-vs-payload parity), L23 (service-role authz defense-in-depth). Memory `feedback_audit_verifier_cannot_trust_own_subagent.md` codifies the near-miss where my own verification subagent mis-labeled OI-26 as FALSE_ALARM by conflating source order with execution order. closes-diagnose: 9a7c14 + b3e052 + c8f229. |
| **AUDIT 2026-05-16 — comprehensive 22-task fix batch (APK Test #16.2)** | Phase A→D + E.1-E.17. Largest single-batch sweep since Test #11. Headline closures: **F3-1.1 coach_notes upward sync (9th writer/reader drift instance)** — `syncCoachMemoryNow` now projects Hive `coaching_notes` → cloud `coach_notes`; AI memory no longer lost on reinstall. **F6-2 logPR through WorkoutWriteService (8th drift)** — `tool_dispatcher._executeLogPR` routed off legacy `WorkoutRepository.logSetWithPrRescan` onto canonical `WorkoutWriteService.logExercise`. **F6-3 + F6-4 ai-proxy placeholder resolution (v66 deployed)** — every exit branch awaits `resolvePlaceholder` (success / failed_gemini / failed_parse); cross-channel orphan dedup via 5-min SELECT lookback in `sync_coach._syncCoachInteractions`. **F11-C11-2 WorkoutScheduleService refactor** — 9 schedule mutations now route through `WorkoutWriteService.upsertScheduled` (mutex + sync + invalidation); 3 non-schedule writes (plan-key + template metadata) keep direct put + explicit `unawaited(SyncService.instance.syncWorkoutData())`. **F-A HealthWriteService** — new canonical writer for sleep/weight/measurement/water/urine/hydration, mirrors WorkoutWriteService pattern, IST baked into every date key (closes F2-R2 sleep IST drift). **E.11 legacy widgets deleted** — `RankChip` + `RankInsignia` files removed, 5 callsites migrated to `WardRankInsignia`. **E.12 17 dead columns dropped** — migration 067 across `workout_logs`/`workout_templates`/`template_exercises`/`user_progress`/`user_preferences`/`ai_coach_interactions`/`workout_log_exercises`; client sync projections trimmed to match. **E.2 appVersion bump** — `1.0.0+23` → `1.0.0+27` + new `scripts/check_app_version_matches_pubspec.dart` build gate. **E.8 dead-code purge** — 3 feature constants + `softDeleteAccount` method + `MySubmissionsScreen` route deleted. **E.10 referral restore methods** — `_restoreReferralCodes` + `_restoreReferralRedemptions` added to `sync_restore_completeness.dart`. 11 new diagnose-docs, 11 new contract tests, 1 new gate script, 1 new live migration. **Class lesson:** every batch since Test #6 has surfaced a writer/reader drift instance. The audit reframed F8.3 (9 inline `if (isPro)` widget reads) as audit-OK after per-callsite verification (3 action callbacks + 4 constructor-props with reactive callers + 2 notifier methods — none in the Test #12 PRO-upgrade-unlock pattern). Source: `docs/audit/2026-05-16/findings-verified.md` + per-agent reports + `docs/diagnoses/2026-05-16-*.md` (11 docs). |
